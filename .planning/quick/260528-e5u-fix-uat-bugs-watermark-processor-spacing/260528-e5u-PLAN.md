---
phase: quick
plan: 260528-e5u
type: execute
wave: 1
depends_on: []
files_modified:
  - WatermarkRemove/ui/components/watermark_processor.py
autonomous: true
requirements:
  - UAT-FIX-SPACING
  - UAT-FIX-AUTO-CLEANUP

must_haves:
  truths:
    - "Cuando los panel_seleccion y panel_auto se expanden dentro del QStackedWidget, sus controles internos quedan compactados arriba y el espacio sobrante va al fondo (no se reparte entre items)."
    - "Al cambiar desde modo Automático (mode 2) a Selección (0) o Recorte (1) vía set_mode(), el preview YOLO desaparece de la imagen y la lista de detecciones queda vacía sin requerir cambio de imagen ni acción adicional del usuario."
  artifacts:
    - path: "WatermarkRemove/ui/components/watermark_processor.py"
      provides: "set_mode() con cleanup completo de auto state + addStretch(1) en ambos layouts"
      contains: "seleccion_layout.addStretch"
  key_links:
    - from: "WatermarkProcessor.set_mode(0|1)"
      to: "preview_changed signal + request_redraw signal + detected_marks/detections_list cleanup"
      via: "auto-mode cleanup block (espejo de _toggle_auto_mode rama else)"
      pattern: "preview_changed\\.emit\\(None\\)"
---

<objective>
Cerrar dos bugs reportados en UAT de Phase 4 sobre `WatermarkRemove/ui/components/watermark_processor.py`:

1. **Spacing (UAT-FIX-SPACING):** los QVBoxLayout `seleccion_layout` y `auto_layout` no terminan con `addStretch(1)`. Cuando el `QStackedWidget` del slideshow_viewer expande estas páginas, Qt reparte el espacio sobrante entre items en lugar de empujar todo hacia arriba. Resultado visual: controles dispersos en vertical.

2. **Auto-cleanup (UAT-FIX-AUTO-CLEANUP):** `set_mode()` al salir del modo Automático (2 → 0 o 2 → 1) sólo cambia `auto_mode_enabled = False` y no limpia el preview ni el state asociado. El usuario queda con el preview YOLO superpuesto y la lista de detecciones poblada hasta que cambia de imagen. Esto difiere del comportamiento de `_toggle_auto_mode` (rama else, líneas 1368-1375), que sí limpia todo.

Purpose: dejar la UX coherente con D-06 (cambio de modo via `set_mode`) y con el cleanup ya establecido por `_toggle_auto_mode`. Sin nuevas features, sin tocar API pública.

Output: el mismo archivo `watermark_processor.py` con dos cambios localizados y verificables visualmente + por grep.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/04-visual-polish/04-CONTEXT.md
@WatermarkRemove/ui/components/watermark_processor.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix spacing y auto cleanup en watermark_processor.py</name>
  <files>WatermarkRemove/ui/components/watermark_processor.py</files>
  <action>
Aplicar dos ediciones puntuales sobre `WatermarkRemove/ui/components/watermark_processor.py`. Ambas viven en el mismo archivo y son independientes; se agrupan en una sola tarea por tamaño mínimo (~5% contexto).

**Edit A — Spacing (UAT-FIX-SPACING):**

A.1 En `_setup_ui()`, dentro del bloque del `seleccion_group` (QGroupBox "📁 Selección"), insertar `seleccion_layout.addStretch(1)` como última instrucción del layout, inmediatamente ANTES de `outer.addWidget(seleccion_group)` (actualmente línea 306). El stretch debe quedar después de `seleccion_layout.addLayout(manual_confirm_layout)` y antes del `outer.addWidget(seleccion_group)`.

A.2 En el mismo método, dentro del bloque del `auto_group` (QGroupBox "🤖 Detección automática"), insertar `auto_layout.addStretch(1)` como última instrucción del layout, inmediatamente ANTES de `self.auto_group.hide()` (actualmente línea 376). El stretch debe quedar después de `auto_layout.addLayout(auto_btns)` y antes de `self.auto_group.hide()`.

Razón: cuando el `QStackedWidget` del slideshow_viewer asigna al panel una altura mayor que la suma de los items, Qt sin stretch reparte el sobrante entre los espacios. El `addStretch(1)` al final actúa como item elástico al fondo y mantiene los controles compactos arriba — patrón estándar de Qt para "controles arriba, espacio abajo".

NO agregar stretch en `recorte_layout` (línea 398) porque ya tiene `recorte_layout.addStretch(1)`. NO mover ni reordenar otros widgets.

**Edit B — Auto cleanup en set_mode (UAT-FIX-AUTO-CLEANUP):**

B.1 En `set_mode()` (línea 1608+), reemplazar la rama `mode_index == 0`. Actualmente sólo hace:
- if crop_mode_enabled: crop_mode_enabled=False; request_redraw.emit()
- if auto_mode_enabled: auto_mode_enabled=False

Cambiar a: capturar `was_auto = self.auto_mode_enabled` antes del bloque. Después de poner `auto_mode_enabled = False`, si `was_auto` era True, ejecutar el mismo cleanup que la rama else de `_toggle_auto_mode` (líneas 1367-1375):
- `self.detected_marks = []`
- `self.selected_mark_index = -1`
- `self.auto_preview_image = None`
- `self.detections_list.clear()`
- `self.preview_changed.emit(None)`
- `self.request_redraw.emit()`

NOTA: el `request_redraw.emit()` que ya existe dentro del `if crop_mode_enabled` debe quedarse como está (el cleanup auto emite uno adicional cuando aplique; los redraws son idempotentes). Pero para evitar emisión doble cuando NO había auto activo, el `request_redraw` de la rama auto sólo se emite si `was_auto` era True (queda envuelto en el `if was_auto:`).

B.2 En la rama `mode_index == 1` (línea 1628+), aplicar el mismo patrón: capturar `was_auto = self.auto_mode_enabled` al inicio de la rama. Después de `self.auto_mode_enabled = False` (que ya está), si `was_auto`: ejecutar el mismo bloque de 6 instrucciones de cleanup (detected_marks, selected_mark_index, auto_preview_image, detections_list.clear, preview_changed.emit(None), request_redraw.emit()).

B.3 NO modificar la rama `mode_index == 2`. NO tocar `_toggle_auto_mode` (es la fuente canónica del cleanup; preservarla evita drift).

Notas de implementación:
- Mantener el comentario `# Phase 4 (D-06)` y el docstring de `set_mode` intactos.
- Usar exactamente los mismos nombres de atributos que `_toggle_auto_mode` para que un grep futuro (`grep -n "preview_changed.emit(None)" watermark_processor.py`) muestre ambos sitios y delate cualquier divergencia.
- No introducir helper privado `_clear_auto_state()` — el alcance es UAT cerrar bugs, no refactor. Si más adelante el cleanup se necesita en un tercer sitio, ese refactor pertenece a otro plan.

Resultado esperado del diff: ~2 líneas insertadas para spacing + ~14 líneas insertadas (7 por cada rama) para auto cleanup. Total: ~16 líneas añadidas, 0 eliminadas.
  </action>
  <verify>
    <automated>cd "C:\Users\Felix\Desktop\Python\manhwa_ocr\SmartStitch" && python -c "import ast; tree = ast.parse(open('WatermarkRemove/ui/components/watermark_processor.py', encoding='utf-8').read()); print('SYNTAX_OK')" && python -c "src=open('WatermarkRemove/ui/components/watermark_processor.py', encoding='utf-8').read(); import re; sel=len(re.findall(r'seleccion_layout\.addStretch\(1\)', src)); aut=len(re.findall(r'auto_layout\.addStretch\(1\)', src)); pc=len(re.findall(r'preview_changed\.emit\(None\)', src)); dc=len(re.findall(r'detections_list\.clear\(\)', src)); assert sel==1, f'seleccion stretch count={sel}'; assert aut==1, f'auto stretch count={aut}'; assert pc>=3, f'preview_changed(None) count={pc} (esperado >=3: revert, toggle_auto else, set_mode x2)'; assert dc>=3, f'detections_list.clear count={dc} (esperado >=3: on_image_changed, toggle_auto else, set_mode x2)'; print('GREP_OK')"</automated>
    <human-check>
Abrir SmartStitch, ir a WatermarkRemove, abrir el slideshow viewer. Con ventana alta:
1. Modo Selección: los controles deben quedar agrupados arriba con espacio vacío al fondo (no dispersos verticalmente).
2. Modo Automático (si YOLO disponible): igual — controles arriba, espacio vacío al fondo.
3. Cambiar de Automático a Selección con detecciones visibles: el preview YOLO debe desaparecer de la imagen y la lista de detecciones quedar vacía inmediatamente, sin cambiar de imagen.
4. Cambiar de Automático a Recorte con detecciones visibles: idem (3).
    </human-check>
  </verify>
  <done>
- `seleccion_layout.addStretch(1)` aparece exactamente 1 vez antes de `outer.addWidget(seleccion_group)`.
- `auto_layout.addStretch(1)` aparece exactamente 1 vez antes de `self.auto_group.hide()`.
- `set_mode(0)` y `set_mode(1)`, cuando se invocan con `auto_mode_enabled=True`, limpian `detected_marks`, `selected_mark_index`, `auto_preview_image`, `detections_list`, emiten `preview_changed.emit(None)` y `request_redraw.emit()`.
- `set_mode(2)` no fue modificada.
- `_toggle_auto_mode` no fue modificada.
- Sintaxis Python válida (gate automatizado).
- Verificación visual del usuario confirma ambos bugs cerrados.
  </done>
</task>

</tasks>

<verification>
**Automated (gate de la task):**
- `python -c "import ast; ast.parse(open(...))"` confirma sintaxis válida.
- `grep` confirma counts esperados de los tokens clave (`addStretch`, `preview_changed.emit(None)`, `detections_list.clear()`).

**Manual:**
- Resize de ventana confirma layout compactado arriba con espacio al fondo en ambos modos.
- Switch desde modo Automático con preview activo a Selección y a Recorte limpia el state visible.
</verification>

<success_criteria>
- Ambos bugs UAT cerrados sin tocar otros archivos.
- Sin breaking changes a la API pública de `WatermarkProcessor` (`set_mode` mantiene firma).
- `_toggle_auto_mode` intacto — sigue siendo la fuente canónica del cleanup; `set_mode` lo replica intencionalmente porque no comparte signal de origen.
- Diff total < 20 líneas añadidas, 0 eliminadas.
</success_criteria>

<output>
Crear `.planning/quick/260528-e5u-fix-uat-bugs-watermark-processor-spacing/260528-e5u-SUMMARY.md` al terminar, documentando:
- Líneas exactas donde quedaron los `addStretch(1)`.
- Líneas exactas del cleanup en `set_mode` ramas 0 y 1.
- Resultado del gate automatizado y de la verificación humana.
</output>
