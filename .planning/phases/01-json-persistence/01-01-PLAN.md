---
phase: 01-json-persistence
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-json-persistence/01-01-AUDIT.md
autonomous: true
requirements:
  - ARCH-03

must_haves:
  truths:
    - "Un documento de auditoría existe que enumera los 4 call sites de UtilJson en slideshow_viewer.py, los keys migrados, y la firma pública del servicio a crear"
    - "El diseño del servicio está especificado: nombre de archivo, nombre de clase, métodos, path del singleton"
    - "Está documentado que watermark_tab.py importa UtilJson pero no lo usa — el import es dead code"
  artifacts:
    - path: ".planning/phases/01-json-persistence/01-01-AUDIT.md"
      provides: "Mapa de call sites + diseño de contrato del servicio WmPersistence"
      contains: "UtilJson call sites"
  key_links:
    - from: "slideshow_viewer.py líneas 262, 605, 631, 865"
      to: "WmPersistence service"
      via: "reemplazo directo en plan 01-02"
      pattern: "last_crop_pixels|last_watermark_folder"
---

<objective>
Auditar todos los accesos directos a UtilJson dentro de WatermarkRemove/ y producir el contrato del servicio de persistencia unificado que el plan 01-02 implementará.

Purpose: El plan 01-02 necesita un contrato preciso (nombre de clase, firma de métodos, path) antes de escribir código. Esta auditoría entrega ese contrato + confirma el scope exacto de la migración.

Output: Archivo 01-01-AUDIT.md en la carpeta de la fase con: tabla de call sites, decisión de diseño del servicio, y firma de la API pública.
</objective>

<execution_context>
@C:\Users\Felix\AppData\Local\Programs\Claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Felix\AppData\Local\Programs\Claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-json-persistence/01-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Auditar call sites de UtilJson en WatermarkRemove/ y producir contrato del servicio</name>
  <files>.planning/phases/01-json-persistence/01-01-AUDIT.md</files>
  <read_first>
    - WatermarkRemove/ui/slideshow_viewer.py (líneas 1-30 para imports, 255-270, 598-640, 858-870 para los 4 call sites)
    - WatermarkRemove/ui/watermark_tab.py (líneas 1-30 para confirmar que UtilJson no se usa — solo se importa)
    - WatermarkRemove/ui/position_editor.py (línea 578 para confirmar que wm_positions.json NO entra en scope)
    - WatermarkRemove/wm_remove.py (línea 611 para confirmar que wm_positions.json NO entra en scope)
    - utils/json_utils.py (API completa de UtilJson — el servicio la wrappea)
    - core/utils/constants.py (valor de SETTINGS_REL_DIR — path base para wm_settings.json)
    - core/services/settings_handler.py (entender qué NO debe cambiarse — perfiles de SmartStitch)
  </read_first>
  <action>
    Crear el archivo .planning/phases/01-json-persistence/01-01-AUDIT.md con las siguientes secciones:

    Sección 1 — Call sites a migrar (tabla):
    Columnas: Archivo | Línea | Operación | Key JSON | Valor default | Acción en 01-02
    Registrar exactamente los 4 call sites de slideshow_viewer.py que acceden a 'settings.json' con las keys 'last_crop_pixels' y 'last_watermark_folder'. Para cada uno especificar si es get o set.

    Sección 2 — Call sites fuera de scope (tabla):
    Registrar los accesos a wm_positions.json en position_editor.py (línea 578) y wm_remove.py (línea 611) con nota "NO migrar — D-03". Registrar el import muerto en watermark_tab.py con nota "Eliminar import — dead code".

    Sección 3 — Diferencias SettingsHandler vs UtilJson:
    SettingsHandler: gestiona perfiles SmartStitch, escribe a 'settings.json', requiere AppProfiles/AppSettings models, usa load()/save() con profile awareness. UtilJson: get/set genérico sobre cualquier archivo JSON, sin awareness de profiles. El servicio WM usa UtilJson directamente — no hay overlap de responsabilidad real, son dos cosas distintas. ARCH-03 se satisface creando un wrapper con nombres de dominio que oculte la instancia de UtilJson.

    Sección 4 — Diseño del servicio (contrato):
    Archivo: WatermarkRemove/services/wm_persistence.py
    Clase: WmPersistenceService
    Path del archivo JSON: os.path.join(SETTINGS_REL_DIR, 'wm_settings.json') — usar SETTINGS_REL_DIR de core.utils.constants
    Singleton: instancia 'wm_persistence' en WatermarkRemove/services/__init__.py

    API pública (firma de métodos, per D-02 solo estas 4):
    - get_last_crop_pixels() -> int  (default: 0)
    - set_last_crop_pixels(value: int) -> None
    - get_last_watermark_folder() -> Optional[str]  (default: None)
    - set_last_watermark_folder(folder_name: str) -> None

    Implementación interna: cada método instancia UtilJson(self._path).get/set(...) — wrapping stateless, sin cache en memoria (consistente con el patrón actual en slideshow_viewer.py que no cachea).

    Sección 5 — Estrategia de migración de datos:
    Si 'settings.json' ya tiene 'last_crop_pixels' o 'last_watermark_folder', el servicio NO intenta migrarlos — simplemente empieza limpio con wm_settings.json. Al primer get(), si la key no existe en wm_settings.json, retorna el default (0 o None). El usuario recuperará el valor al usarlo. No hay migración automática — las keys en settings.json quedan como legacy ignoradas.
  </action>
  <verify>
    <automated>python -c "import pathlib; p = pathlib.Path('.planning/phases/01-json-persistence/01-01-AUDIT.md'); assert p.exists(), 'AUDIT.md no existe'; content = p.read_text(encoding='utf-8'); assert 'last_crop_pixels' in content, 'Falta last_crop_pixels'; assert 'last_watermark_folder' in content, 'Falta last_watermark_folder'; assert 'WmPersistenceService' in content, 'Falta nombre de clase'; assert 'wm_persistence.py' in content, 'Falta nombre de archivo'; assert 'wm_settings.json' in content, 'Falta nombre del JSON'; print('AUDIT.md valido')"</automated>
  </verify>
  <acceptance_criteria>
    - .planning/phases/01-json-persistence/01-01-AUDIT.md existe
    - El archivo contiene exactamente 4 call sites en la tabla "Call sites a migrar" (líneas 262, 605, 631, 865 de slideshow_viewer.py)
    - El archivo contiene la nota "NO migrar — D-03" para position_editor.py y wm_remove.py
    - El archivo contiene la nota "dead code" o "Eliminar import" para watermark_tab.py
    - El archivo especifica la clase WmPersistenceService con los 4 métodos de dominio firmados
    - El archivo especifica que el singleton se llama 'wm_persistence' y se exporta desde WatermarkRemove/services/__init__.py
    - El archivo especifica que no hay migración automática desde settings.json
  </acceptance_criteria>
  <done>El AUDIT.md existe con las 5 secciones completas y el contrato del servicio está completamente especificado para que 01-02 lo implemente sin ambigüedades.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → aplicación | El archivo wm_settings.json es escrito/leído desde el directorio __settings__ — no hay input externo de red |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | 01-01-AUDIT.md | accept | El archivo es documentación interna de planificación — no contiene secretos ni PII |
</threat_model>

<verification>
Ejecutar el comando Python en `<verify>` desde la raíz del repositorio.
</verification>

<success_criteria>
01-01-AUDIT.md existe con contrato completo del servicio WmPersistenceService — suficiente para que 01-02 implemente sin leer código adicional.
</success_criteria>

<output>
Crear `.planning/phases/01-json-persistence/01-01-SUMMARY.md` cuando termine.
</output>
