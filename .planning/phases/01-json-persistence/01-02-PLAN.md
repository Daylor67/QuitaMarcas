---
phase: 01-json-persistence
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - WatermarkRemove/services/__init__.py
  - WatermarkRemove/services/wm_persistence.py
  - WatermarkRemove/ui/slideshow_viewer.py
  - WatermarkRemove/ui/watermark_tab.py
autonomous: true
requirements:
  - ARCH-03

must_haves:
  truths:
    - "slideshow_viewer.py no contiene ninguna llamada a UtilJson — las 4 llamadas han sido reemplazadas por métodos del singleton wm_persistence"
    - "watermark_tab.py no importa UtilJson — el import muerto fue eliminado"
    - "WatermarkRemove/services/wm_persistence.py existe con la clase WmPersistenceService y los 4 métodos de dominio"
    - "WatermarkRemove/services/__init__.py exporta el singleton 'wm_persistence'"
    - "El módulo WatermarkRemove arranca sin ImportError — la aplicación sigue funcionando"
  artifacts:
    - path: "WatermarkRemove/services/__init__.py"
      provides: "Singleton wm_persistence exportado"
      contains: "wm_persistence"
    - path: "WatermarkRemove/services/wm_persistence.py"
      provides: "WmPersistenceService con 4 métodos de dominio"
      exports:
        - "WmPersistenceService"
        - "get_last_crop_pixels"
        - "set_last_crop_pixels"
        - "get_last_watermark_folder"
        - "set_last_watermark_folder"
  key_links:
    - from: "WatermarkRemove/ui/slideshow_viewer.py"
      to: "WatermarkRemove/services/__init__.py"
      via: "from WatermarkRemove.services import wm_persistence"
      pattern: "wm_persistence\\.get_last_crop_pixels|wm_persistence\\.set_last_crop_pixels|wm_persistence\\.get_last_watermark_folder|wm_persistence\\.set_last_watermark_folder"
    - from: "WatermarkRemove/services/wm_persistence.py"
      to: "utils/json_utils.py"
      via: "UtilJson wrapping stateless"
      pattern: "UtilJson\\(self\\._path\\)"
---

<objective>
Crear el servicio de persistencia WmPersistenceService con el contrato definido en 01-01-AUDIT.md, y migrar los 4 call sites directos a UtilJson en slideshow_viewer.py para que usen el singleton. También eliminar el import muerto de UtilJson en watermark_tab.py.

Purpose: Satisface ARCH-03 — WatermarkRemove ya no hace llamadas directas a UtilJson desde la UI. Un único punto de persistencia para los settings del módulo.

Output: WatermarkRemove/services/ con __init__.py y wm_persistence.py, más slideshow_viewer.py y watermark_tab.py migrados.
</objective>

<execution_context>
@C:\Users\Felix\AppData\Local\Programs\Claude\get-shit-done\workflows\execute-plan.md
@C:\Users\Felix\AppData\Local\Programs\Claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-json-persistence/01-CONTEXT.md
@.planning/phases/01-json-persistence/01-01-AUDIT.md
@.planning/phases/01-json-persistence/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear WatermarkRemove/services/ con WmPersistenceService y singleton</name>
  <files>
    WatermarkRemove/services/__init__.py
    WatermarkRemove/services/wm_persistence.py
  </files>
  <read_first>
    - .planning/phases/01-json-persistence/01-01-AUDIT.md (contrato completo del servicio — leer antes de escribir una sola línea)
    - utils/json_utils.py (API de UtilJson que WmPersistenceService wrappea)
    - core/utils/constants.py (valor exacto de SETTINGS_REL_DIR)
    - core/services/settings_handler.py (patrón de singleton de módulo en core/services/ — imitar la estructura)
    - WatermarkRemove/__init__.py (verificar imports existentes — no romper el __init__ del módulo padre)
  </read_first>
  <action>
    Crear el directorio WatermarkRemove/services/ y dos archivos:

    wm_persistence.py — contiene WmPersistenceService:
    - Import: from utils import UtilJson y from core.utils.constants import SETTINGS_REL_DIR
    - Constructor __init__(self): asigna self._path = os.path.join(SETTINGS_REL_DIR, 'wm_settings.json')
    - get_last_crop_pixels(self) -> int: retorna UtilJson(self._path).get('last_crop_pixels', 0) casteado a int (mismo manejo defensivo que la línea 262 original: `or 0`)
    - set_last_crop_pixels(self, value: int) -> None: llama UtilJson(self._path).set('last_crop_pixels', int(value))
    - get_last_watermark_folder(self) -> Optional[str]: retorna UtilJson(self._path).get('last_watermark_folder', None)
    - set_last_watermark_folder(self, folder_name: str) -> None: llama UtilJson(self._path).set('last_watermark_folder', folder_name)
    - Agregar import Optional from typing

    __init__.py — expone el singleton:
    - from .wm_persistence import WmPersistenceService
    - wm_persistence = WmPersistenceService()
    - __all__ = ['wm_persistence', 'WmPersistenceService']

    El servicio NO cachea datos en memoria — cada llamada crea una instancia de UtilJson y lee/escribe el archivo. Este es el comportamiento idéntico al patrón actual en slideshow_viewer.py (per D-05: singleton de módulo, pero sin in-memory cache).

    No tocar WatermarkRemove/__init__.py a menos que sea necesario para resolver un ImportError — verificar primero.
  </action>
  <verify>
    <automated>cd /c/Users/Felix/Desktop/Python/manhwa_ocr/SmartStitch && python -c "import sys; sys.path.insert(0, '.'); from WatermarkRemove.services import wm_persistence; print(type(wm_persistence).__name__); assert hasattr(wm_persistence, 'get_last_crop_pixels'), 'missing get_last_crop_pixels'; assert hasattr(wm_persistence, 'set_last_crop_pixels'), 'missing set_last_crop_pixels'; assert hasattr(wm_persistence, 'get_last_watermark_folder'), 'missing get_last_watermark_folder'; assert hasattr(wm_persistence, 'set_last_watermark_folder'), 'missing set_last_watermark_folder'; print('WmPersistenceService OK')"</automated>
  </verify>
  <acceptance_criteria>
    - WatermarkRemove/services/__init__.py existe y contiene la línea 'wm_persistence = WmPersistenceService()'
    - WatermarkRemove/services/wm_persistence.py existe y contiene 'class WmPersistenceService'
    - wm_persistence.py contiene los 4 métodos: get_last_crop_pixels, set_last_crop_pixels, get_last_watermark_folder, set_last_watermark_folder
    - wm_persistence.py contiene 'UtilJson(self._path)' (wrapping stateless, no cache)
    - wm_persistence.py contiene 'wm_settings.json' en la asignación de self._path
    - El comando Python en verify retorna 'WmPersistenceService OK' sin excepción
  </acceptance_criteria>
  <done>WatermarkRemove.services importa sin error y wm_persistence expone los 4 métodos de dominio correctamente.</done>
</task>

<task type="auto">
  <name>Task 2: Migrar 4 call sites en slideshow_viewer.py y limpiar import muerto en watermark_tab.py</name>
  <files>
    WatermarkRemove/ui/slideshow_viewer.py
    WatermarkRemove/ui/watermark_tab.py
  </files>
  <read_first>
    - WatermarkRemove/ui/slideshow_viewer.py (leer completo — importa UtilJson en línea 22 y SETTINGS_REL_DIR en línea 23, usa UtilJson en líneas 262, 605, 631, 865)
    - WatermarkRemove/ui/watermark_tab.py (leer las primeras 30 líneas para ver el import de UtilJson — confirmar que no se usa en ningún otro lugar del archivo)
    - WatermarkRemove/services/__init__.py (confirmar el nombre exacto del singleton antes de importarlo)
  </read_first>
  <action>
    En slideshow_viewer.py:

    1. Reemplazar los imports en las líneas 22-23:
       ANTES: `from utils import UtilJson` y `from core.utils.constants import SETTINGS_REL_DIR`
       DESPUÉS: eliminar ambas líneas y agregar `from WatermarkRemove.services import wm_persistence`
       IMPORTANTE: verificar que SETTINGS_REL_DIR y UtilJson no se usen en otro lugar de slideshow_viewer.py antes de eliminar el import — si se usan en otro contexto, mantener solo el que sea necesario.

    2. Línea 262 — reemplazar:
       ANTES: `UtilJson(os.path.join(SETTINGS_REL_DIR, 'settings.json')).get('last_crop_pixels', 0) or 0`
       DESPUÉS: `wm_persistence.get_last_crop_pixels()`
       (el método ya maneja el default 0 y el cast a int internamente)

    3. Línea 605 — reemplazar:
       ANTES: `UtilJson(os.path.join(SETTINGS_REL_DIR, 'settings.json')).get('last_watermark_folder', None)`
       DESPUÉS: `wm_persistence.get_last_watermark_folder()`

    4. Línea 631 — reemplazar:
       ANTES: `UtilJson(os.path.join(SETTINGS_REL_DIR, 'settings.json')).set('last_watermark_folder', folder_name)`
       DESPUÉS: `wm_persistence.set_last_watermark_folder(folder_name)`

    5. Línea 865 — reemplazar:
       ANTES: `UtilJson(os.path.join(SETTINGS_REL_DIR, 'settings.json')).set('last_crop_pixels', int(value))`
       DESPUÉS: `wm_persistence.set_last_crop_pixels(value)`
       (el método hace int(value) internamente)

    En watermark_tab.py:

    Eliminar la línea `from utils import UtilJson` (import muerto — UtilJson no se usa en ningún método de WatermarkTab según auditoría). Si al eliminar el import Python lanza NameError en otro lugar del archivo, agregar nota y NO eliminar — reportar en SUMMARY.
  </action>
  <verify>
    <automated>cd /c/Users/Felix/Desktop/Python/manhwa_ocr/SmartStitch && python -c "
content = open('WatermarkRemove/ui/slideshow_viewer.py', encoding='utf-8').read()
assert 'UtilJson' not in content, 'UtilJson still present in slideshow_viewer.py'
assert 'wm_persistence.get_last_crop_pixels' in content, 'missing get_last_crop_pixels call'
assert 'wm_persistence.set_last_crop_pixels' in content, 'missing set_last_crop_pixels call'
assert 'wm_persistence.get_last_watermark_folder' in content, 'missing get_last_watermark_folder call'
assert 'wm_persistence.set_last_watermark_folder' in content, 'missing set_last_watermark_folder call'
assert 'from WatermarkRemove.services import wm_persistence' in content, 'missing service import'
wt = open('WatermarkRemove/ui/watermark_tab.py', encoding='utf-8').read()
assert 'from utils import UtilJson' not in wt, 'dead import still present in watermark_tab.py'
print('Migration OK')
"</automated>
  </verify>
  <acceptance_criteria>
    - slideshow_viewer.py no contiene ninguna ocurrencia de la cadena 'UtilJson'
    - slideshow_viewer.py contiene 'from WatermarkRemove.services import wm_persistence'
    - slideshow_viewer.py contiene exactamente 4 llamadas al singleton: get_last_crop_pixels(), set_last_crop_pixels(...), get_last_watermark_folder(), set_last_watermark_folder(...)
    - watermark_tab.py no contiene la línea 'from utils import UtilJson'
    - El comando verify retorna 'Migration OK' sin excepción
    - Si SETTINGS_REL_DIR se usa en otro lugar de slideshow_viewer.py (no relacionado con settings.json), ese import se mantiene y se documenta en SUMMARY
  </acceptance_criteria>
  <done>Los 4 call sites directos a UtilJson en slideshow_viewer.py han sido reemplazados por el singleton wm_persistence, y el import muerto de UtilJson en watermark_tab.py ha sido eliminado. ARCH-03 satisfecho.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Filesystem → servicio | wm_settings.json escrito/leído desde __settings__/ — sin input de red |
| UI widget → servicio | slideshow_viewer.py llama al singleton con valores int/str — sin validación de red |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Tampering | wm_settings.json | accept | Archivo local de settings de usuario — no hay PII ni secretos, valor de bajo riesgo |
| T-01-02-02 | Denial of Service | WmPersistenceService.get_last_crop_pixels | mitigate | UtilJson.read() retorna {} si el archivo no existe o está corrupto — el servicio hereda este comportamiento defensivo; los defaults (0, None) se aplican en ese caso |
</threat_model>

<verification>
Ejecutar en la raíz del repositorio los dos comandos Python de los `<verify>` de cada task. Ambos deben imprimir su mensaje OK sin excepción.

Verificación adicional de integración:
- python -c "import sys; sys.path.insert(0, '.'); from WatermarkRemove.services import wm_persistence; v = wm_persistence.get_last_crop_pixels(); assert isinstance(v, int), f'Expected int, got {type(v)}'; print(f'get_last_crop_pixels returns int: {v}')"
- python -c "import sys; sys.path.insert(0, '.'); from WatermarkRemove.services import wm_persistence; f = wm_persistence.get_last_watermark_folder(); assert f is None or isinstance(f, str), f'Expected str or None, got {type(f)}'; print(f'get_last_watermark_folder returns: {f}')"
</verification>

<success_criteria>
1. WatermarkRemove/services/ existe con wm_persistence.py y __init__.py
2. slideshow_viewer.py contiene cero ocurrencias de 'UtilJson'
3. watermark_tab.py no importa UtilJson
4. from WatermarkRemove.services import wm_persistence no lanza ImportError
5. wm_persistence.get_last_crop_pixels() retorna int (0 si wm_settings.json no existe aún)
6. wm_persistence.get_last_watermark_folder() retorna None si wm_settings.json no existe aún
7. ARCH-03: no hay llamadas directas a UtilJson desde ningún archivo en WatermarkRemove/ui/
</success_criteria>

<output>
Crear `.planning/phases/01-json-persistence/01-02-SUMMARY.md` cuando termine.
</output>
