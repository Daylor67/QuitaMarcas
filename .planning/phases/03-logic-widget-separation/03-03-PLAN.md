---
phase: 03-logic-widget-separation
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - WatermarkRemove/ui/watermark_tab.py
autonomous: false
requirements: [ARCH-04]
must_haves:
  truths:
    - "watermark_tab.py no contiene lógica winreg inline — delega detección y toggle a ContextMenuService"
    - "watermark_tab.py preserva el contrato externo: constructor sin args, atributo run_quita_marcas, método log(), get_settings/set_settings, y un alias apply_settings"
    - "WatermarkTab conecta señales de UI con servicios sin condicionales de dominio propios (coordinador puro)"
    - "El flujo completo (abrir imágenes/editor/menú contextual/updates + Ejecutar Quita Marcas) funciona sin regresión"
  artifacts:
    - path: "WatermarkRemove/ui/watermark_tab.py"
      provides: "Coordinador puro que cablea señales UI con servicios"
      contains: "context_menu_service"
  key_links:
    - from: "WatermarkRemove/ui/watermark_tab.py"
      to: "WatermarkRemove.services.context_menu_service"
      via: "import context_menu_service"
      pattern: "context_menu_service"
    - from: "gui/controller.py"
      to: "WatermarkTab.run_quita_marcas"
      via: "acceso directo al atributo (contrato externo)"
      pattern: "run_quita_marcas"
---

<objective>
Convertir `watermark_tab.py` en un coordinador puro (ARCH-04): mover la lógica de dominio OS (winreg / menú contextual) a `ContextMenuService`, dejar el widget solo cableando señales de UI con servicios y reflejando estado en la presentación. Preservar estrictamente el contrato externo que `gui/controller.py` y los 3 componentes de Phase 2 dependen. Cerrar Phase 3 con un checkpoint UAT manual que verifica el flujo completo sin regresión.

Purpose: ARCH-04 — `WatermarkTab` no debe contener lógica de negocio inline; conecta UI con servicios. SC-1/SC-4 — el contrato externo y el flujo completo no se rompen.
Output: watermark_tab.py refactorizado como coordinador puro + alias apply_settings; checkpoint UAT manual con sign-off humano.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-logic-widget-separation/03-RESEARCH.md
@.planning/phases/03-logic-widget-separation/03-PATTERNS.md
@.planning/phases/03-logic-widget-separation/03-DOMAIN-GREP.md
@.planning/phases/03-logic-widget-separation/03-HUMAN-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactorizar watermark_tab.py — delegar winreg a ContextMenuService + alias apply_settings</name>
  <files>WatermarkRemove/ui/watermark_tab.py</files>
  <read_first>
    - WatermarkRemove/ui/watermark_tab.py (estado actual completo — el archivo a modificar)
    - WatermarkRemove/services/context_menu_service.py (Plan 03-01: is_registered, toggle)
    - WatermarkRemove/services/__init__.py (instancia singleton context_menu_service)
    - gui/controller.py (contrato externo REAL: WatermarkTab() L76 sin args, run_quita_marcas.isChecked() L317 acceso directo)
    - .planning/phases/03-logic-widget-separation/03-PATTERNS.md (qué MOVES vs STAYS L209-221; contrato a preservar L215-221)
    - .planning/phases/03-logic-widget-separation/03-RESEARCH.md (Pitfall 1 apply_settings vs set_settings L216-220; Pitfall 2 contrato real L222-226; Open Question #2 rigor update check L301-304)
  </read_first>
  <action>
Modificar `WatermarkRemove/ui/watermark_tab.py`. Agregar import del servicio: `from WatermarkRemove.services import context_menu_service`.

MOVER a ContextMenuService (la lógica winreg deja de vivir en el widget):
- `_is_context_menu_registered` (L146-153): eliminar el método; sus dos usos pasan a `context_menu_service.is_registered()`.
- `_update_context_menu_btn` (L155-160): MANTENER el método (es presentación — texto del botón), pero reemplazar la condición `if self._is_context_menu_registered():` por `if context_menu_service.is_registered():`.
- `_toggle_context_menu` (L162-180): eliminar la lógica winreg/register inline. Nuevo cuerpo: en un `try`, llamar `now_registered = context_menu_service.toggle()`, mostrar el `QMessageBox.information` apropiado según `now_registered` (mensaje "registrado" con la instrucción de click derecho, o "eliminado"), luego `self._update_context_menu_btn()`; en `except Exception as e: QMessageBox.critical(self, "Error", ...)`. El widget conserva SOLO el feedback QMessageBox + refresh del botón (presentación); NO importa winreg ni register_context_menu directamente.

DECISIÓN DE RIGOR (RESEARCH Open Question #2 / A5): `_check_for_updates` (L182-206) YA delega a `UpdateChecker` (servicio existente en core.services) — la única "lógica" inline es el try/except + el toggle de estado del botón (`setEnabled` + `setText`), que es feedback de presentación legítimo de un coordinador. DEJAR `_check_for_updates` como está (no crear un servicio nuevo para envolver un try/except de UI). Documentar esta decisión en el SUMMARY: orquestar el diálogo de update + estado del botón es coordinación de UI aceptable; lo que ARCH-04 exige sacar es la lógica de dominio OS (winreg), que SÍ se mueve.

PRESERVAR EL CONTRATO EXTERNO (CRÍTICO — RESEARCH Pitfalls 1-2, verificado en gui/controller.py):
1. Constructor `WatermarkTab(self, parent=None)` sin args obligatorios — NO cambiar la firma (controller.py:76 instancia `WatermarkTab()`).
2. Atributo público `self.run_quita_marcas` (QCheckBox) — NO envolverlo en property ni renombrarlo (controller.py:317 accede directo a `.isChecked()`).
3. Método `log(self, message)` — NO tocar (lo usan NavigationController/WatermarkProcessor/TrainingDataCollector vía `watermark_tab.log`).
4. `get_settings` / `set_settings` — preservar tal cual. AGREGAR al final de la clase el alias `apply_settings = set_settings` (Pitfall 1: la constraint de CLAUDE.md/ROADMAP dice `apply_settings` pero el método real es `set_settings`; el alias cumple la constraint literal sin romper nada).

QUE SE QUEDA (coordinación legítima — PATTERNS L213): `_open_image_viewer` y `_open_position_editor` solo instancian diálogos (coordinación), `_get_main_window`, `log`, todo `_setup_ui`. NO mover estos a servicios.
  </action>
  <verify>
    <automated>cd "c:/Users/Felix/Desktop/Python/manhwa_ocr/SmartStitch" && python -m py_compile WatermarkRemove/ui/watermark_tab.py && echo "WINREG_HITS:" && (grep -vE "^\s*#" WatermarkRemove/ui/watermark_tab.py | grep -cE "winreg\.|register_context_menu")</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile WatermarkRemove/ui/watermark_tab.py` sale 0
    - El grep de dominio OS (filtrado por comentarios) sobre `winreg.|register_context_menu` retorna 0 (línea WINREG_HITS: 0)
    - `watermark_tab.py` contiene `from WatermarkRemove.services import context_menu_service` y al menos un uso de `context_menu_service.is_registered()` y `context_menu_service.toggle()`
    - `watermark_tab.py` contiene `apply_settings = set_settings` (alias de contrato)
    - El contrato se preserva: grep confirma que existen `def __init__(self, parent=None)`, `self.run_quita_marcas = QCheckBox`, `def log(self, message`, `def get_settings`, `def set_settings`
    - `_update_context_menu_btn` y los QMessageBox del menú contextual permanecen en el widget (grep `QMessageBox` en watermark_tab.py >=1)
    - Smoke + contrato: `python -c "from PySide6.QtWidgets import QApplication; import sys; app=QApplication(sys.argv); from WatermarkRemove.ui import WatermarkTab; t=WatermarkTab(); assert hasattr(t,'run_quita_marcas') and hasattr(t,'log') and hasattr(t,'get_settings') and hasattr(t,'set_settings') and hasattr(t,'apply_settings'); print('WT_CONTRACT_OK')"` imprime WT_CONTRACT_OK
    - `git diff gui/controller.py` está vacío (contrato externo no requirió cambios)
  </acceptance_criteria>
  <done>watermark_tab.py delega winreg a ContextMenuService, expone apply_settings alias, preserva todo el contrato externo (constructor/run_quita_marcas/log/get_settings/set_settings); 0 hits de dominio OS; controller intacto.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: UAT manual completo de Phase 3 + verificación de contrato</name>
  <files>.planning/phases/03-logic-widget-separation/03-HUMAN-UAT.md</files>
  <action>Pausar para verificación humana — NO hay edición de código en este task. Tras completar Task 1, el ejecutor presenta al usuario el guion UAT de `03-HUMAN-UAT.md` (5 secciones) junto con el resultado de los gates automáticos (`git diff gui/controller.py` vacío, grep de dominio en 0 sobre los 3 widgets, py_compile + smoke OK) y espera el sign-off "approved" antes de cerrar la fase. Si el usuario reporta una regresión, registrarla para gap-closure en lugar de marcar la fase completa.</action>
  <verify>
    <human-check>Las 5 secciones de 03-HUMAN-UAT.md pasan y el usuario escribe "approved".</human-check>
  </verify>
  <done>El usuario aprobó el UAT manual ("approved") y los gates automáticos están en 0/vacío; Phase 3 cerrada sin regresión.</done>
  <what-built>
    Phase 3 completa: la lógica de dominio salió de los 3 widgets hacia servicios (PositionEditorService, folder_scan_service, WmPositionsPersistenceService, ContextMenuService). position_editor.py, image_viewer.py y watermark_tab.py quedan como coordinadores de presentación. WatermarkTab es coordinador puro con el contrato externo preservado (+ alias apply_settings).
  </what-built>
  <how-to-verify>
    Ejecutar las 5 secciones de `.planning/phases/03-logic-widget-separation/03-HUMAN-UAT.md`:
    1. Correr `python SmartStitchGUI.py`.
    2. Sección 1 — Editor de Posiciones: abrir el editor desde la pestaña Quita Marcas, seleccionar carpeta de imágenes + carpeta de marcas + marca específica, ajustar offset X/Y y los combos de posición → el preview debe actualizarse en vivo. Pulsar "Guardar y Siguiente" hasta la última imagen → debe aparecer el QMessageBox "Completado" con el conteo correcto. Cerrar, reabrir el editor, recargar la misma carpeta+marca y confirmar que las posiciones persisten en `WatermarkRemove/wm_positions.json` (abrir el JSON y verificar la estructura carpeta→marca→pos_N intacta).
    3. Sección 2 — Visor de Imágenes: "Ver Imágenes de Input" con una carpeta válida → grid de thumbnails + contador correcto.
    4. Sección 3 — Menú Contextual (Windows): click en el botón → registra (texto cambia a "Desregistrar"); confirmar la entrada con click derecho en una carpeta del explorador; click de nuevo → desregistra (texto vuelve a "Registrar").
    5. Sección 4 — Buscar Actualizaciones: click → muestra diálogo de update o "Ya tienes la última versión".
    6. Sección 5 — Contrato: marcar el checkbox "Ejecutar Quita Marcas" y correr el pipeline → debe abrir el SlideshowViewer (flujo completo Phase 2 intacto).
    7. Confirmar gates automáticos en consola:
       - `git diff gui/controller.py` → vacío.
       - Grep de dominio sobre los 3 widgets retorna 0 (usar el comando de `03-DOMAIN-GREP.md`).
       - py_compile + smoke de los 3 widgets sale 0.
  </how-to-verify>
  <resume-signal>Escribir "approved" si las 5 secciones pasan sin regresión y los gates automáticos están en 0/vacío, o describir la regresión observada (sección + comportamiento inesperado) para gap-closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| widget → ContextMenuService | el widget dispara toggle/consulta estado; el servicio toca winreg |
| ContextMenuService → registro de Windows | winreg lee/escribe HKEY_CURRENT_USER |
| gui/controller.py → WatermarkTab | el controller externo lee run_quita_marcas e instancia el tab (contrato) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | ContextMenuService.toggle | accept | Solo reubica el call-site; la clave de registro y su alcance NO cambian; guard FileNotFoundError preservado en el servicio (RESEARCH Runtime State L208) |
| T-03-03-02 | Repudiation/Integrity | contrato externo WatermarkTab | mitigate | Verificar `git diff gui/controller.py` vacío + smoke hasattr de run_quita_marcas/log/get_settings/set_settings/apply_settings (RESEARCH Pitfall 2) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No se instalan paquetes en Phase 3 (RESEARCH Package Legitimacy Audit: 0 installs) |
</threat_model>

<verification>
- `python -m py_compile WatermarkRemove/ui/watermark_tab.py` sale 0.
- Grep de dominio OS (filtrado por comentarios) sobre watermark_tab.py retorna 0 para `winreg.|register_context_menu`.
- Smoke + contrato: WatermarkTab() instancia y expone run_quita_marcas/log/get_settings/set_settings/apply_settings.
- `git diff gui/controller.py` vacío.
- Checkpoint UAT manual: las 5 secciones de 03-HUMAN-UAT.md pasan con sign-off "approved".
- Phase gate: grep de dominio en los 3 widgets (position_editor, image_viewer, watermark_tab) retorna 0.
</verification>

<success_criteria>
WatermarkTab es coordinador puro: la lógica winreg vive en ContextMenuService, el widget solo cablea y refleja estado en la presentación. El contrato externo (constructor sin args, run_quita_marcas, log, get_settings/set_settings, alias apply_settings) está intacto y `gui/controller.py` no requirió cambios. El UAT manual confirma el flujo completo sin regresión (ARCH-04 + SC-1 + SC-4 satisfechos).
</success_criteria>

<output>
Create `.planning/phases/03-logic-widget-separation/03-03-SUMMARY.md` when done
</output>
