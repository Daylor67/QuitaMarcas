---
phase: 03-logic-widget-separation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - WatermarkRemove/ui/position_editor.py
  - WatermarkRemove/ui/image_viewer.py
autonomous: true
requirements: [ARCH-02]
must_haves:
  truths:
    - "position_editor.py no calcula ni align_watermark ni remove_watermark ni conversión cv2→QPixmap inline — delega a PositionEditorService"
    - "position_editor.py no escanea carpetas inline — usa folder_scan_service"
    - "position_editor.py no escribe wm_positions.json inline — usa WmPositionsPersistenceService"
    - "image_viewer.py no escanea la carpeta inline — usa folder_scan_service.scan_images"
    - "El comportamiento observable del editor y del visor es idéntico al anterior (preview, guardado, grid de thumbnails)"
  artifacts:
    - path: "WatermarkRemove/ui/position_editor.py"
      provides: "Widget editor de posiciones que coordina UI y delega dominio a servicios"
      contains: "PositionEditorService"
    - path: "WatermarkRemove/ui/image_viewer.py"
      provides: "Widget visor que delega el escaneo de carpeta al servicio"
      contains: "scan_images"
  key_links:
    - from: "WatermarkRemove/ui/position_editor.py"
      to: "WatermarkRemove.services"
      via: "import PositionEditorService/folder_scan/WmPositionsPersistenceService"
      pattern: "from WatermarkRemove.services"
    - from: "WatermarkRemove/ui/image_viewer.py"
      to: "WatermarkRemove.services.folder_scan_service"
      via: "import scan_images"
      pattern: "scan_images"
---

<objective>
Vaciar la lógica de dominio inline de los widgets `position_editor.py` (mayor concentración de deuda del módulo) y `image_viewer.py` (ligero), delegando a los servicios creados en Plan 03-01. Los widgets conservan TODO su estado de UI (índices, spinboxes, combos, zoom, geometría de ventana) y su construcción de presentación (thumbnails, layout); solo las llamadas de cálculo/IO/persistencia se mueven a servicios.

Purpose: ARCH-02 — ningún widget en `ui/` debe contener lógica de dominio inline. position_editor es el objetivo principal (align/remove/cv2/load/persist), image_viewer solo cede el escaneo de carpeta.
Output: position_editor.py e image_viewer.py refactorizados como coordinadores de presentación; 0 hits del patrón grep de dominio (salvo coordinación legítima).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-logic-widget-separation/03-RESEARCH.md
@.planning/phases/03-logic-widget-separation/03-PATTERNS.md
@.planning/phases/03-logic-widget-separation/03-DOMAIN-GREP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactorizar position_editor.py para delegar dominio a los servicios</name>
  <files>WatermarkRemove/ui/position_editor.py</files>
  <read_first>
    - WatermarkRemove/ui/position_editor.py (estado actual completo — el archivo a modificar)
    - WatermarkRemove/services/position_editor_service.py (Plan 03-01: build_preview_pixmap, load_image)
    - WatermarkRemove/services/folder_scan_service.py (Plan 03-01: scan_images, scan_pngs, scan_subfolders)
    - WatermarkRemove/services/wm_positions_persistence.py (Plan 03-01: save_positions)
    - WatermarkRemove/services/__init__.py (instancias singleton: position_editor_service, wm_positions_persistence; funciones scan_*)
    - .planning/phases/03-logic-widget-separation/03-PATTERNS.md (qué STAYS vs qué MOVES — L178-189; Pitfall 5 L189)
    - .planning/phases/03-logic-widget-separation/03-DOMAIN-GREP.md (gate de aceptación)
  </read_first>
  <action>
Modificar `WatermarkRemove/ui/position_editor.py`. En el constructor `__init__`, instanciar los servicios: `self.service = position_editor_service` (la instancia singleton del barrel) y usar `wm_positions_persistence` para guardar. Import: `from WatermarkRemove.services import position_editor_service, wm_positions_persistence, scan_images, scan_pngs, scan_subfolders`.

MOVER a servicio (eliminar la lógica inline del widget, reemplazar por llamadas):
- `_update_preview` (L474-507): reemplazar el bloque try (copy + align_watermark + remove_watermark + cvtColor + QImage + QPixmap, L480-497) por: `pixmap = self.service.build_preview_pixmap(self.current_image, self.current_watermark, offset_x=self.offset_x, offset_y=self.offset_y, side_x=self.side_x, side_y=self.side_y)`. Mantener el guard inicial `if self.current_image is None or self.current_watermark is None: return`, el `self.image_label.set_image(pixmap)`, la llamada `self._adjust_window_size(...)` (necesita width/height — obtener de `pixmap.width()`/`pixmap.height()`), y el `except Exception as e: self.image_label.setText(f"❌ Error: {str(e)}")` (error-handling se queda en el widget — PATTERNS L70).
- `_load_current_image` (L444-460): reemplazar `self.current_image = load_images_cv2(str(image_path))` por `self.current_image = self.service.load_image(image_path)`.
- `_load_current_watermark`: reemplazar `self.current_watermark = load_images_cv2(str(watermark_path))` por `self.current_watermark = self.service.load_image(watermark_path)`.
- `_load_images` (L401-412): reemplazar el loop `for file in natsorted(...)` por `self.image_files = scan_images(self.images_folder, self.SUPPORTED_FORMATS)`. Mantener `self.current_image_index = 0` y `self._update_counter()`.
- `_load_watermark_folders` (L374-387): reemplazar el bloque de scan de subcarpetas por `folders = scan_subfolders(self.marcas_base_path)` (preserva orden reverse), tras el guard `if not self.marcas_base_path.exists(): return`. Mantener el `clear()` del combo y el loop `addItem(folder.name, str(folder))`.
- `_load_watermarks_into_combo` (L414-427): reemplazar el loop de PNG por `self.watermark_files = scan_pngs(self.watermarks_folder)` tras el guard de existencia; mantener `clear()` del combo y el loop `addItem(file.name, str(file))`.
- `_save_to_json` (L569-600): reemplazar el bloque de construcción de path + UtilJson + positions_dict + folder_data + set (L575-596) por una llamada al servicio: `target_label = wm_positions_persistence.save_positions(self.watermarks_folder.name, self.saved_positions, save_by_watermark=self.save_by_watermark_checkbox.isChecked(), watermark_filename=(self.watermark_path.name if self.watermark_path else None))`. Mantener el guard inicial `if not self.watermarks_folder or not self.saved_positions: return`, el `print(f"✓ Guardadas...")` y el `except Exception as e: QMessageBox.critical(...)` (feedback se queda en el widget).

ELIMINAR imports muertos del top del archivo que ya no se usan: `from utils import UtilJson`, `from WatermarkRemove import load_images_cv2, align_watermark, remove_watermark`, `from natsort import natsorted`, `import numpy as np`, `import cv2` — SOLO si ya no quedan usos en el archivo (verificar con el grep gate). PRESERVAR `QPixmap` solo si sigue usándose (type hint del set_image); si `_update_preview` ya no construye QImage, eliminar `QImage` del import de QtGui. Verificar caso por caso con grep antes de borrar cada símbolo.

QUE SE QUEDA EN EL WIDGET (presentación — PATTERNS L178-184): clase `ZoomableImageLabel`, todo `_create_controls_panel`/`_create_image_panel`, `keyPressEvent`, `_on_zoom_changed`, callbacks de spinbox/combo (offset_x, side_x, current_image_index), `_adjust_window_size` (geometría = presentación), y la NAVEGACIÓN de `_save_and_next` (índice + zoom reset + QMessageBox). CONSTRAINT Pitfall 5: `_save_and_next` mantiene la acumulación en `self.saved_positions` y solo llama `_save_to_json()` (que ahora delega al servicio) al llegar a la última imagen — NO cambiar el comportamiento "guarda solo al final del batch" ni el conteo mostrado en el diálogo.
  </action>
  <verify>
    <automated>cd "c:/Users/Felix/Desktop/Python/manhwa_ocr/SmartStitch" && python -m py_compile WatermarkRemove/ui/position_editor.py && echo "DOMAIN_HITS:" && (grep -vE "^\s*#" WatermarkRemove/ui/position_editor.py | grep -cE "align_watermark\(|remove_watermark\(|load_images_cv2\(|cv2\.|UtilJson\(")</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile WatermarkRemove/ui/position_editor.py` sale 0
    - El grep de dominio (filtrado por comentarios) sobre `align_watermark(|remove_watermark(|load_images_cv2(|cv2.|UtilJson(` retorna 0 (línea DOMAIN_HITS: 0)
    - `position_editor.py` contiene `from WatermarkRemove.services import` con `position_editor_service`, `wm_positions_persistence`, `scan_images`, `scan_pngs`, `scan_subfolders`
    - `position_editor.py` contiene `self.service.build_preview_pixmap(` y `wm_positions_persistence.save_positions(`
    - El error-handling permanece: grep `self.image_label.setText(f"❌ Error` retorna >=1 y `QMessageBox.critical` permanece en `_save_to_json`
    - `_save_and_next` sigue conteniendo `self.saved_positions.append(` y la rama `else:` que llama `_save_to_json()` + `QMessageBox.information` (comportamiento de guardar al final preservado)
    - Smoke: `python -c "from PySide6.QtWidgets import QApplication; import sys; app=QApplication(sys.argv); from WatermarkRemove.ui import PositionEditor; PositionEditor(); print('PE_OK')"` imprime PE_OK
  </acceptance_criteria>
  <done>position_editor.py delega align/remove/preview/load/scan/persist a servicios; 0 hits de dominio; instancia y compila; comportamiento de guardado preservado.</done>
</task>

<task type="auto">
  <name>Task 2: Refactorizar image_viewer.py para delegar el escaneo de carpeta al servicio</name>
  <files>WatermarkRemove/ui/image_viewer.py</files>
  <read_first>
    - WatermarkRemove/ui/image_viewer.py (estado actual completo — el archivo a modificar; _load_images L83-118)
    - WatermarkRemove/services/folder_scan_service.py (Plan 03-01: scan_images)
    - WatermarkRemove/services/__init__.py (export de scan_images)
    - .planning/phases/03-logic-widget-separation/03-PATTERNS.md (image_viewer: qué MOVES vs STAYS — L194-201)
    - .planning/phases/03-logic-widget-separation/03-DOMAIN-GREP.md (gate)
  </read_first>
  <action>
Modificar `WatermarkRemove/ui/image_viewer.py`. Agregar import: `from WatermarkRemove.services import scan_images`.

MOVER al servicio SOLO el escaneo en `_load_images` (L83-118): reemplazar el bloque `image_files = []` + el loop `for file in natsorted(self.folder_path.iterdir()): if file.is_file() and file.suffix.lower() in self.SUPPORTED_FORMATS: image_files.append(file)` (L93,98-101) por: `image_files = scan_images(self.folder_path, self.SUPPORTED_FORMATS)`. CONSTRAINT comportamiento observable: el original reasigna `self.folder_path = self.folder_path.parent` cuando la ruta es un archivo (L94-96), lo que afecta `path_label`; PRESERVAR ese efecto manteniendo `if self.folder_path.is_file(): self.folder_path = self.folder_path.parent` ANTES de llamar `scan_images` (no eliminarlo, aunque scan_images también lo haga internamente). Pasar `self.SUPPORTED_FORMATS` (incluye .psd/.psb — NO unificar con otros widgets, PATTERNS L104).

QUE SE QUEDA (presentación — PATTERNS L198-201): `_setup_ui`, `_create_image_widget` (thumbnails), `_show_full_image`, `_clear_grid`, el layout de grid, y la actualización del contador `self.image_count_label.setText(...)` (lee `len(image_files)` retornado por el servicio). Eliminar `from natsort import natsorted` del top SOLO si ya no queda ningún otro uso en el archivo (verificar con grep). NO tocar `QPixmap(str(image_path))` en `_create_image_widget`/`_show_full_image`: son thumbnails de presentación con QPixmap nativo de Qt, NO carga de dominio cv2 — quedan en el widget y NO entran en el patrón grep de dominio.
  </action>
  <verify>
    <automated>cd "c:/Users/Felix/Desktop/Python/manhwa_ocr/SmartStitch" && python -m py_compile WatermarkRemove/ui/image_viewer.py && echo "SCAN_REFS:" && (grep -c "scan_images" WatermarkRemove/ui/image_viewer.py)</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile WatermarkRemove/ui/image_viewer.py` sale 0
    - `image_viewer.py` contiene `from WatermarkRemove.services import scan_images` y `scan_images(self.folder_path, self.SUPPORTED_FORMATS)` (línea SCAN_REFS >=2)
    - `image_viewer.py` aún contiene `if self.folder_path.is_file(): self.folder_path = self.folder_path.parent` (comportamiento de path_label preservado)
    - `image_viewer.py` aún define `SUPPORTED_FORMATS` con `.psd` y `.psb` (no unificado)
    - Smoke: `python -c "from PySide6.QtWidgets import QApplication; import sys; app=QApplication(sys.argv); from WatermarkRemove.ui import ImageViewer; ImageViewer('.'); print('IV_OK')"` imprime IV_OK
  </acceptance_criteria>
  <done>image_viewer.py delega el escaneo a scan_images; conserva los thumbnails y el efecto de path_label; instancia y compila.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| widget → servicio | el widget pasa paths e índices al servicio; el servicio hace el IO/cómputo |
| filesystem → servicio | paths de imágenes/marcas (posiblemente no-ASCII) leídos vía servicio |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering/DoS | position_editor.load image | mitigate | El widget delega a `service.load_image` que usa `load_images_cv2` (np.fromfile) — NUNCA cv2.imread; preserva robustez non-ASCII (RESEARCH Pitfall 3) |
| T-03-02-02 | Tampering | position_editor save | mitigate | Persistencia delegada a `wm_positions_persistence` que preserva el parseo defensivo y la estructura del JSON (no cambia formato) |
| T-03-02-03 | Input Validation | image_viewer/position_editor scan | mitigate | Filtro de extensiones (`SUPPORTED_FORMATS`) preservado y pasado como parámetro al servicio (RESEARCH Security V5) |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No se instalan paquetes en Phase 3 (RESEARCH Package Legitimacy Audit: 0 installs) |
</threat_model>

<verification>
- `python -m py_compile` sobre position_editor.py e image_viewer.py sale 0.
- Grep de dominio (filtrado por comentarios) sobre position_editor.py retorna 0 para `align_watermark(|remove_watermark(|load_images_cv2(|cv2.|UtilJson(`.
- Smoke runtime: instanciar `PositionEditor()` e `ImageViewer('.')` con QApplication sale 0.
- Comportamiento preservado: error-handling y QMessageBox permanecen en los widgets; `_save_and_next` guarda al final del batch; image_viewer reasigna folder.parent para el label.
- `git diff gui/controller.py` vacío (contrato externo intacto — este plan no toca el coordinador).
- UAT manual Secciones 1 y 2 de 03-HUMAN-UAT.md se verifican en el wave merge / phase gate (Plan 03-03 checkpoint).
</verification>

<success_criteria>
position_editor.py e image_viewer.py ya no contienen lógica de dominio inline (0 hits del patrón grep), delegan a los servicios de Plan 03-01, compilan, instancian, y preservan el comportamiento observable (preview, guardado al final del batch, thumbnails, contador, efecto de path_label). ARCH-02 satisfecho para estos dos widgets.
</success_criteria>

<output>
Create `.planning/phases/03-logic-widget-separation/03-02-SUMMARY.md` when done
</output>
