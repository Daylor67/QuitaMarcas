---
phase: 02-slideshowviewer-decomposition
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - WatermarkRemove/ui/components/watermark_processor.py
  - WatermarkRemove/ui/components/navigation_controller.py
  - WatermarkRemove/ui/slideshow_viewer.py
autonomous: true
requirements:
  - ARCH-01
must_haves:
  truths:
    - "WatermarkProcessor(QWidget) en WatermarkRemove/ui/components/watermark_processor.py contiene toda la logica de manual mode, auto YOLO mode, position-grid (cuadros rojo/verde), y crop mode"
    - "El usuario puede activar modo manual, click izquierdo sobre la imagen, ajustar alpha/offset, click Accept, y la marca se remueve y guarda — comportamiento idéntico al pre-refactor"
    - "El usuario puede activar modo auto YOLO, ver lista de detecciones, ajustar X/Y, eliminar marcas, y click Guardar/Guardar y Siguiente — comportamiento idéntico"
    - "Los cuadros rojo/verde de posiciones guardadas y el overlay de crop se pintan de nuevo (regresion del Plan 01 restaurada via signal/slot decorate)"
    - "SlideshowViewer ya no contiene métodos _accept_preview, _revert_preview, _remove_watermark_preview, _process_watermark_at_position, _run_auto_detection, _accept_auto_detections, _delete_selected_detection, _refresh_auto_preview, _on_detection_selected, _populate_detections_list, _on_auto_offset_changed, _toggle_auto_mode, _toggle_manual_mode, _toggle_crop_mode, _apply_crop, _on_crop_pixels_changed, _on_alpha_changed, _on_offset_adj_changed, _compute_live_preview, _load_watermark_folders, _on_watermark_folder_changed, _load_watermarks_into_combo, _filter_watermark_combo, _on_watermark_changed, _load_watermark_positions, _update_manual_overlay, _draw_watermark_overlays, _draw_crop_overlay, eventFilter, mousePressEvent — viven en WatermarkProcessor"
    - "grep -nE 'remove_watermark\\(|detect_watermarks\\(|resolve_png_for_class\\(|find_wm\\(|quick_align_preview\\(' WatermarkRemove/ui/slideshow_viewer.py retorna 0 hits"
  artifacts:
    - path: "WatermarkRemove/ui/components/watermark_processor.py"
      provides: "WatermarkProcessor(QWidget) — manual + auto YOLO + posiciones guardadas + crop mode + alpha/offset adj + maquina de eventos atomicos"
      contains: "class WatermarkProcessor(QWidget):"
      min_lines: 700
  key_links:
    - from: "WatermarkRemove/ui/components/watermark_processor.py"
      to: "WatermarkRemove.wm_remove.remove_watermark"
      via: "from WatermarkRemove.wm_remove import find_wm, quick_align_preview, ..."
      pattern: "remove_watermark"
    - from: "WatermarkRemove/ui/components/watermark_processor.py"
      to: "WatermarkRemove.yolo.auto_detector.detect_watermarks"
      via: "from WatermarkRemove.yolo.auto_detector import detect_watermarks, resolve_png_for_class"
      pattern: "detect_watermarks"
    - from: "WatermarkRemove/ui/slideshow_viewer.py"
      to: "self.processor.preview_changed.connect(self.navigation.on_preview_changed)"
      via: "Composer wires processor signals → navigation slots"
      pattern: "self\\.processor\\..*\\.connect"
    - from: "WatermarkRemove/ui/components/watermark_processor.py"
      to: "image_processed = Signal(...)"
      via: "Signal emitida tras accept manual o auto — Plan 03 conecta a collector"
      pattern: "image_processed = Signal"
---

<objective>
Extraer toda la logica de procesamiento de marcas de agua desde `SlideshowViewer` hacia `WatermarkProcessor(QWidget)`: manual mode (preview en vivo + accept/revert con maquina de eventos atomicos), auto YOLO mode (deteccion + lista + accept), position-grid handlers (cuadros rojo/verde de posiciones guardadas via mousePressEvent y eventFilter), crop mode, y todos los callbacks de combos/spinboxes asociados.

Purpose: ARCH-01 SC-2 — la deteccion YOLO/auto y la ejecucion de `remove_watermark()` viven en componentes separados del widget de navegacion. Tambien restaura la regresion temporal del Plan 01: los overlays de posiciones y crop se pintan de nuevo, esta vez via signal/slot (`decorate_pixmap`) coordinado por el composer.

Output:
- `WatermarkRemove/ui/components/watermark_processor.py` completo con todos los metodos extraidos
- `WatermarkRemove/ui/components/navigation_controller.py` modificado para conectar `decorate_pixmap` slot a la cadena de render (signal/slot pattern)
- `WatermarkRemove/ui/slideshow_viewer.py` mas adelgazado — sin metodos de procesamiento; el composer wire signals processor↔navigation
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-slideshowviewer-decomposition/02-RESEARCH.md
@.planning/phases/02-slideshowviewer-decomposition/02-PATTERNS.md
@.planning/phases/02-slideshowviewer-decomposition/02-HUMAN-UAT.md
@.planning/phases/02-slideshowviewer-decomposition/02-01-PLAN.md
@WatermarkRemove/ui/slideshow_viewer.py
@WatermarkRemove/ui/components/navigation_controller.py
@WatermarkRemove/ui/components/watermark_processor.py
@WatermarkRemove/wm_remove.py
@WatermarkRemove/yolo/auto_detector.py
@CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implementar WatermarkProcessor con UI de controles + estado de eventos atomicos + manual/auto/crop logic</name>
  <files>
    WatermarkRemove/ui/components/watermark_processor.py
  </files>
  <read_first>
    WatermarkRemove/ui/slideshow_viewer.py,
    WatermarkRemove/ui/components/navigation_controller.py,
    .planning/phases/02-slideshowviewer-decomposition/02-PATTERNS.md,
    .planning/phases/02-slideshowviewer-decomposition/02-RESEARCH.md,
    WatermarkRemove/wm_remove.py,
    WatermarkRemove/yolo/auto_detector.py
  </read_first>
  <action>
    Implementar `WatermarkProcessor(QWidget)` extrayendo VERBATIM (copy-extract) los metodos de procesamiento desde `WatermarkRemove/ui/slideshow_viewer.py`. NO inventar logica nueva. Cada referencia a `self.image_files`, `self.current_index`, `self.working_image`, `self.output_folder`, `self.processed_images`, `self.processed_positions` que vivia en SlideshowViewer ahora apunta a `self._nav` (la referencia a NavigationController, recibida via constructor o via senales — preferir signals para sibling-to-sibling comunicacion, ver Pattern 2 PATTERNS line 632).

    **DECISION arquitectonica (RESEARCH Open Question #2)**: el processor mantiene el estado de `watermark_positions`, `watermark_rectangles`, `watermark_folder`, `watermark_files`, `watermark_files_all`, `watermark_alpha_values`, y expone un slot publico `decorate_pixmap(pixmap, scale_factor) -> QPixmap` que el NavigationController llama desde su `_apply_zoom`. El processor pinta los cuadros rojo/verde sobre el pixmap escalado y lo retorna. Esto resuelve la regresion temporal del Plan 01 y respeta el patron "render barato en navigation, dominio rico en processor".

    **Estado del componente** (en __init__ — espejando slideshow_viewer.py lineas 68-104):
    - `self.watermark_tab = watermark_tab`
    - `self.watermark_folder = None`
    - `self.watermark_name = None`
    - `self.watermark_positions = {}`
    - `self.watermark_files = []`
    - `self.watermark_files_all = []`
    - `self.watermark_rectangles = {}`
    - `self.crop_mode_enabled = False`
    - `self.auto_mode_enabled = False`
    - `self.detected_marks: list = []`
    - `self.selected_mark_index = -1`
    - `self.auto_preview_image: Optional[np.ndarray] = None`
    - `self.manual_mode_enabled = False`
    - `self.mouse_position = None`
    - `self.preview_image = None`
    - `self.is_preview_active = False`
    - `self.current_event_position: Optional[Tuple[int, int]] = None`
    - `self.current_event_watermark_index: Optional[int] = None`
    - `self.current_event_watermark: Optional[np.ndarray] = None`
    - `self.base_image_for_preview: Optional[np.ndarray] = None` (MIGRA desde SlideshowViewer aqui)
    - `self.watermark_alpha_values: dict = {}`

    **Path adjust** (PATTERNS line 648-656):
    - `current_dir = os.path.abspath(os.path.dirname(__file__))` — apunta a `WatermarkRemove/ui/components/`
    - `wm_dir = os.path.dirname(os.path.dirname(__file__))` — apunta a `WatermarkRemove/`
    - Reemplazar en `_load_watermark_folders` (linea 589: `wm_dir = os.path.dirname(current_dir)`) y `_load_watermark_positions` (linea 701: idem). Documentar el cambio con comentario verbatim del PATTERNS.

    **Imports** (top de archivo):
    - stdlib: `os`, `sys`, `from pathlib import Path`, `from typing import Optional, Tuple`
    - PySide6: `QWidget, QVBoxLayout, QHBoxLayout, QLabel, QPushButton, QComboBox, QLineEdit, QCheckBox, QSpinBox, QDoubleSpinBox, QListWidget, QListWidgetItem, QGroupBox, QMessageBox`
    - QtCore: `Qt, Signal, QEvent, QPoint, QRect`
    - QtGui: `QPainter, QPen, QColor, QPixmap, QMouseEvent`
    - `import numpy as np`
    - `from natsort import natsorted`
    - `from utils import UtilJson` (sigue usandose para `wm_positions.json` — RESEARCH out of scope ARCH-03 cubrio settings, no posiciones)
    - `from WatermarkRemove import align_watermark, remove_watermark`
    - `from WatermarkRemove.wm_remove import load_images_cv2, guardar, find_wm, quick_align_preview`
    - `from WatermarkRemove.yolo.auto_detector import detect_watermarks, resolve_png_for_class`
    - Imports lazy (no en top — preservar pattern de slideshow_viewer.py lines 1540, 1662, 1854): `from WatermarkRemove.yolo.training_collector import save_training_sample, remove_training_sample` — **NO** importar al top porque el `image_processed` signal le pasa los datos al collector (Plan 03); el processor NO llama directamente save_training_sample. La emision via signal desacopla.

    **Senales publicas** (declarar al inicio de la clase):
    - `preview_changed = Signal(object)` — comentario: `np.ndarray preview_image (sub-evento manual o auto preview)`
    - `image_processed = Signal(object, object, int, int, object, object, str, object)` — comentario: `(file_path, working_image, x, y, watermark_array, watermark_path, watermark_folder_name, base_image)` — para que `TrainingDataCollector` en Plan 03 capture todo el contexto via slot
    - `image_reset = Signal(object)` — comentario: `current_file_path` — emitida al ejecutar `_reset_current_image` para que collector limpie training samples
    - `processing_blocked = Signal(bool)` — comentario: `True=bloquear navegacion durante preview activo` (Pitfall 2)
    - `request_redraw = Signal()` — comentario: emitida cuando cambia un parametro visual que require re-render

    **UI a construir** (en `_setup_ui` — extraido de slideshow_viewer.py `_create_controls_panel` lineas 170-497, MENOS los grupos "Info" y "Navegacion" que ya viven en NavigationController + boton finish/cancel que queda en composer):
    - Checkbox `🤖 Modo detección automática` (lineas 217-223) — `self.auto_mode_checkbox`
    - GroupBox "📁 Selección" (`self.seleccion_group`, lineas 226-367) — combo carpetas marcas, filtro, combo marca, crop checkbox + spinbox + invert + apply, modo manual checkbox, alpha_adjust + label, offset_adj_container, quick_preview_checkbox, reset_btn, accept_btn, revert_btn
    - GroupBox "🤖 Detección automática" (`self.auto_group`, lineas 369-444) — auto_quick_preview_checkbox, detections_list, auto_offset_x/y, auto_delete_btn, auto_redetect_btn, auto_accept_btn, auto_accept_next_btn

    **Metodos a extraer (copy-extract verbatim)**:
    - `_log` (141-149) — replicar con fallback a watermark_tab
    - `_setup_ui` — extraer el UI listado arriba (slideshow_viewer.py lineas 170-497, parcial)
    - `_load_watermark_folders` (582-616) — **preservar verbatim** el `blockSignals(True/False)` bracket + el `_on_watermark_folder_changed` manual fire al final (PATTERNS line 658-668; RESEARCH Pitfall 3)
    - `_on_watermark_folder_changed` (618-638) — adaptar `if not self.output_folder and self.folder_path: self._create_output_folder()` → emitir signal `request_output_folder_creation` o conectar via slot a navigation. SIMPLIFICAR: en su lugar, `self._nav_request_create_output_folder()` que emite signal `output_folder_request = Signal()` y NavigationController la conecta a su `_create_output_folder`. Tambien adaptar `self._show_current_image()` → emitir `request_redraw`.
    - `_load_watermarks_into_combo` (640-654)
    - `_filter_watermark_combo` (656-676)
    - `_on_watermark_changed` (678-692) — **preservar** el `if hasattr(self, 'alpha_adjust'):` guard (RESEARCH Pitfall 3, PATTERNS line 327-338)
    - `_load_watermark_positions` (694-724)
    - `_toggle_crop_mode` (845-861) — adaptar self._apply_zoom() → emit request_redraw
    - `_on_crop_pixels_changed` (863-867) — idem
    - `_draw_crop_overlay` (869-898) — preservar (sera invocado desde `decorate_pixmap`)
    - `_apply_crop` (900-932) — adaptar referencias: `self.image_files[self.current_index]` → recibir via senal `on_image_changed(index, path, np_array)` y mantener cache local `self._current_file`, `self._current_index`; `self.working_image` → recibir y modificar via slot/signal a navigation. Despues de modificar, emit `image_processed` para que navigation actualice su working_image.
    - `_draw_watermark_overlays` (934-1049) — preservar — sera invocado desde `decorate_pixmap`
    - `_process_watermark_at_position` (1178-1258) — adaptar `self.output_folder` → cached desde signal de navigation; `self._show_current_image()` → emit request_redraw; `self._next_image()` → emit `request_advance` signal que navigation conecta a `request_next`
    - `eventFilter` (1260-1281) — **CRITICO**: este filter esta instalado sobre `image_label` que vive en NavigationController. **Solucion** (RESEARCH Open Question + Pitfall 1): NavigationController instala su propio eventFilter, detecta clicks en image_label, y emite `image_clicked = Signal(QPoint, str)` con `(pos, button_type)` donde button_type ∈ {"left", "right"}. WatermarkProcessor conecta este signal a un slot `on_image_clicked(pos, button)` que reemplaza la logica del eventFilter actual (mouse move + left/right click). Tambien NavigationController emite `mouse_moved = Signal(QPoint)` para el manual overlay tracking.
    - `_toggle_manual_mode` (1283-1316) — adaptar `self.image_label.setMouseTracking(True)` → emit signal `manual_tracking_requested = Signal(bool)` que navigation conecta a `set_mouse_tracking`; `self.manual_overlay_label` → owns by navigation, processor emite signal para mostrarlo/ocultarlo (`manual_overlay_visibility = Signal(bool)`)
    - `_update_manual_overlay` (1318-1357) — adaptar para emitir signal `manual_overlay_geometry = Signal(int, int, int, int)` que navigation conecta a `setGeometry` sobre `manual_overlay_label`
    - `_on_alpha_changed` (1359-1383) — preservar logica; al final emit `preview_changed` con `self.preview_image`
    - `_on_offset_adj_changed` (1385-1400) — idem
    - `_compute_live_preview` (1402-1417) — preservar
    - `_remove_watermark_preview` (1419-1509) — preservar. Adaptar bloqueo UI: en lugar de `self.next_btn.setEnabled(False)` etc, emit `processing_blocked.emit(True)`. NavigationController conecta a `set_navigation_enabled(not blocked)`. `self.watermark_combo.setEnabled(False)` queda interno (combo es del processor). El call `self._apply_zoom()` al final → emit `preview_changed.emit(self.preview_image)`.
    - `_accept_preview` (1511-1589) — preservar. La parte `from WatermarkRemove.yolo.training_collector import save_training_sample` y el bloque `try: save_training_sample(...) except: ...` se REEMPLAZA por emitir `image_processed.emit(current_file, self.working_image, best_x, best_y, self.current_event_watermark, wm_file, self.watermark_folder.name, self.base_image_for_preview)`. El collector (Plan 03) recibira el signal y hara save_training_sample en su slot. Al final, en lugar de `self.next_btn.setEnabled(True)` → emit `processing_blocked.emit(False)`. `self.watermark_combo.setEnabled(True)` queda interno.
    - `_revert_preview` (1591-1618) — idem (emit processing_blocked(False))
    - `_reset_current_image` (1620-1670) — preservar. La parte `from WatermarkRemove.yolo.training_collector import remove_training_sample; remove_training_sample(current_dir, current_file, self._log)` se REEMPLAZA por emit `image_reset.emit(current_file)` — el collector (Plan 03) maneja remove_training_sample en su slot. `self.working_image = load_images_cv2(current_file)` se REEMPLAZA por emit signal `request_image_reload = Signal()` y navigation se encarga. `self._show_current_image()` → emit request_redraw.
    - `_toggle_auto_mode` (1674-1689)
    - `_run_auto_detection` (1691-1752) — preservar; `self.working_image` → cache local actualizado via slot `on_image_changed`
    - `_populate_detections_list` (1754-1764)
    - `_on_detection_selected` (1766-1780)
    - `_on_auto_offset_changed` (1782-1789)
    - `_delete_selected_detection` (1791-1798)
    - `_refresh_auto_preview` (1800-1823) — al final emit `preview_changed.emit(self.auto_preview_image)` en lugar de `self._apply_zoom()`
    - `_accept_auto_detections` (1825-1885) — para cada mark, emit `image_processed.emit(...)` (lista de detecciones procesadas — el collector recibira un signal por cada). Al final emit `request_redraw`.

    **Slots publicos para wiring del composer**:
    - `on_image_changed(index, path, working_image)` — cache local: `self._current_index = index; self._current_file = path; self._working_image = working_image; self.detected_marks = []` etc; al cambiar de imagen, refresh combos si necesario y NO disparar auto-detection a menos que `self.auto_mode_enabled` (en cuyo caso llamar `self._run_auto_detection()`)
    - `on_output_folder_ready(output_folder)` — cache local: `self._output_folder = output_folder`
    - `on_image_clicked(pos: QPoint, button: str)` — reemplazo del eventFilter+mousePressEvent: if button == "left" and self.manual_mode_enabled: `self._remove_watermark_preview(pos)`; if button == "right" and self.manual_mode_enabled and self.is_preview_active: `self._revert_preview()`; if button in ("left","right") and not self.manual_mode_enabled and self.watermark_rectangles: replicar `mousePressEvent` (1913-1953) verbatim, adaptando `self.watermark_rectangles` (vive aqui) y dispatch a `_process_watermark_at_position`. PASS el `pos` directamente (sin recalcular scroll_pos — navigation hizo la conversion antes de emitir).
    - `on_mouse_moved(pos: QPoint)` — para manual overlay tracking
    - `is_preview_active(self) -> bool` — getter publico para que composer keyPressEvent pueda chequear
    - `accept_preview(self)` — slot publico (delegar a `_accept_preview`)
    - `revert_preview(self)` — slot publico (delegar a `_revert_preview`)
    - `decorate_pixmap(self, pixmap: QPixmap, scale_factor: float) -> QPixmap` — slot publico **CRITICO** para restaurar overlays:
      ```
      result = pixmap
      # Overlays de posiciones rojo/verde (solo si NO modo recorte/manual/auto, y hay positions+files cargados)
      if (self.watermark_positions and self.watermark_files
              and not self.manual_mode_enabled and not self.crop_mode_enabled
              and not self.auto_mode_enabled):
          result = self._draw_watermark_overlays(result, scale_factor)
      # Overlay de crop
      if self.crop_mode_enabled:
          result = self._draw_crop_overlay(result, scale_factor)
      # Highlight amarillo modo auto
      if (self.auto_mode_enabled and 0 <= self.selected_mark_index < len(self.detected_marks)):
          # codigo verbatim lineas 824-839
          ...
      return result
      ```
      Esto replica las lineas 814-839 de slideshow_viewer.py pero ahora vive en el processor; NavigationController._apply_zoom llama `pixmap = self.processor_decorate_callback(pixmap, scale_factor)` si el callback esta seteado.

    **min_lines target:** ~700-900 lineas (la mayoria del archivo original esta aqui).
  </action>
  <verify>
    <automated>python -m py_compile WatermarkRemove/ui/components/watermark_processor.py</automated>
    <automated>python -c "from WatermarkRemove.ui.components import WatermarkProcessor; print(WatermarkProcessor.preview_changed); print(WatermarkProcessor.image_processed); print(WatermarkProcessor.image_reset); print(WatermarkProcessor.processing_blocked)"</automated>
    <automated>grep -cE "def (decorate_pixmap|on_image_changed|on_image_clicked|on_mouse_moved|is_preview_active|accept_preview|revert_preview)" WatermarkRemove/ui/components/watermark_processor.py</automated>
    <automated>grep -cE "(remove_watermark\(|detect_watermarks\(|find_wm\(|quick_align_preview\(|align_watermark\()" WatermarkRemove/ui/components/watermark_processor.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class WatermarkProcessor(QWidget):" WatermarkRemove/ui/components/watermark_processor.py` retorna 1
    - `grep -c "preview_changed = Signal" WatermarkRemove/ui/components/watermark_processor.py` retorna 1
    - `grep -c "image_processed = Signal" WatermarkRemove/ui/components/watermark_processor.py` retorna 1
    - `grep -c "image_reset = Signal" WatermarkRemove/ui/components/watermark_processor.py` retorna 1
    - `grep -c "processing_blocked = Signal" WatermarkRemove/ui/components/watermark_processor.py` retorna 1
    - `grep -cE "def (decorate_pixmap|on_image_changed|on_image_clicked|on_mouse_moved|is_preview_active|accept_preview|revert_preview)" WatermarkRemove/ui/components/watermark_processor.py` >= 7
    - `grep -cE "def (_remove_watermark_preview|_accept_preview|_revert_preview|_process_watermark_at_position|_run_auto_detection|_accept_auto_detections|_compute_live_preview|_draw_watermark_overlays|_draw_crop_overlay|_load_watermark_folders|_load_watermark_positions|_toggle_manual_mode|_toggle_auto_mode|_toggle_crop_mode|_apply_crop)" WatermarkRemove/ui/components/watermark_processor.py` >= 15
    - `grep -c "blockSignals(True)" WatermarkRemove/ui/components/watermark_processor.py` >= 4 (preservados defensivos)
    - `grep -c "hasattr(self, 'alpha_adjust')" WatermarkRemove/ui/components/watermark_processor.py` retorna 1 (preservado Pitfall 3)
    - `grep -c "from WatermarkRemove.yolo.auto_detector import detect_watermarks" WatermarkRemove/ui/components/watermark_processor.py` retorna 1
    - `grep -c "from WatermarkRemove.wm_remove import" WatermarkRemove/ui/components/watermark_processor.py` retorna 1
    - `wc -l WatermarkRemove/ui/components/watermark_processor.py` >= 700
    - `python -m py_compile WatermarkRemove/ui/components/watermark_processor.py` sale 0
    - `python -c "from WatermarkRemove.ui.components import WatermarkProcessor"` sale 0
  </acceptance_criteria>
  <done>
    WatermarkProcessor(QWidget) implementado y autocontenido — UI propia (combos, checkboxes, alpha/offset spinboxes, accept/revert/reset buttons, detection list, auto buttons), estado completo (positions, rectangles, detected_marks, event-machine fields, etc), slots publicos para wiring, senales para emitir cambios (preview/processed/reset/blocked/redraw), y `decorate_pixmap` para restaurar overlays. Compila e importable. Sin regresion logica (codigo extraido verbatim, defensivos preservados).
  </done>
</task>

<task type="auto">
  <name>Task 2: Modificar NavigationController para emit image_clicked/mouse_moved + invocar processor.decorate_pixmap en _apply_zoom</name>
  <files>
    WatermarkRemove/ui/components/navigation_controller.py
  </files>
  <read_first>
    WatermarkRemove/ui/components/navigation_controller.py,
    WatermarkRemove/ui/components/watermark_processor.py,
    WatermarkRemove/ui/slideshow_viewer.py,
    .planning/phases/02-slideshowviewer-decomposition/02-PATTERNS.md
  </read_first>
  <action>
    Modificar `WatermarkRemove/ui/components/navigation_controller.py` para soportar el wiring que el processor necesita. Cambios puntuales:

    1. **Agregar senales publicas** (al inicio de la clase, junto a las existentes):
       - `image_clicked = Signal(object, str)` — `(QPoint pos_in_image_coords, button_type "left"|"right")` — emitida cuando el usuario clickea sobre image_label
       - `mouse_moved = Signal(object)` — `QPoint pos_in_image_coords` — emitida en mouse move sobre image_label cuando mouseTracking esta activo (manual mode)
       - `output_folder_request = Signal()` — slot del processor para pedir `_create_output_folder`

    2. **Agregar slot publico `set_processor_decorator(callback)`** — el composer llamara `self.navigation.set_processor_decorator(self.processor.decorate_pixmap)` para que NavigationController pueda invocar al processor desde su `_apply_zoom`:
       ```
       def set_processor_decorator(self, callback):
           """Registra el callback decorate_pixmap del processor (Plan 02 wiring)."""
           self._processor_decorate = callback
       ```
       Inicializar `self._processor_decorate = None` en __init__.

    3. **Modificar `_apply_zoom`** (donde estaba el `# TODO Plan 02:` del Plan 01) — invocar el decorator si esta seteado:
       ```
       # Plan 02 wiring: el processor decora el pixmap con overlays de posiciones/crop/auto-highlight
       if self._processor_decorate is not None:
           scaled_pixmap = self._processor_decorate(scaled_pixmap, scale_factor)
       ```
       Esta linea va inmediatamente despues de `scaled_pixmap = pixmap_to_scale.scaled(...)` (donde se hace el escalado), antes de `self.image_label.setPixmap(scaled_pixmap)`. **Esto restaura los overlays de posiciones (rojo/verde) y crop overlay** que se perdieron en el Plan 01.

    4. **Agregar slot publico `set_mouse_tracking(enabled: bool)`** — el processor lo invoca al toggle del manual mode:
       ```
       def set_mouse_tracking(self, enabled: bool):
           self.image_label.setMouseTracking(enabled)
       ```

    5. **Agregar slot publico `set_manual_overlay_visible(visible: bool)`** — para mostrar/ocultar `manual_overlay_label`:
       ```
       def set_manual_overlay_visible(self, visible: bool):
           if visible: self.manual_overlay_label.show()
           else: self.manual_overlay_label.hide()
       ```

    6. **Agregar slot publico `set_manual_overlay_geometry(x, y, w, h)`** — para que processor mueva el overlay manual:
       ```
       def set_manual_overlay_geometry(self, x: int, y: int, w: int, h: int):
           self.manual_overlay_label.setGeometry(x, y, w, h)
           self.manual_overlay_label.raise_()
       ```

    7. **Modificar `eventFilter`** (que ya vive en NavigationController desde Plan 01, instalado sobre image_label en el panel image). Si NavigationController NO tenia un eventFilter en Plan 01, agregarlo ahora: instalar sobre `self.image_label` con `self.image_label.installEventFilter(self)`. El handler:
       ```
       def eventFilter(self, watched, event):
           if watched is self.image_label:
               if event.type() == QEvent.Type.MouseMove:
                   # Convertir pos a coords de imagen (sin escala de zoom)
                   scale_factor = self.zoom_level / 100.0
                   image_x = int(event.pos().x() / scale_factor)
                   image_y = int(event.pos().y() / scale_factor)
                   self.mouse_moved.emit(QPoint(image_x, image_y))
                   return False  # propagar
               if event.type() == QEvent.Type.MouseButtonPress:
                   if event.button() == Qt.MouseButton.LeftButton:
                       self.image_clicked.emit(event.pos(), "left")
                       return True  # consumir
                   if event.button() == Qt.MouseButton.RightButton:
                       self.image_clicked.emit(event.pos(), "right")
                       return True
           return super().eventFilter(watched, event)
       ```
       **NOTA**: el `event.pos()` que se emite via `image_clicked` esta en coordenadas del image_label (con zoom). El processor en `on_image_clicked` recibe ESAS coordenadas y replica las conversiones que originalmente hacia `mousePressEvent` de SlideshowViewer (lineas 1929-1939). Documentar esta convencion en docstring del signal.

    8. **Agregar slot publico `reset_current_image()`** (slot que el composer conecta a `processor.image_reset` para que NavigationController recargue desde disco):
       ```
       def reset_current_image(self):
           """Recarga working_image desde disco y refresca display (Plan 02 wiring)."""
           if not self.image_files:
               return
           current_file = self.image_files[self.current_index]
           self.working_image = load_images_cv2(current_file)
           # Quitar marcadores
           self.processed_images.discard(self.current_index)
           self.processed_positions.pop(self.current_index, None)
           self._show_current_image()
       ```

    9. **Modificar slot `on_image_processed`** — agregar el set/discard de processed_positions si el processor pasa pos_name (pero como la firma actual es `(file, working_image, x, y, ...)` sin pos_name, simplemente actualizar working_image y marcar processed). Para `_process_watermark_at_position` que pasa pos_name, agregar slot adicional `on_position_processed(pos_name)` que actualiza `processed_positions`.

    10. **Conservar TODOS los metodos del Plan 01** sin tocar (load_image_list, show_current_image, set/get zoom, request_next, request_previous, etc).
  </action>
  <verify>
    <automated>python -m py_compile WatermarkRemove/ui/components/navigation_controller.py</automated>
    <automated>grep -cE "(image_clicked|mouse_moved|set_processor_decorator|set_mouse_tracking|set_manual_overlay_visible|set_manual_overlay_geometry|reset_current_image)" WatermarkRemove/ui/components/navigation_controller.py</automated>
    <automated>grep -c "self._processor_decorate" WatermarkRemove/ui/components/navigation_controller.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "image_clicked = Signal" WatermarkRemove/ui/components/navigation_controller.py` retorna 1
    - `grep -c "mouse_moved = Signal" WatermarkRemove/ui/components/navigation_controller.py` retorna 1
    - `grep -cE "def (set_processor_decorator|set_mouse_tracking|set_manual_overlay_visible|set_manual_overlay_geometry|reset_current_image)" WatermarkRemove/ui/components/navigation_controller.py` >= 5
    - `grep -c "self._processor_decorate" WatermarkRemove/ui/components/navigation_controller.py` >= 2 (init + invocacion en _apply_zoom)
    - `grep -c "self.image_label.installEventFilter(self)" WatermarkRemove/ui/components/navigation_controller.py` retorna 1
    - `grep -c "def eventFilter(self, watched, event)" WatermarkRemove/ui/components/navigation_controller.py` retorna 1
    - `python -m py_compile WatermarkRemove/ui/components/navigation_controller.py` sale 0
    - `python -c "from WatermarkRemove.ui.components import NavigationController; print(NavigationController.image_clicked); print(NavigationController.mouse_moved)"` sale 0
  </acceptance_criteria>
  <done>
    NavigationController expone slots y senales que permiten al processor (1) recibir clicks/mouse moves sobre image_label, (2) bloquear nav durante preview, (3) decorar pixmap con overlays via callback, (4) controlar manual_overlay_label, y (5) reset de imagen completa. La regresion temporal de overlays del Plan 01 esta resuelta en cuanto SlideshowViewer wire el callback (Task 3).
  </done>
</task>

<task type="auto">
  <name>Task 3: Refactorizar SlideshowViewer — eliminar metodos de processing, wire signals processor↔navigation</name>
  <files>
    WatermarkRemove/ui/slideshow_viewer.py
  </files>
  <read_first>
    WatermarkRemove/ui/slideshow_viewer.py,
    WatermarkRemove/ui/components/navigation_controller.py,
    WatermarkRemove/ui/components/watermark_processor.py,
    .planning/phases/02-slideshowviewer-decomposition/02-PATTERNS.md
  </read_first>
  <action>
    Eliminar de `WatermarkRemove/ui/slideshow_viewer.py` todos los metodos que ahora viven en `WatermarkProcessor`, eliminar los campos de estado de processing del `__init__`, y agregar el wiring de senales entre `self.processor` y `self.navigation`.

    **Cambios concretos:**

    1. **`__init__`** — eliminar los siguientes campos (todos viven en self.processor ahora):
       - `self.watermark_folder`, `self.watermark_name`, `self.watermark_positions`, `self.watermark_files`, `self.watermark_files_all`, `self.watermark_rectangles`
       - `self.crop_mode_enabled`, `self.auto_mode_enabled`, `self.detected_marks`, `self.selected_mark_index`, `self.auto_preview_image`
       - `self.manual_mode_enabled`, `self.mouse_position`, `self.preview_image`, `self.is_preview_active`
       - `self.current_event_position`, `self.current_event_watermark_index`, `self.current_event_watermark`, `self.base_image_for_preview`
       - `self.watermark_alpha_values`

    2. **Wire signals + slots (en `__init__` despues de instanciar nav/processor/collector)**:
       ```
       # Navegacion -> Processor (notificar cambio de imagen)
       self.navigation.image_changed.connect(self.processor.on_image_changed)
       self.navigation.output_folder_ready.connect(self.processor.on_output_folder_ready)
       self.navigation.image_clicked.connect(self.processor.on_image_clicked)
       self.navigation.mouse_moved.connect(self.processor.on_mouse_moved)

       # Processor -> Navegacion (preview / final / bloqueo UI / reset / output folder request)
       self.processor.preview_changed.connect(self.navigation.on_preview_changed)
       self.processor.image_processed.connect(self.navigation.on_image_processed)
       self.processor.processing_blocked.connect(lambda blocked: self.navigation.set_navigation_enabled(not blocked))
       self.processor.image_reset.connect(self.navigation.reset_current_image)
       self.processor.request_redraw.connect(self.navigation._apply_zoom)
       self.processor.output_folder_request.connect(self.navigation._create_output_folder)
       self.processor.manual_tracking_requested.connect(self.navigation.set_mouse_tracking)
       self.processor.manual_overlay_visibility.connect(self.navigation.set_manual_overlay_visible)
       self.processor.manual_overlay_geometry.connect(self.navigation.set_manual_overlay_geometry)

       # Wire decorate_pixmap callback (restaura overlays de posiciones y crop)
       self.navigation.set_processor_decorator(self.processor.decorate_pixmap)
       ```

    3. **`_setup_ui`** — completar el panel izquierdo con `self.processor` ahora:
       - El panel izquierdo se construye en `_create_controls_panel`. En lugar de construir todos los GroupBoxes inline, ahora:
         - Eliminar los grupos "Selección" y "Detección automática" (lineas 226-444) — viven en `self.processor`
         - El layout izquierdo ahora hace `controls_layout.addWidget(self.processor)` para insertar el QWidget del processor con sus GroupBoxes propios
         - Conservar SOLO el grupo "Navegación" (con finish_btn + cancel_btn — sin prev/next que estan en navigation) y el grupo "Datos recopilados" (vive todavia inline hasta Plan 03)
       - El processor `_setup_ui` construye sus propios GroupBoxes internamente — el composer solo lo agrega al layout via `addWidget`

    4. **Eliminar metodos** (todos viven en self.processor):
       - `_load_watermark_folders` (582-616)
       - `_on_watermark_folder_changed` (618-638)
       - `_load_watermarks_into_combo` (640-654)
       - `_filter_watermark_combo` (656-676)
       - `_on_watermark_changed` (678-692)
       - `_load_watermark_positions` (694-724)
       - `_toggle_crop_mode` (845-861)
       - `_on_crop_pixels_changed` (863-867)
       - `_draw_crop_overlay` (869-898)
       - `_apply_crop` (900-932)
       - `_draw_watermark_overlays` (934-1049)
       - `_process_watermark_at_position` (1178-1258)
       - `eventFilter` (1260-1281) — vive en NavigationController ahora
       - `_toggle_manual_mode` (1283-1316)
       - `_update_manual_overlay` (1318-1357)
       - `_on_alpha_changed` (1359-1383)
       - `_on_offset_adj_changed` (1385-1400)
       - `_compute_live_preview` (1402-1417)
       - `_remove_watermark_preview` (1419-1509)
       - `_accept_preview` (1511-1589)
       - `_revert_preview` (1591-1618)
       - `_reset_current_image` (1620-1670)
       - `_toggle_auto_mode` (1674-1689)
       - `_run_auto_detection` (1691-1752)
       - `_populate_detections_list` (1754-1764)
       - `_on_detection_selected` (1766-1780)
       - `_on_auto_offset_changed` (1782-1789)
       - `_delete_selected_detection` (1791-1798)
       - `_refresh_auto_preview` (1800-1823)
       - `_accept_auto_detections` (1825-1885)
       - `mousePressEvent` (1913-1953) — vive en NavigationController.eventFilter ahora
       - `wheelEvent` (1955-1966) — delega a navigation (ya esta alli)

       Eliminar imports muertos:
       - `from WatermarkRemove import align_watermark, remove_watermark` (lineas 26)
       - `from WatermarkRemove.wm_remove import load_images_cv2, guardar, find_wm, quick_align_preview` (linea 27)
       - `from WatermarkRemove.yolo.auto_detector import detect_watermarks, resolve_png_for_class` (linea 28)
       - `from utils import UtilJson` (linea 22) — verificar si todavia se usa; sino eliminar
       - `import numpy as np` (linea 24) — verificar si todavia se usa; sino eliminar
       - `from natsort import natsorted` (linea 25) — verificar si todavia se usa; sino eliminar

    5. **`keyPressEvent`** — refactorizar siguiendo PATTERNS lines 515-551:
       ```
       def keyPressEvent(self, event):
           key = event.key()
           # Zoom keys → navigation
           if key in (Qt.Key.Key_Plus, Qt.Key.Key_Equal):
               self.navigation.adjust_zoom(10); event.accept(); return
           if key == Qt.Key.Key_Minus:
               self.navigation.adjust_zoom(-10); event.accept(); return
           if key == Qt.Key.Key_0:
               self.navigation.set_zoom_level(100); event.accept(); return
           # Si hay preview activo en processor, Space/Backspace van a accept/revert
           if self.processor.is_preview_active():
               if key == Qt.Key.Key_Space:
                   self.processor.accept_preview(); event.accept(); return
               if key == Qt.Key.Key_Backspace:
                   self.processor.revert_preview(); event.accept(); return
           # Caso normal: navegacion
           if key == Qt.Key.Key_Space:
               self.navigation.request_next(); event.accept(); return
           if key == Qt.Key.Key_Backspace:
               self.navigation.request_previous(); event.accept(); return
           super().keyPressEvent(event)
       ```
       **CRITICO** (RESEARCH Pitfall 2): el check `if self.processor.is_preview_active()` MUST go BEFORE navigation. Orden importa.

    6. **Conservar metodos** (no migran en este Plan):
       - `_log` (141-149) — todavia usado por `_finish_review`/`_cancel_review` para logging
       - `_update_counts_label` (115-139) — migra en Plan 03
       - `_finish_review` (1887-1898)
       - `_cancel_review` (1900-1911)
       - `__init__` (refactorizado)
       - `_setup_ui` (refactorizado)
       - `_create_controls_panel` (refactorizado — solo conserva nav buttons + conteo group)
       - API publica `get_approved`, `get_output_folder`, `has_processed_images` (2008-2018)
       - `current_dir` global (todavia se usa por `_update_counts_label`; migra en Plan 03)

    7. **El bloque `if __name__ == "__main__":`** (2022-2040) — conservar verbatim (test entry point para `python -m WatermarkRemove.ui.slideshow_viewer`).

    **Verificacion final**: el archivo `slideshow_viewer.py` debe pasar de ~2040 lineas a aproximadamente 250-400 lineas.
  </action>
  <verify>
    <automated>python -m py_compile WatermarkRemove/ui/slideshow_viewer.py</automated>
    <automated>python -c "from WatermarkRemove.ui import SlideshowViewer; print('OK')"</automated>
    <automated>grep -cE "^\s*def (_load_watermark_folders|_on_watermark_folder_changed|_load_watermarks_into_combo|_filter_watermark_combo|_on_watermark_changed|_load_watermark_positions|_toggle_crop_mode|_on_crop_pixels_changed|_draw_crop_overlay|_apply_crop|_draw_watermark_overlays|_process_watermark_at_position|eventFilter|_toggle_manual_mode|_update_manual_overlay|_on_alpha_changed|_on_offset_adj_changed|_compute_live_preview|_remove_watermark_preview|_accept_preview|_revert_preview|_reset_current_image|_toggle_auto_mode|_run_auto_detection|_populate_detections_list|_on_detection_selected|_on_auto_offset_changed|_delete_selected_detection|_refresh_auto_preview|_accept_auto_detections|mousePressEvent|wheelEvent)" WatermarkRemove/ui/slideshow_viewer.py</automated>
    <automated>grep -cE "(remove_watermark\(|detect_watermarks\(|find_wm\(|quick_align_preview\(|resolve_png_for_class\(|align_watermark\()" WatermarkRemove/ui/slideshow_viewer.py</automated>
    <automated>grep -c "self.processor" WatermarkRemove/ui/slideshow_viewer.py</automated>
    <automated>grep -c "self.navigation" WatermarkRemove/ui/slideshow_viewer.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\s*def (_load_watermark_folders|_on_watermark_folder_changed|_load_watermarks_into_combo|_filter_watermark_combo|_on_watermark_changed|_load_watermark_positions|_toggle_crop_mode|_on_crop_pixels_changed|_draw_crop_overlay|_apply_crop|_draw_watermark_overlays|_process_watermark_at_position|eventFilter|_toggle_manual_mode|_update_manual_overlay|_on_alpha_changed|_on_offset_adj_changed|_compute_live_preview|_remove_watermark_preview|_accept_preview|_revert_preview|_reset_current_image|_toggle_auto_mode|_run_auto_detection|_populate_detections_list|_on_detection_selected|_on_auto_offset_changed|_delete_selected_detection|_refresh_auto_preview|_accept_auto_detections|mousePressEvent)" WatermarkRemove/ui/slideshow_viewer.py` retorna 0
    - `grep -cE "(remove_watermark\(|detect_watermarks\(|find_wm\(|quick_align_preview\(|resolve_png_for_class\(|align_watermark\()" WatermarkRemove/ui/slideshow_viewer.py` retorna 0 (no quedan calls directos a la API de wm_remove/auto_detector)
    - `grep -c "self.processor.preview_changed.connect" WatermarkRemove/ui/slideshow_viewer.py` retorna 1
    - `grep -c "self.processor.image_processed.connect" WatermarkRemove/ui/slideshow_viewer.py` retorna 1
    - `grep -c "self.processor.is_preview_active()" WatermarkRemove/ui/slideshow_viewer.py` >= 1
    - `grep -c "self.navigation.set_processor_decorator(self.processor.decorate_pixmap)" WatermarkRemove/ui/slideshow_viewer.py` retorna 1
    - `grep -c "self.navigation.image_clicked.connect" WatermarkRemove/ui/slideshow_viewer.py` retorna 1
    - `grep -c "manual_tracking_requested.connect" WatermarkRemove/ui/slideshow_viewer.py` >= 1
    - `grep -c "manual_overlay_visibility.connect" WatermarkRemove/ui/slideshow_viewer.py` >= 1
    - `grep -c "manual_overlay_geometry.connect" WatermarkRemove/ui/slideshow_viewer.py` >= 1
    - `grep -c "class SlideshowViewer(QDialog):" WatermarkRemove/ui/slideshow_viewer.py` retorna 1
    - `grep -c "review_completed = Signal(bool)" WatermarkRemove/ui/slideshow_viewer.py` retorna 1
    - `grep -cE "def (get_approved|get_output_folder|has_processed_images)" WatermarkRemove/ui/slideshow_viewer.py` retorna 3
    - `grep -c "def __init__(self, folder_path: str, parent=None, watermark_tab=None)" WatermarkRemove/ui/slideshow_viewer.py` retorna 1
    - `wc -l WatermarkRemove/ui/slideshow_viewer.py` < 500
    - `python -m py_compile WatermarkRemove/ui/slideshow_viewer.py WatermarkRemove/ui/components/*.py` sale 0
    - `python -c "from WatermarkRemove.ui import SlideshowViewer; from WatermarkRemove.ui.components import NavigationController, WatermarkProcessor, TrainingDataCollector"` sale 0
    - `git diff gui/controller.py` retorna vacio (gui/controller.py NO modificado)
    - Manual: `python -m WatermarkRemove.ui.slideshow_viewer` lanza el visor, Space/Backspace navega, modo manual + auto + reset funcionan, los cuadros rojo/verde y crop overlay se pintan de vuelta
  </acceptance_criteria>
  <done>
    SlideshowViewer es un composer adelgazado (<500 lineas): instancia los 3 componentes, wire las senales, conserva keyPressEvent guard de preview, conserva API publica intacta, conserva `_finish_review`/`_cancel_review` y `_update_counts_label` (este ultimo migra en Plan 03). El visor funcionalmente equivalente al pre-refactor — manual + auto + position-grid + reset + crop + zoom funcionan. Overlays de posiciones y crop restaurados (regresion Plan 01 resuelta).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WatermarkProcessor → wm_remove.remove_watermark | numpy arrays + path; mitigaciones existentes en wm_remove.py heredadas |
| WatermarkProcessor → auto_detector.detect_watermarks | numpy array; mitigaciones existentes en auto_detector.py (cache de modelo, error handling) heredadas |
| WatermarkProcessor → UtilJson (wm_positions.json) | path es derivado de `wm_dir = os.path.dirname(os.path.dirname(__file__))` (constante); JSON parsing defensive (try/except) preservado verbatim |
| NavigationController eventFilter → WatermarkProcessor.on_image_clicked | sibling-to-sibling via composer-wired signal; pos coords no son user input directo (vienen de Qt QMouseEvent) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02-01 | Tampering | WatermarkProcessor._load_watermark_positions (UtilJson read) | accept | Defensive parsing JSON existente (try/except, isinstance dict) preservado VERBATIM (slideshow_viewer.py lines 700-724). RESEARCH Pitfall 3 prohíbe "cleaning". |
| T-02-02-02 | Tampering | WatermarkProcessor._load_watermark_folders (filesystem read) | accept | `Path` objects sin string concat; mitigacion existente. |
| T-02-02-03 | Information Disclosure | WatermarkProcessor.image_processed signal payload | accept | Payload contiene numpy arrays + paths locales; no se serializa fuera del proceso. El collector (Plan 03) usara base64 para training_data.json — mitigacion existente en training_collector.py. |
| T-02-02-04 | DoS | WatermarkProcessor._run_auto_detection (YOLO inference) | accept | Cache de modelo gestionado internamente por auto_detector.py; QMessageBox para FileNotFoundError preservado. |
| T-02-02-05 | Tampering | NavigationController.eventFilter (mouse coord conversion) | accept | Coords se calculan via `event.pos() / scale_factor` — operacion sobre datos Qt internos, no user input arbitrary. |
| T-02-02-SC | Tampering | npm/pip/cargo installs | N/A | No se instalan paquetes (refactor 100% estructural — RESEARCH N/A). |

Conclusion: ningun threat nuevo introducido. Todos los mitigantes existentes se heredan verbatim al extraer codigo desde slideshow_viewer.py.
</threat_model>

<verification>
- `python -m py_compile WatermarkRemove/ui/slideshow_viewer.py WatermarkRemove/ui/components/*.py` sale 0
- `grep -cE "(remove_watermark\(|detect_watermarks\(|find_wm\(|quick_align_preview\()" WatermarkRemove/ui/slideshow_viewer.py` retorna 0
- Manual UAT Secciones 2 + 3 (Modo Manual + Posiciones Guardadas + Modo Auto YOLO) de 02-HUMAN-UAT.md completadas
- Manual UAT Seccion 6 (Comportamiento Observable Identico) — cuadros rojo/verde + crop overlay se pintan de vuelta
- `git diff gui/controller.py` vacio
</verification>

<success_criteria>
- WatermarkProcessor implementado con 30+ metodos extraidos verbatim
- NavigationController extendido con eventFilter (image_clicked + mouse_moved signals) + decorate_pixmap callback hook
- SlideshowViewer reducido a <500 lineas — composer puro, sin metodos de processing
- Sin imports muertos de wm_remove/auto_detector/numpy/natsort en slideshow_viewer.py si ya no se usan
- Overlays de posiciones (rojo/verde) y crop overlay restaurados — la regresion temporal del Plan 01 ya no existe
- API publica del SlideshowViewer intacta; gui/controller.py funciona sin cambios
- Defensivos preservados verbatim (blockSignals brackets, hasattr guards)
</success_criteria>

<output>
Al completar este plan, crear `.planning/phases/02-slideshowviewer-decomposition/02-02-SUMMARY.md`. Incluir:
- Lista de los ~30 metodos migrados desde slideshow_viewer.py a watermark_processor.py
- Confirmacion de que defensivos (blockSignals, hasattr) fueron preservados verbatim
- Resultado de manual UAT Secciones 2, 3 y 6
- Edge count interim del SlideshowViewer (medir con grep -cE "(self\\.navigation|self\\.processor|self\\.collector)\\." slideshow_viewer.py para baseline antes del Plan 03)
- Confirmacion de que `gui/controller.py:321` sigue funcionando sin modificacion
</output>
