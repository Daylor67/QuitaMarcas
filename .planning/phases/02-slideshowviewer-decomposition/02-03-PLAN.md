---
phase: 02-slideshowviewer-decomposition
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - WatermarkRemove/ui/components/training_data_collector.py
  - WatermarkRemove/ui/slideshow_viewer.py
autonomous: false
requirements:
  - ARCH-01
must_haves:
  truths:
    - "TrainingDataCollector(QWidget) en WatermarkRemove/ui/components/training_data_collector.py contiene el GroupBox de conteo y los slots on_image_processed/on_image_reset que llaman save_training_sample/remove_training_sample"
    - "SlideshowViewer ya no contiene los metodos _update_counts_label ni el GroupBox 'Datos recopilados' inline — viven en TrainingDataCollector"
    - "El usuario ve el conteo de training samples actualizado en tiempo real tras cada accept manual o auto YOLO"
    - "El usuario puede hacer reset de una imagen y el conteo decrece por las entradas removidas"
    - "grep -nE 'save_training_sample\\(|remove_training_sample\\(' WatermarkRemove/ui/slideshow_viewer.py retorna 0 hits"
    - "SlideshowViewer composer tiene 20 o menos edges de dependencia directa segun graphify (ARCH-01 SC-4)"
    - "El comportamiento observable del visor es idéntico al estado pre-refactor (ARCH-01 SC-5)"
  artifacts:
    - path: "WatermarkRemove/ui/components/training_data_collector.py"
      provides: "TrainingDataCollector(QWidget) — UI de conteo + wrappers de save/remove training samples"
      contains: "class TrainingDataCollector(QWidget):"
      min_lines: 100
    - path: ".planning/phases/02-slideshowviewer-decomposition/02-EDGE-COUNT.md"
      provides: "Reporte de edge count post-refactor — verifica ARCH-01 SC-4"
      contains: "OUT edges del nodo class SlideshowViewer"
  key_links:
    - from: "WatermarkRemove/ui/components/training_data_collector.py"
      to: "WatermarkRemove.yolo.training_collector.save_training_sample"
      via: "from WatermarkRemove.yolo.training_collector import save_training_sample, remove_training_sample"
      pattern: "save_training_sample"
    - from: "WatermarkRemove/ui/slideshow_viewer.py"
      to: "self.processor.image_processed.connect(self.collector.on_image_processed)"
      via: "Composer wires processor.image_processed → collector.on_image_processed"
      pattern: "self\\.collector\\.on_image_processed"
    - from: "WatermarkRemove/ui/slideshow_viewer.py"
      to: "self.processor.image_reset.connect(self.collector.on_image_reset)"
      via: "Composer wires processor.image_reset → collector.on_image_reset"
      pattern: "self\\.collector\\.on_image_reset"
---

<objective>
Extraer la UI y logica de recopilacion de training data desde `SlideshowViewer` hacia `TrainingDataCollector(QWidget)`: el GroupBox "📊 Datos recopilados" con su label de conteo, y los slots `on_image_processed`/`on_image_reset` que envuelven `save_training_sample` / `remove_training_sample` del modulo `yolo/training_collector.py` (que NO se toca — out-of-scope per RESEARCH line 228).

Verificar el target arquitectonico ARCH-01 SC-4 (≤20 edges en SlideshowViewer composer) regenerando graphify y midiendo edges OUT del nodo de clase. Sign-off con UAT manual completo del Phase 2 (las 6 secciones del 02-HUMAN-UAT.md).

Purpose: ARCH-01 SC-3 — la recopilacion de training data opera desde su propio componente sin acoplar el visor. ARCH-01 SC-4 — verificar reduccion de edges. ARCH-01 SC-5 — comportamiento observable idéntico.

Output:
- `WatermarkRemove/ui/components/training_data_collector.py` completo
- `WatermarkRemove/ui/slideshow_viewer.py` adelgazado final: pure composer (~150-250 lineas)
- `.planning/phases/02-slideshowviewer-decomposition/02-EDGE-COUNT.md` con verificacion del target
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-slideshowviewer-decomposition/02-RESEARCH.md
@.planning/phases/02-slideshowviewer-decomposition/02-PATTERNS.md
@.planning/phases/02-slideshowviewer-decomposition/02-HUMAN-UAT.md
@.planning/phases/02-slideshowviewer-decomposition/02-01-PLAN.md
@.planning/phases/02-slideshowviewer-decomposition/02-02-PLAN.md
@WatermarkRemove/ui/slideshow_viewer.py
@WatermarkRemove/ui/components/watermark_processor.py
@WatermarkRemove/yolo/training_collector.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implementar TrainingDataCollector + extraer GroupBox de conteo desde SlideshowViewer</name>
  <files>
    WatermarkRemove/ui/components/training_data_collector.py,
    WatermarkRemove/ui/slideshow_viewer.py
  </files>
  <read_first>
    WatermarkRemove/ui/slideshow_viewer.py,
    WatermarkRemove/ui/components/watermark_processor.py,
    WatermarkRemove/yolo/training_collector.py,
    .planning/phases/02-slideshowviewer-decomposition/02-PATTERNS.md
  </read_first>
  <action>
    Implementar `TrainingDataCollector(QWidget)` extrayendo verbatim desde `WatermarkRemove/ui/slideshow_viewer.py`:
    - El GroupBox "📊 Datos recopilados" (lineas 480-493 — `conteo_group`, `conteo_layout`, `self.training_counts_label`)
    - El metodo `_update_counts_label` (lineas 115-139) — refresh del label leyendo `training_data.json`
    - Los slots para reaccionar a senales del processor:
      - `on_image_processed(file_path, working_image, x, y, watermark_array, watermark_path, watermark_folder_name, base_image)` — invoca `save_training_sample` (slideshow_viewer.py lineas 1538-1554 + 1853-1865 logica equivalente, wrappeada en un slot)
      - `on_image_reset(file_path)` — invoca `remove_training_sample` (slideshow_viewer.py lineas 1662-1663 logica)

    **Imports** (top de archivo):
    - `import os`, `from pathlib import Path`, `from typing import Optional`
    - `from PySide6.QtWidgets import QWidget, QVBoxLayout, QGroupBox, QLabel`
    - `from PySide6.QtCore import Signal, Qt`
    - `from WatermarkRemove.yolo.training_collector import save_training_sample, remove_training_sample` (Pitfall 4 — promover a top-level y documentar en docstring que originalmente eran lazy en slideshow_viewer.py lines 1540/1662/1854, decision por consistencia)

    **Path adjust** (PATTERNS line 648-656, idem que en otros componentes):
    - `current_dir = os.path.abspath(os.path.dirname(__file__))` → `WatermarkRemove/ui/components/`
    - `wm_dir = os.path.dirname(os.path.dirname(__file__))` → `WatermarkRemove/`
    - El path final `training_data.json` se construye como `Path(wm_dir) / 'training_data.json'` — reemplazar el original `Path(os.path.dirname(current_dir)) / 'training_data.json'` (slideshow_viewer.py lineas 119, 1542, 1855) por la version adaptada. **Documentar el cambio con un comentario en docstring**: "originalmente en slideshow_viewer.py current_dir era ui/, ahora components/ — un nivel mas profundo, usar dirname(dirname(__file__))".

    **Estado del componente**:
    - `self.watermark_tab = watermark_tab` (para _log fallback)
    - `self._training_json: Path = Path(wm_dir) / 'training_data.json'` — cache del path

    **Senales publicas**:
    - `counts_updated = Signal()` — opcional, emitida tras refresh del label (para depuracion)

    **UI** (en `_setup_ui`):
    Construir verbatim el bloque del slideshow_viewer.py lineas 480-493:
    ```
    main_layout = QVBoxLayout(self)
    main_layout.setSpacing(0)
    main_layout.setContentsMargins(0, 0, 0, 0)

    conteo_group = QGroupBox("📊 Datos recopilados")
    conteo_layout = QVBoxLayout(conteo_group)
    conteo_layout.setSpacing(4)
    conteo_layout.setContentsMargins(8, 6, 8, 6)

    self.training_counts_label = QLabel("Sin datos aún")
    self.training_counts_label.setStyleSheet(
        "color: #aaaaaa; font-size: 10px; font-family: monospace;"
    )
    self.training_counts_label.setWordWrap(True)
    conteo_layout.addWidget(self.training_counts_label)

    main_layout.addWidget(conteo_group)
    ```
    Llamar `self._update_counts_label()` al final de __init__ (replica linea 493).

    **Metodos**:
    - `_log(self, message)` — replica patron 141-149 de slideshow_viewer.py
    - `_update_counts_label(self)` — extraido verbatim de lineas 115-139, ajustando el path al `self._training_json` cacheado. NO modificar la logica interna (defensive try/except, fallback "Sin datos aún" preservados — RESEARCH Pitfall 3).
    - `on_image_processed(self, file_path, working_image, x, y, watermark_array, watermark_path, watermark_folder_name, base_image)` — slot, replica el bloque try/except de slideshow_viewer.py 1538-1554:
      ```
      try:
          if watermark_path is None:
              # caso auto-detection con mark sin PNG (linea 1842 del original)
              return
          save_training_sample(
              image_path=file_path,
              watermark_path=watermark_path,
              watermark_folder=watermark_folder_name,
              x=x, y=y,
              watermark_array=watermark_array,
              image_array=base_image,
              output_json=self._training_json,
          )
      except Exception as collect_err:
          self._log(f"⚠️ No se pudo guardar dato de entrenamiento: {collect_err}")
      finally:
          self._update_counts_label()
      ```
    - `on_image_reset(self, file_path)` — slot, replica logica de lineas 1661-1668 + 1663 del original:
      ```
      try:
          # Replica de remove_training_sample(current_dir, current_file, log) usando el nuevo path
          # NOTA: la firma original espera current_dir str (no Path) para construir training_data.json path —
          # aqui pasamos self._training_json.parent como current_dir-equivalent, o refactorizamos
          # llamando a remove_training_sample con (str(wm_dir_components), file_path, self._log) — wm_dir es
          # WatermarkRemove/, igual que antes
          import os as _os
          wm_dir_str = _os.path.dirname(_os.path.dirname(__file__))  # WatermarkRemove/
          # remove_training_sample espera current_dir str que es WatermarkRemove/ui (un nivel por encima del archivo original)
          # En el nuevo path, training_data.json esta directamente en WatermarkRemove/, asi que pasar wm_dir_str + '/ui' replica el calculo `os.path.dirname(current_dir)` del original (current_dir era WatermarkRemove/ui)
          fake_current_dir = _os.path.join(wm_dir_str, 'ui')
          remove_training_sample(fake_current_dir, file_path, self._log)
      except Exception as e:
          self._log(f"⚠️ Error en remove_training_sample: {e}")
      finally:
          self._update_counts_label()
      ```
      **NOTA CRITICA**: la firma de `remove_training_sample(current_dir:str, current_file:Path, log)` espera `current_dir` que era `WatermarkRemove/ui/` (el `__file__` del slideshow_viewer.py original). Adentro hace `Path(os.path.dirname(current_dir)) / 'training_data.json'` que apunta a `WatermarkRemove/training_data.json`. Para que el comportamiento sea identico sin modificar `training_collector.py`, pasamos `fake_current_dir = wm_dir_str + '/ui'` (un nivel por encima de `WatermarkRemove/`, lo cual hace que `dirname()` retorne `WatermarkRemove/`). Esto preserva el comportamiento sin tocar `yolo/training_collector.py` (out-of-scope per RESEARCH line 228).

      **Alternativa preferida**: refactorizar para pasar el path directo si la firma de `remove_training_sample` lo permite. Verificar — la firma actual no acepta un path directo, asi que la solucion fake_current_dir es la correcta sin modificar el modulo externo.

    **min_lines target:** ~100-160 lineas (componente delgado).
  </action>
  <verify>
    <automated>python -m py_compile WatermarkRemove/ui/components/training_data_collector.py</automated>
    <automated>python -c "from WatermarkRemove.ui.components import TrainingDataCollector; print(TrainingDataCollector.counts_updated)"</automated>
    <automated>grep -cE "def (on_image_processed|on_image_reset|_update_counts_label)" WatermarkRemove/ui/components/training_data_collector.py</automated>
    <automated>grep -c "from WatermarkRemove.yolo.training_collector import save_training_sample, remove_training_sample" WatermarkRemove/ui/components/training_data_collector.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class TrainingDataCollector(QWidget):" WatermarkRemove/ui/components/training_data_collector.py` retorna 1
    - `grep -cE "def (on_image_processed|on_image_reset|_update_counts_label|_log)" WatermarkRemove/ui/components/training_data_collector.py` >= 4
    - `grep -c "from WatermarkRemove.yolo.training_collector import save_training_sample" WatermarkRemove/ui/components/training_data_collector.py` retorna 1
    - `grep -c "remove_training_sample" WatermarkRemove/ui/components/training_data_collector.py` >= 2 (import + uso)
    - `grep -c "self.training_counts_label" WatermarkRemove/ui/components/training_data_collector.py` >= 2
    - `grep -c "training_data.json" WatermarkRemove/ui/components/training_data_collector.py` >= 1
    - `wc -l WatermarkRemove/ui/components/training_data_collector.py` >= 100
    - `python -m py_compile WatermarkRemove/ui/components/training_data_collector.py` sale 0
    - `python -c "from WatermarkRemove.ui.components import TrainingDataCollector"` sale 0
  </acceptance_criteria>
  <done>
    TrainingDataCollector(QWidget) implementado y autocontenido — UI propia (GroupBox conteo + label), slots para wiring (on_image_processed, on_image_reset), wrappers de save_training_sample/remove_training_sample preservando el comportamiento del modulo externo (yolo/training_collector.py no modificado).
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactorizar SlideshowViewer final — eliminar conteo inline, wire collector signals, commit composer puro</name>
  <files>
    WatermarkRemove/ui/slideshow_viewer.py
  </files>
  <read_first>
    WatermarkRemove/ui/slideshow_viewer.py,
    WatermarkRemove/ui/components/training_data_collector.py,
    WatermarkRemove/ui/components/watermark_processor.py,
    WatermarkRemove/ui/components/navigation_controller.py,
    gui/controller.py
  </read_first>
  <action>
    Eliminar de `WatermarkRemove/ui/slideshow_viewer.py` lo que queda de training data inline, agregar el wiring del collector, y limpiar el composer final.

    **Cambios concretos:**

    1. **Eliminar el metodo `_update_counts_label`** (lineas 115-139 del original — todavia presente despues del Plan 02 porque conservaba inline) — vive en `self.collector`.

    2. **Eliminar el `import json as _json`** lazy en `_update_counts_label` (era local al metodo) — ya no se usa.

    3. **Eliminar el GroupBox "Datos recopilados"** del `_create_controls_panel` — el conteo es ahora `self.collector` (un QWidget) que se agrega al layout izquierdo via `addWidget(self.collector)`.

    4. **Wire signals adicionales (en `__init__` despues del wiring del Plan 02)**:
       ```
       # Processor → Collector (cada accept envia datos al collector)
       self.processor.image_processed.connect(self.collector.on_image_processed)
       self.processor.image_reset.connect(self.collector.on_image_reset)
       ```

    5. **Layout final del `_create_controls_panel`** — el panel izquierdo es ahora un QVBoxLayout que contiene (en orden):
       - El widget `self.processor` (con sus GroupBoxes internos: auto checkbox, seleccion, auto-detection)
       - Un QGroupBox "✳️ Navegación" reducido con SOLO finish_btn + cancel_btn (lineas 446-477 del original, sin prev_btn/next_btn que ya estan en navigation)
       - El widget `self.collector` (con su GroupBox conteo)
       - `layout.addStretch(1)` al final

    6. **Estado final del `__init__` del SlideshowViewer**:
       ```
       def __init__(self, folder_path: str, parent=None, watermark_tab=None):
           super().__init__(parent)
           self.user_approved = False
           self.watermark_tab = watermark_tab  # mantenido solo para _log local en finish/cancel

           # Construir hijos
           self.navigation = NavigationController(folder_path, parent=self, watermark_tab=watermark_tab)
           self.processor = WatermarkProcessor(parent=self, watermark_tab=watermark_tab)
           self.collector = TrainingDataCollector(parent=self, watermark_tab=watermark_tab)

           # Wire signals (Plan 02 + Plan 03)
           self._wire_signals()

           # Setup UI (composer arma layout)
           self._setup_ui()
       ```

    7. **Crear metodo `_wire_signals(self)`** — extraer todo el wiring a un metodo dedicado:
       ```
       def _wire_signals(self):
           # Navigation → Processor
           self.navigation.image_changed.connect(self.processor.on_image_changed)
           self.navigation.output_folder_ready.connect(self.processor.on_output_folder_ready)
           self.navigation.image_clicked.connect(self.processor.on_image_clicked)
           self.navigation.mouse_moved.connect(self.processor.on_mouse_moved)

           # Processor → Navigation
           self.processor.preview_changed.connect(self.navigation.on_preview_changed)
           self.processor.image_processed.connect(self.navigation.on_image_processed)
           self.processor.processing_blocked.connect(lambda b: self.navigation.set_navigation_enabled(not b))
           self.processor.image_reset.connect(self.navigation.reset_current_image)
           self.processor.request_redraw.connect(self.navigation._apply_zoom)
           # output_folder_request ya conectado en Plan 02 Task 3 — no re-conectar aquí (evitar double-connection)
           self.processor.manual_tracking_requested.connect(self.navigation.set_mouse_tracking)
           self.processor.manual_overlay_visibility.connect(self.navigation.set_manual_overlay_visible)
           self.processor.manual_overlay_geometry.connect(self.navigation.set_manual_overlay_geometry)
           self.navigation.set_processor_decorator(self.processor.decorate_pixmap)

           # Processor → Collector
           self.processor.image_processed.connect(self.collector.on_image_processed)
           self.processor.image_reset.connect(self.collector.on_image_reset)

           # Navigation → SlideshowViewer (composer)
           self.navigation.window_resize_requested.connect(self.resize)
           self.navigation.finish_requested.connect(self._finish_review)
       ```

    8. **Eliminar `current_dir` global** (lineas 17-20) — ya no se usa (todos los componentes calculan su propio path).

    9. **Eliminar imports muertos** del top de archivo:
       - `from utils import UtilJson` — solo se usaba en `_load_watermark_positions` (migrado en Plan 02)
       - `import numpy as np` — solo se usaba en metodos migrados
       - `from natsort import natsorted` — solo se usaba en `_load_image_list` y `_load_watermark_folders` (migrados)
       - `from WatermarkRemove.services import wm_persistence` — solo se usaba en metodos migrados a processor/navigation
       - `from typing import Tuple, Optional` — si ya no se anota nada en este archivo, eliminar
       - Tantos QtWidgets imports como no se usen mas (solo conservar los que se usan en el composer reducido: `QDialog, QHBoxLayout, QVBoxLayout, QGroupBox, QPushButton, QMessageBox`)
       - QtCore: `Signal` (para `review_completed`), `Qt` (para Qt.Key.* en keyPressEvent)
       - QtGui: `QKeyEvent`, `QWheelEvent` (si todavia se usa wheelEvent — lo delega a navigation, asi que se puede eliminar)
       - **Verificar con grep**: para cada import, grep el simbolo. Si retorna 0 hits fuera de la linea de import, eliminar.

    10. **Conservar en el archivo final**:
        - docstring del modulo + class
        - imports minimos
        - `class SlideshowViewer(QDialog):` declaration
        - `review_completed = Signal(bool)` class attribute
        - `__init__` (con instanciacion + _wire_signals + _setup_ui)
        - `_wire_signals` (metodo nuevo, extraido del init)
        - `_setup_ui` (reducido — solo HBoxLayout + addWidget(nav) + addWidget(processor) + addWidget(collector) + finish/cancel buttons group)
        - `_create_controls_panel` (reducido a panel izquierdo con processor + collector + nav buttons group)
        - `_log` (usado por `_finish_review`/`_cancel_review`)
        - `keyPressEvent` (del Plan 02 — chequea processor.is_preview_active y delega a navigation)
        - `_finish_review`, `_cancel_review`
        - API publica: `get_approved`, `get_output_folder`, `has_processed_images`
        - `if __name__ == "__main__":` test entry point (verbatim)

    11. **Verificacion final**:
        - El archivo `slideshow_viewer.py` debe tener entre 150 y 280 lineas (vs 2040 originales — reduccion >85%).
        - `git diff gui/controller.py` debe estar vacio.
  </action>
  <verify>
    <automated>python -m py_compile WatermarkRemove/ui/slideshow_viewer.py WatermarkRemove/ui/components/*.py</automated>
    <automated>python -c "from WatermarkRemove.ui import SlideshowViewer; print('OK')"</automated>
    <automated>grep -cE "save_training_sample|remove_training_sample|_update_counts_label" WatermarkRemove/ui/slideshow_viewer.py</automated>
    <automated>grep -c "self.collector.on_image_processed" WatermarkRemove/ui/slideshow_viewer.py</automated>
    <automated>grep -c "self.collector.on_image_reset" WatermarkRemove/ui/slideshow_viewer.py</automated>
    <automated>wc -l WatermarkRemove/ui/slideshow_viewer.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "save_training_sample\(|remove_training_sample\(|def _update_counts_label" WatermarkRemove/ui/slideshow_viewer.py` retorna 0
    - `grep -c "self.collector.on_image_processed" WatermarkRemove/ui/slideshow_viewer.py` retorna 1
    - `grep -c "self.collector.on_image_reset" WatermarkRemove/ui/slideshow_viewer.py` retorna 1
    - `grep -c "self.collector = TrainingDataCollector" WatermarkRemove/ui/slideshow_viewer.py` retorna 1
    - `grep -c "def _wire_signals" WatermarkRemove/ui/slideshow_viewer.py` retorna 1
    - `grep -c "class SlideshowViewer(QDialog):" WatermarkRemove/ui/slideshow_viewer.py` retorna 1
    - `grep -c "review_completed = Signal(bool)" WatermarkRemove/ui/slideshow_viewer.py` retorna 1
    - `grep -c "def __init__(self, folder_path: str, parent=None, watermark_tab=None)" WatermarkRemove/ui/slideshow_viewer.py` retorna 1
    - `grep -cE "def (get_approved|get_output_folder|has_processed_images)" WatermarkRemove/ui/slideshow_viewer.py` retorna 3
    - `grep -cE "(from utils import UtilJson|import numpy as np|from natsort import natsorted|from WatermarkRemove.services import wm_persistence)" WatermarkRemove/ui/slideshow_viewer.py` retorna 0 (imports muertos eliminados)
    - `grep -c "current_dir = os" WatermarkRemove/ui/slideshow_viewer.py` retorna 0 (global eliminado)
    - `wc -l WatermarkRemove/ui/slideshow_viewer.py` <= 280
    - `python -m py_compile WatermarkRemove/ui/slideshow_viewer.py WatermarkRemove/ui/components/*.py` sale 0
    - `python -c "from WatermarkRemove.ui import SlideshowViewer; v = SlideshowViewer.__init__; import inspect; sig = inspect.signature(v); assert list(sig.parameters.keys()) == ['self', 'folder_path', 'parent', 'watermark_tab'], f'Signature mismatch: {sig}'; print('signature OK')"` sale 0
    - `git diff gui/controller.py` no muestra cambios
  </acceptance_criteria>
  <done>
    SlideshowViewer es un composer puro: ~150-280 lineas, sin metodos de processing ni training inline. Instancia los 3 componentes, ejecuta `_wire_signals`, arma layout, conserva keyPressEvent guard, expone API publica intacta. El visor funciona end-to-end con todas las funcionalidades (navegacion, manual, auto YOLO, posiciones rojo/verde, crop, reset, training counts, finish/cancel). `gui/controller.py:321` funciona sin modificar.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: UAT manual completo + verificacion edge count (ARCH-01 SC-4 + SC-5)</name>
  <files>
    .planning/phases/02-slideshowviewer-decomposition/02-HUMAN-UAT.md,
    .planning/phases/02-slideshowviewer-decomposition/02-EDGE-COUNT.md
  </files>
  <read_first>
    .planning/phases/02-slideshowviewer-decomposition/02-HUMAN-UAT.md,
    WatermarkRemove/ui/slideshow_viewer.py,
    WatermarkRemove/ui/components/navigation_controller.py,
    WatermarkRemove/ui/components/watermark_processor.py,
    WatermarkRemove/ui/components/training_data_collector.py
  </read_first>
  <what-built>
    Phase 2 completa: SlideshowViewer descompuesto en 3 componentes (NavigationController, WatermarkProcessor, TrainingDataCollector) bajo `WatermarkRemove/ui/components/`. SlideshowViewer reducido de 2040 lineas a ~150-280 lineas (composer puro). API publica preservada (`__init__(folder_path, parent, watermark_tab)`, `review_completed`, `get_approved`, `get_output_folder`, `has_processed_images`). Modulo `yolo/training_collector.py` y `wm_remove.py` no modificados (out-of-scope per REQUIREMENTS).
  </what-built>
  <how-to-verify>
    **A. UAT manual (las 6 secciones de `02-HUMAN-UAT.md`)** — el usuario debe ejecutar:

    1. Limpiar caches: `find WatermarkRemove -name __pycache__ -exec rm -rf {} +` (RESEARCH Runtime State Inventory)
    2. Smoke: `python -m py_compile WatermarkRemove/ui/slideshow_viewer.py WatermarkRemove/ui/components/*.py` → sale 0
    3. End-to-end: `python SmartStitchGUI.py`:
       - Activar "Ejecutar Quita Marcas", click "Iniciar", seleccionar carpeta de test con 3+ imagenes
       - Ejecutar las 6 secciones del 02-HUMAN-UAT.md una por una (Navegación → Modo Manual → Modo Auto YOLO → Training Data → Edge Count → Comportamiento Idéntico)
       - Marcar checkboxes en el archivo a medida que se confirma cada item
       - **CRITICO**: que los cuadros rojo/verde de posiciones se pinten (regresion del Plan 01 restaurada en Plan 02-02), que el manual mode + auto YOLO + reset todos funcionen como antes, y que el conteo de training samples se actualice tras cada accept.

    **B. Edge count verification (ARCH-01 SC-4)** — el usuario debe ejecutar:

    1. Regenerar grafo: `node bin/graphify.js` (o el script equivalente — verificar nombre en el repo)
    2. Abrir `graphify-out/graph.json` y buscar nodo cuyo id contenga `slideshow_viewer` y class `SlideshowViewer`
    3. Contar OUT edges (referencias salientes) del nodo del **class** (no del file completo). Comando alternativo:
       ```
       node -e "const g = require('./graphify-out/graph.json'); const node = g.nodes.find(n => /slideshow_viewer.*slideshowviewer/i.test(n.id)); const out = g.edges.filter(e => e.from === node.id); console.log('OUT edges:', out.length); console.log(out.map(e => e.to).join('\n'));"
       ```
    4. Crear `.planning/phases/02-slideshowviewer-decomposition/02-EDGE-COUNT.md` con:
       - Pre-refactor baseline: 58 OUT edges (segun RESEARCH line 622)
       - Post-refactor count: N (tu medicion)
       - Lista de los N edges restantes con clasificacion (Qt base, hijos componentes, API publica, otros)
       - Veredicto: PASS si ≤20, FAIL si >20

    **C. Verificacion contract con gui/controller.py**:
    - `git diff gui/controller.py` debe estar vacio
    - Cargar SmartStitchGUI.py y confirmar que la opcion "Ejecutar Quita Marcas" abre el visor y la carpeta de salida `[sin marca]` se propaga a `inputField` correctamente

    **D. Verificacion grep gates (todos esperados 0 hits)**:
    ```
    grep -nE "remove_watermark\(|detect_watermarks\(|find_wm\(|quick_align_preview\(|resolve_png_for_class\(|align_watermark\(|save_training_sample\(|remove_training_sample\(" WatermarkRemove/ui/slideshow_viewer.py
    ```
    Debe retornar 0 hits.

    **E. Confirmar archivo final**: pegar el output de `wc -l WatermarkRemove/ui/slideshow_viewer.py WatermarkRemove/ui/components/*.py` en el comentario del approve.

    **F. Si TODO esta verde**: aprobar con "approved + edge count: N" donde N es el numero medido. Si edge count > 20 pero comportamiento OK: aprobar con nota "edge count: N (excede target, abrir issue de extraccion adicional fuera del scope de esta phase)".

    **G. Si hay cualquier regresion funcional o falla de UAT**: describir el issue exacto (que paso clickeaste, que esperabas, que sucedio).
  </how-to-verify>
  <resume-signal>
    Type "approved + edge count: N + wc -l: [output]" si todo paso. O describir issue para que el siguiente plan de gap closure pueda abrirse.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TrainingDataCollector → training_collector.save_training_sample | numpy arrays + paths; mitigaciones existentes en training_collector.py heredadas |
| TrainingDataCollector → training_collector.remove_training_sample | path operations defensive — el modulo externo (out-of-scope) ya maneja exceptions |
| TrainingDataCollector.image_to_base64 (via save_training_sample) | lee bytes del archivo de imagen original; mitigacion existente (base64 encoding, no eval) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03-01 | Tampering | TrainingDataCollector._update_counts_label (JSON read) | accept | Defensive try/except preservado verbatim del original (slideshow_viewer.py lines 120-139); fallback a "Sin datos aún". |
| T-02-03-02 | Tampering | TrainingDataCollector.on_image_processed (calls save_training_sample) | accept | Mitigacion existente en training_collector.py — class filter (`is_trainable_class`), defensive append, atomic write. Sin cambio. |
| T-02-03-03 | Tampering | TrainingDataCollector.on_image_reset (calls remove_training_sample with fake_current_dir) | accept | `fake_current_dir` construido a partir de `__file__` (controlado por el codigo, no user input); el modulo externo hace defensive JSON parse. |
| T-02-03-04 | Information Disclosure | training_data.json (contiene base64 de imagenes) | accept | Archivo local, gestionado por el usuario (RESEARCH out-of-scope — formato pre-existente). |
| T-02-03-SC | Tampering | npm/pip/cargo installs | N/A | No se instalan paquetes (refactor 100% estructural — RESEARCH N/A). |

Conclusion: ningun threat nuevo. Mitigantes heredados al wrappear funciones de `yolo/training_collector.py` sin modificarlas.
</threat_model>

<verification>
- `python -m py_compile WatermarkRemove/ui/slideshow_viewer.py WatermarkRemove/ui/components/*.py` sale 0
- `grep -nE "remove_watermark\(|detect_watermarks\(|find_wm\(|quick_align_preview\(|resolve_png_for_class\(|align_watermark\(|save_training_sample\(|remove_training_sample\(" WatermarkRemove/ui/slideshow_viewer.py` retorna 0 hits
- `wc -l WatermarkRemove/ui/slideshow_viewer.py` <= 280
- `git diff gui/controller.py` vacio
- 02-HUMAN-UAT.md las 6 secciones completadas (checkpoint Task 3)
- 02-EDGE-COUNT.md existe con verdict de ARCH-01 SC-4 (checkpoint Task 3)
</verification>

<success_criteria>
- ARCH-01 SC-1: Space/Backspace navegan sin logica inline (verificado en Plan 01 + UAT Seccion 1)
- ARCH-01 SC-2: YOLO + remove_watermark viven en WatermarkProcessor (verificado en Plan 02 + UAT Secciones 2, 3 + grep)
- ARCH-01 SC-3: training data collection vive en TrainingDataCollector (verificado en Plan 03 Task 1 + UAT Seccion 4 + grep)
- ARCH-01 SC-4: SlideshowViewer ≤20 edges (verificado en Plan 03 Task 3 + 02-EDGE-COUNT.md)
- ARCH-01 SC-5: comportamiento observable identico (verificado en UAT Seccion 6 + Task 3 sign-off)
- yolo/training_collector.py y wm_remove.py NO modificados (RESEARCH out-of-scope)
- gui/controller.py NO modificado (contract preservado)
- __pycache__ limpiado para evitar confusion en build artifacts (RESEARCH Runtime State Inventory)
</success_criteria>

<output>
Al completar este plan, crear:
1. `.planning/phases/02-slideshowviewer-decomposition/02-03-SUMMARY.md` siguiendo `$HOME/.claude/get-shit-done/templates/summary.md`. Incluir:
   - Confirmacion de extraccion completa de training data collection
   - Final wc -l de los 4 archivos (slideshow_viewer.py + 3 components)
   - Confirmacion de wiring de las 3 componentes en `_wire_signals`
   - Resultado del checkpoint UAT manual (las 6 secciones)
   - Numero final de edges del SlideshowViewer composer
   - Lista de regresion observada (si alguna) o "Comportamiento observable idéntico al pre-refactor"
2. `.planning/phases/02-slideshowviewer-decomposition/02-EDGE-COUNT.md` (creado durante Task 3) con el verdict ARCH-01 SC-4
</output>
