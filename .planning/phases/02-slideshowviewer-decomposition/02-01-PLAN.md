---
phase: 02-slideshowviewer-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WatermarkRemove/ui/components/__init__.py
  - WatermarkRemove/ui/components/navigation_controller.py
  - WatermarkRemove/ui/components/watermark_processor.py
  - WatermarkRemove/ui/components/training_data_collector.py
  - WatermarkRemove/ui/slideshow_viewer.py
autonomous: true
requirements:
  - ARCH-01
must_haves:
  truths:
    - "El paquete WatermarkRemove/ui/components/ existe con __init__.py exportando los 3 componentes"
    - "NavigationController(QWidget) en WatermarkRemove/ui/components/navigation_controller.py gestiona image_files, current_index, working_image, output_folder, zoom_level"
    - "El usuario puede presionar Space/Backspace en SlideshowViewer y navegar imágenes — la navegación funciona idénticamente al estado anterior"
    - "SlideshowViewer ya no contiene los métodos _load_image_list, _show_current_image, _create_output_folder, _next_image, _previous_image, _save_current_image_as_is, _update_counter, _set_zoom, _apply_zoom, _adjust_window_size, _clear_image_memory, _show_zoom_overlay, _hide_zoom_overlay — están en NavigationController"
    - "gui/controller.py:321 sigue funcionando sin cambios: SlideshowViewer(input_path, MainWindow, watermark_tab=watermark_tab) construye el visor y exec()/get_approved()/get_output_folder()/has_processed_images() responden igual"
  artifacts:
    - path: "WatermarkRemove/ui/components/__init__.py"
      provides: "Barrel export de NavigationController, WatermarkProcessor, TrainingDataCollector"
      contains: "from .navigation_controller import NavigationController"
    - path: "WatermarkRemove/ui/components/navigation_controller.py"
      provides: "NavigationController(QWidget) con lista de imagenes, working_image, zoom, render"
      contains: "class NavigationController(QWidget):"
      min_lines: 250
    - path: "WatermarkRemove/ui/components/watermark_processor.py"
      provides: "Stub WatermarkProcessor(QWidget) para Plan 02"
      contains: "class WatermarkProcessor(QWidget):"
    - path: "WatermarkRemove/ui/components/training_data_collector.py"
      provides: "Stub TrainingDataCollector(QWidget) para Plan 03"
      contains: "class TrainingDataCollector(QWidget):"
    - path: ".planning/phases/02-slideshowviewer-decomposition/02-HUMAN-UAT.md"
      provides: "Checklist UAT manual (creado en Wave 0)"
      contains: "## Sección 1 — Navegación"
  key_links:
    - from: "WatermarkRemove/ui/slideshow_viewer.py"
      to: "WatermarkRemove/ui/components/navigation_controller.py"
      via: "from WatermarkRemove.ui.components import NavigationController"
      pattern: "from WatermarkRemove.ui.components import"
    - from: "WatermarkRemove/ui/slideshow_viewer.py"
      to: "self.navigation.image_changed"
      via: "Signal/slot — self.navigation = NavigationController(folder_path, ...); self.navigation.image_changed.connect(...)"
      pattern: "self\\.navigation\\."
    - from: "WatermarkRemove/ui/components/navigation_controller.py"
      to: "WatermarkRemove.wm_remove.load_images_cv2"
      via: "from WatermarkRemove.wm_remove import load_images_cv2, guardar"
      pattern: "load_images_cv2"
---

<objective>
Crear el paquete `WatermarkRemove/ui/components/` con stubs para los 3 componentes, extraer la responsabilidad de **navegación** (lista de imágenes, índice actual, working_image, output_folder, zoom y render) desde `SlideshowViewer` hacia `NavigationController`, y refactorizar `slideshow_viewer.py` para delegar navegación al nuevo componente vía señales/slots.

Purpose: ARCH-01 SC-1 — el visor navega con Space/Backspace sin que el widget tenga lógica de navegación/render inline. Establece el patrón composer + componentes con Signal/Slot que los Plans 02 y 03 usarán.

Output:
- `WatermarkRemove/ui/components/__init__.py` (barrel export — analog: `WatermarkRemove/services/__init__.py`)
- `WatermarkRemove/ui/components/navigation_controller.py` (NavigationController completo)
- `WatermarkRemove/ui/components/watermark_processor.py` (stub mínimo)
- `WatermarkRemove/ui/components/training_data_collector.py` (stub mínimo)
- `WatermarkRemove/ui/slideshow_viewer.py` modificado (instancia NavigationController, delega navegación; conserva manual+auto+training inline para Plans 02/03)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-slideshowviewer-decomposition/02-RESEARCH.md
@.planning/phases/02-slideshowviewer-decomposition/02-PATTERNS.md
@.planning/phases/02-slideshowviewer-decomposition/02-HUMAN-UAT.md
@.planning/phases/01-json-persistence/01-02-SUMMARY.md
@WatermarkRemove/ui/slideshow_viewer.py
@WatermarkRemove/ui/image_viewer.py
@WatermarkRemove/services/__init__.py
@WatermarkRemove/services/wm_persistence.py
@gui/controller.py
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear paquete components/ con stubs mínimos y export barrel</name>
  <files>
    WatermarkRemove/ui/components/__init__.py,
    WatermarkRemove/ui/components/navigation_controller.py,
    WatermarkRemove/ui/components/watermark_processor.py,
    WatermarkRemove/ui/components/training_data_collector.py
  </files>
  <read_first>
    WatermarkRemove/services/__init__.py,
    WatermarkRemove/services/wm_persistence.py,
    WatermarkRemove/ui/image_viewer.py,
    .planning/phases/02-slideshowviewer-decomposition/02-PATTERNS.md
  </read_first>
  <action>
    Crear el paquete nuevo `WatermarkRemove/ui/components/` siguiendo el patrón barrel-export del PATTERNS analog `WatermarkRemove/services/__init__.py` (1-5).

    `__init__.py`: docstring de paquete, luego en orden — `from .navigation_controller import NavigationController`, `from .watermark_processor import WatermarkProcessor`, `from .training_data_collector import TrainingDataCollector`, y `__all__ = ['NavigationController', 'WatermarkProcessor', 'TrainingDataCollector']`. NO instanciar singletons (no es el patrón aquí — los widgets se construyen por-diálogo).

    `navigation_controller.py` (stub minimo, se completa en Task 2): docstring + imports de PySide6.QtWidgets.QWidget + PySide6.QtCore.Signal; declarar `class NavigationController(QWidget): pass` con `def __init__(self, folder_path: str, parent=None, watermark_tab=None): super().__init__(parent)`.

    `watermark_processor.py` (stub para Plan 02): igual estructura — `class WatermarkProcessor(QWidget): pass` con `def __init__(self, parent=None, watermark_tab=None): super().__init__(parent)`. Docstring: "Placeholder — implementacion en Plan 02-02".

    `training_data_collector.py` (stub para Plan 03): `class TrainingDataCollector(QWidget): pass` con `def __init__(self, parent=None, watermark_tab=None): super().__init__(parent)`. Docstring: "Placeholder — implementacion en Plan 02-03".

    Documentar en cada archivo el patron `current_dir = os.path.dirname(os.path.dirname(__file__))` cuando se necesite reach a `WatermarkRemove/` (PATTERNS line 648-656) — pero NO ejecutar la modificacion de sys.path en los stubs (no la necesitan todavia). Dejar comentario `# NOTA: components/ esta dos niveles bajo el package root — usar os.path.dirname(os.path.dirname(__file__)) para alcanzar WatermarkRemove/`.

    NO tocar `WatermarkRemove/ui/__init__.py` (no se exporta nada de components/ desde alli — el composer importa directamente desde `WatermarkRemove.ui.components`).
  </action>
  <verify>
    <automated>
      python -m py_compile WatermarkRemove/ui/components/__init__.py WatermarkRemove/ui/components/navigation_controller.py WatermarkRemove/ui/components/watermark_processor.py WatermarkRemove/ui/components/training_data_collector.py
    </automated>
    <automated>
      python -c "from WatermarkRemove.ui.components import NavigationController, WatermarkProcessor, TrainingDataCollector; print('OK')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `WatermarkRemove/ui/components/__init__.py` existe y contiene las 3 lineas `from .navigation_controller import NavigationController`, `from .watermark_processor import WatermarkProcessor`, `from .training_data_collector import TrainingDataCollector`
    - `WatermarkRemove/ui/components/__init__.py` contiene `__all__ = ['NavigationController', 'WatermarkProcessor', 'TrainingDataCollector']`
    - `grep -c "class NavigationController(QWidget):" WatermarkRemove/ui/components/navigation_controller.py` retorna 1
    - `grep -c "class WatermarkProcessor(QWidget):" WatermarkRemove/ui/components/watermark_processor.py` retorna 1
    - `grep -c "class TrainingDataCollector(QWidget):" WatermarkRemove/ui/components/training_data_collector.py` retorna 1
    - `python -c "from WatermarkRemove.ui.components import NavigationController, WatermarkProcessor, TrainingDataCollector"` sale con codigo 0
    - El comando py_compile sale sin errores
  </acceptance_criteria>
  <done>
    El paquete components/ existe con su barrel-export funcional; los 3 stubs son importables y heredan de QWidget; los Plans 02 y 03 tendran archivos sobre los que iterar.
    Actualizar `.planning/phases/02-slideshowviewer-decomposition/02-VALIDATION.md` frontmatter: `wave_0_complete: true` y `nyquist_compliant: true` (los 5 stubs de Wave 0 estan creados inline en esta tarea).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implementar NavigationController extrayendo navegacion + render desde SlideshowViewer</name>
  <files>
    WatermarkRemove/ui/components/navigation_controller.py
  </files>
  <read_first>
    WatermarkRemove/ui/slideshow_viewer.py,
    WatermarkRemove/ui/image_viewer.py,
    WatermarkRemove/services/__init__.py,
    .planning/phases/02-slideshowviewer-decomposition/02-PATTERNS.md,
    .planning/phases/02-slideshowviewer-decomposition/02-RESEARCH.md
  </read_first>
  <action>
    Implementar `NavigationController(QWidget)` extrayendo verbatim los siguientes metodos y estado desde `WatermarkRemove/ui/slideshow_viewer.py`. NO inventar logica nueva — copy-extract con cambios minimos para que cada referencia a `self.<x>` que vivia en `SlideshowViewer` ahora viva en `NavigationController`.

    **Estado del componente** (__init__ de NavigationController, espejando slideshow_viewer.py lineas 57-64, 76-79, 103-107):
    - `self.folder_path = Path(folder_path) if folder_path else None`
    - `self.image_files: list = []`
    - `self.current_index: int = 0`
    - `self.current_pixmap = None`
    - `self.zoom_level: int = 100`
    - `self.controls_panel_width = 280`
    - `self.watermark_tab = watermark_tab` (recibido en __init__)
    - `self.output_folder: Optional[Path] = None`
    - `self.processed_images: set = set()`
    - `self.processed_positions: dict = {}`
    - `self.working_image: Optional[np.ndarray] = None`
    - `self.base_image_for_preview: Optional[np.ndarray] = None` (sera leido por el processor en Plan 02 — exposed via property/getter)

    **Constante de clase** (espejo de slideshow_viewer.py linea 53):
    - `SUPPORTED_FORMATS = ('.png', '.jpg', '.jpeg', '.webp', '.bmp', '.tiff', '.tga', '.psd', '.psb', '.jfif')`

    **Senales publicas** (declarar al inicio de la clase, con comentarios de payload — patron PATTERNS lines 99-107):
    - `image_changed = Signal(int, object, object)` — comentario: `(index, Path, np.ndarray working_image)`
    - `output_folder_ready = Signal(object)` — comentario: `Path`
    - `request_redraw = Signal()` — comentario: emitida cuando el composer/processor debe re-renderizar (placeholder para Plan 02)

    **Imports** (top de archivo, antes de la clase):
    - `import os`, `import sys`, `from pathlib import Path`, `from typing import Optional, Tuple`
    - `from PySide6.QtWidgets import QWidget, QVBoxLayout, QHBoxLayout, QLabel, QPushButton, QScrollArea, QGroupBox, QSpinBox, QCheckBox`
    - `from PySide6.QtCore import Qt, Signal, QTimer`
    - `from PySide6.QtGui import QPixmap, QImage, QPainter, QPen, QColor, QWheelEvent`
    - `import numpy as np`
    - `from natsort import natsorted`
    - `from WatermarkRemove.wm_remove import load_images_cv2, guardar`
    - `from WatermarkRemove.services import wm_persistence`
    - Path adjust: `current_dir = os.path.abspath(os.path.dirname(__file__))`; `wm_dir_root = os.path.dirname(os.path.dirname(__file__))` con comentario "components/ esta dos niveles bajo el package root (PATTERNS line 648-656)"

    **Metodos a extraer (copy-extract verbatim, replace `self.image_label` con `self.image_label` interno + adjust `_log` fallback)** — listar referencia a slideshow_viewer.py:
    - `_log(self, message)` — lineas 141-149
    - `_setup_ui(self)` — adaptado: el NavigationController construye SOLO el panel derecho de imagen (lineas 499-552 del original `_create_image_panel`), MAS los botones de navegacion + contador + filename label + zoom overlay timer (lineas 194-214, 446-477 — los GroupBox "Info" y "Navegacion"). NO incluir aqui los grupos "Seleccion", "Detección automática", "Datos recopilados" — esos quedan inicialmente inline en SlideshowViewer (Task 3) y migran en Plans 02/03. El NavigationController construye `QVBoxLayout` con: contador label + filename label + boton Anterior + boton Siguiente + scroll_area conteniendo image_label + zoom_overlay_label + manual_overlay_label (deshabilitado, lo activa el processor por slot en Plan 02).
    - `_create_output_folder(self)` — lineas 554-564
    - `_load_image_list(self)` — lineas 566-580
    - `_show_current_image(self)` — lineas 726-771 (al final, emitir `self.image_changed.emit(self.current_index, current_file, self.working_image)` antes de `if self.auto_mode_enabled: self._run_auto_detection()`; **remover** el bloque `if self.auto_mode_enabled` — eso vive en el processor en Plan 02; comentar con `# Auto-detection se gatilla desde WatermarkProcessor via slot on_image_changed (Plan 02)`)
    - `_apply_zoom(self)` — lineas 773-843. **Adaptar**: el bloque que dibuja overlays de watermark posiciones (lineas 814-839, condicional `if (self.watermark_positions and self.watermark_files...`) se DEJA como pass-through stub que emite `request_redraw` — los overlays los pinta el processor en Plan 02. Por ahora, dejar `# TODO Plan 02: el processor decorara el pixmap via signal/slot (RESEARCH Open Question #2)` y no incluir el codigo de overlays. El bloque de crop overlay (lineas 820-822) tambien se mueve al processor en Plan 02 — dejar TODO comment similar.
    - `_set_zoom(self, new_zoom)` — lineas 1051-1056
    - `_show_zoom_overlay(self)` — lineas 1058-1074
    - `_hide_zoom_overlay(self)` — lineas 1076-1078
    - `_adjust_window_size(self, image_width, image_height)` — lineas 1080-1107. **Adaptar**: como NavigationController es QWidget y no QDialog, NO puede llamar `self.resize()` directamente. Renombrar a `_request_window_resize` y emitir senal nueva `window_resize_requested = Signal(int, int)` (agregar a las senales publicas). El composer (SlideshowViewer) conecta esta senal a su propio resize.
    - `_update_counter(self)` — lineas 1109-1116
    - `_clear_image_memory(self)` — lineas 1118-1126. **Adaptar**: solo limpiar `self.working_image`, `self.base_image_for_preview` aqui; los campos `current_event_*` y `preview_image` viven en el processor (Plan 02). Dejar `# TODO Plan 02: el processor limpia su propio state via slot on_image_changed`.
    - `_next_image(self)` — lineas 1128-1142. **Renombrar a `request_next` (slot publico)**. Llama `_clear_image_memory`, `_save_current_image_as_is`, incrementa index, llama `_show_current_image`. Si `current_index == len-1`, NO llama `_finish_review` (eso vive en el composer) — en cambio, emite senal nueva `finish_requested = Signal()` (agregar a senales).
    - `_previous_image(self)` — lineas 1144-1151. **Renombrar a `request_previous` (slot publico)**.
    - `_save_current_image_as_is(self)` — lineas 1153-1176
    - `wheelEvent(self, event)` — lineas 1955-1966 (zoom con Ctrl+rueda) — el NavigationController owns image_label y scroll_area, asi que es el natural owner del wheel zoom. El composer puede tambien delegarle.
    - `adjust_zoom(self, delta)` — metodo publico nuevo que el composer puede llamar desde sus keyPressEvent (delegacion).

    **Persistencia**: el `set_navigation_enabled(bool)` slot publico para que el processor pueda bloquear prev/next durante preview (PATTERNS line 632 — patron processing_blocked):
    ```
    def set_navigation_enabled(self, enabled: bool):
        self.prev_btn.setEnabled(enabled and self.current_index > 0)
        self.next_btn.setEnabled(enabled and self.current_index < len(self.image_files) - 1)
    ```

    **Slot para wiring del processor (Plan 02 lo conectara)** — declarar pero dejar pass o minimal:
    ```
    def on_preview_changed(self, preview_image):
        # Plan 02 wiring: el processor envia preview_image (np.ndarray) para mostrar
        # En esta fase: guardar el preview y refrescar
        self._preview_image_from_processor = preview_image
        self._apply_zoom()
    def on_image_processed(self, file, working_image, *args):
        # Plan 02 wiring: tras accept del processor, actualizar working_image
        self.working_image = working_image
        self.processed_images.add(self.current_index)
        self._apply_zoom()
    def reset_current_image(self):
        # Plan 02/03 wiring: stub - sera implementado cuando el processor extraiga _reset_current_image
        pass
    ```

    **Defensivos a preservar VERBATIM** (RESEARCH Pitfall 3):
    - El `blockSignals(True/False)` bracket (no aplica directamente aqui pero NO eliminar guards futuros)
    - El `if self.working_image is None: self.working_image = load_images_cv2(current_file)` guard (linea 733-735) es load-bearing — preservar el `is None` check antes de cargar

    **Inicializacion en __init__** (orden critico, mirror de slideshow_viewer.py lineas 109-113):
    ```
    self._setup_ui()
    self._load_image_list()
    if self.image_files:
        self._show_current_image()
    ```

    **min_lines target:** ~280-350 lineas. Si menos, faltan metodos. Si mas de 500, sobra logica (debe estar en stubs o composer).
  </action>
  <verify>
    <automated>python -m py_compile WatermarkRemove/ui/components/navigation_controller.py</automated>
    <automated>python -c "from WatermarkRemove.ui.components import NavigationController; print(NavigationController.image_changed); print(NavigationController.window_resize_requested); print(NavigationController.finish_requested)"</automated>
    <automated>grep -cE "^\s*def (request_next|request_previous|set_navigation_enabled|on_preview_changed|on_image_processed|adjust_zoom)" WatermarkRemove/ui/components/navigation_controller.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "class NavigationController(QWidget):" WatermarkRemove/ui/components/navigation_controller.py` retorna 1
    - `grep -c "image_changed = Signal(int, object, object)" WatermarkRemove/ui/components/navigation_controller.py` retorna 1
    - `grep -c "output_folder_ready = Signal" WatermarkRemove/ui/components/navigation_controller.py` retorna 1
    - `grep -c "window_resize_requested = Signal" WatermarkRemove/ui/components/navigation_controller.py` retorna 1
    - `grep -c "finish_requested = Signal" WatermarkRemove/ui/components/navigation_controller.py` retorna 1
    - `grep -cE "def (request_next|request_previous|set_navigation_enabled|on_preview_changed|on_image_processed|adjust_zoom|_show_current_image|_apply_zoom|_load_image_list|_create_output_folder|_save_current_image_as_is|_set_zoom|_update_counter|_clear_image_memory|_log)" WatermarkRemove/ui/components/navigation_controller.py` retorna >= 14
    - `grep -c "from WatermarkRemove.wm_remove import load_images_cv2" WatermarkRemove/ui/components/navigation_controller.py` retorna 1
    - `grep -c "from WatermarkRemove.services import wm_persistence" WatermarkRemove/ui/components/navigation_controller.py` retorna 1
    - `grep -c "SUPPORTED_FORMATS = " WatermarkRemove/ui/components/navigation_controller.py` retorna 1
    - `wc -l WatermarkRemove/ui/components/navigation_controller.py` >= 280
    - `python -m py_compile WatermarkRemove/ui/components/navigation_controller.py` sale 0
    - `python -c "from WatermarkRemove.ui.components import NavigationController"` sale 0
    - `python -c "from PySide6.QtWidgets import QApplication; import sys; app = QApplication(sys.argv); from WatermarkRemove.ui.components import NavigationController; nc = NavigationController('.', None, None); print('init OK'); sys.exit(0)"` sale 0
  </acceptance_criteria>
  <done>
    NavigationController(QWidget) implementado y autocontenido — tiene UI propia (panel derecho + botones prev/next + contador + filename label + zoom overlay), gestiona estado de navegacion (image_files, current_index, working_image, output_folder, processed_images, processed_positions, zoom_level), expone slots publicos (request_next, request_previous, set_navigation_enabled, on_preview_changed, on_image_processed, adjust_zoom, reset_current_image), y emite senales (image_changed, output_folder_ready, window_resize_requested, finish_requested, request_redraw). El archivo compila e instancia sin errores en QApplication.
  </done>
</task>

<task type="auto">
  <name>Task 3: Refactorizar SlideshowViewer para usar NavigationController; preservar manual+auto+training inline</name>
  <files>
    WatermarkRemove/ui/slideshow_viewer.py
  </files>
  <read_first>
    WatermarkRemove/ui/slideshow_viewer.py,
    WatermarkRemove/ui/components/navigation_controller.py,
    gui/controller.py,
    .planning/phases/02-slideshowviewer-decomposition/02-PATTERNS.md
  </read_first>
  <action>
    Refactorizar `WatermarkRemove/ui/slideshow_viewer.py` para que la navegacion + render delegue en `NavigationController`. **CRITICO**: los Plans 02 y 03 todavia necesitan que el manual mode, auto YOLO, training data collection y position-grid handlers funcionen en `slideshow_viewer.py` (se extraen en Plans posteriores). En este plan SOLO se extrae navegacion.

    **Cambios a slideshow_viewer.py:**

    1. **Top de archivo (imports, lineas 1-28)** — agregar:
       - `from WatermarkRemove.ui.components import NavigationController, WatermarkProcessor, TrainingDataCollector`

    2. **`__init__` (lineas 55-113)** — reemplazar el bloque de inicializacion de estado de navegacion+render por delegacion a `self.navigation = NavigationController(folder_path, parent=self, watermark_tab=watermark_tab)`. Conservar SOLO los campos que el manual/auto/training todavia usan inline: `watermark_folder`, `watermark_name`, `watermark_positions`, `watermark_files`, `watermark_files_all`, `watermark_rectangles`, `crop_mode_enabled`, `auto_mode_enabled`, `detected_marks`, `selected_mark_index`, `auto_preview_image`, `manual_mode_enabled`, `mouse_position`, `preview_image`, `is_preview_active`, `current_event_*`, `watermark_alpha_values`. Estos quedan inline **en SlideshowViewer** (el composer) hasta el Plan 02 — NO van en NavigationController. `crop_mode_enabled` en particular migra a `WatermarkProcessor` completamente en Plan 02 Task 1.

       Eliminar de `__init__`: `self.image_files`, `self.current_index`, `self.current_pixmap`, `self.zoom_level`, `self.controls_panel_width`, `self.output_folder`, `self.processed_images`, `self.processed_positions`, `self.working_image`, `self.base_image_for_preview` — todos viven ahora en `self.navigation`.

       Conservar: `self.user_approved = False`, `self.watermark_tab = watermark_tab` (necesario para _log local del manual mode).

       Crear stubs para los otros 2 componentes (placeholders sin uso real, Plans 02 y 03 los activaran):
       - `self.processor = WatermarkProcessor(parent=self, watermark_tab=watermark_tab)` — instancia stub (no wired aun)
       - `self.collector = TrainingDataCollector(parent=self, watermark_tab=watermark_tab)` — instancia stub (no wired aun)

    3. **`_setup_ui` (lineas 151-168)** — refactorizar:
       - Crear `QHBoxLayout(self)` con spacing 15, margins (10,10,10,10) — preservar
       - Panel izquierdo: llamar `left_panel = self._create_controls_panel()` (este metodo se modifica abajo, parte 4)
       - Panel derecho: `main_layout.addWidget(self.navigation, 1)` — el NavigationController ES el panel derecho ahora; `self.navigation` tiene el image_label y scroll area como hijos
       - Conectar las senales del navigation:
         - `self.navigation.window_resize_requested.connect(self.resize)` — el composer (QDialog) ejecuta resize
         - `self.navigation.finish_requested.connect(self._finish_review)` — cuando navigation llega al final, mostrar dialogo

    4. **`_create_controls_panel` (lineas 170-497)** — refactorizar parcialmente:
       - Eliminar el bloque "Info" (lineas 194-214: counter_label + filename_label) — esos labels ahora viven en `self.navigation` (ya construidos por NavigationController._setup_ui). Si Tab necesita acceso, exponer via `self.navigation.counter_label` / `self.navigation.filename_label` getters.
       - Eliminar el bloque "Navegacion" (lineas 446-477: prev_btn, next_btn, finish_btn, cancel_btn). **Adaptar**: finish_btn y cancel_btn quedan en el composer (boton "Finalizar y Procesar" y "Cancelar" — son acciones del dialogo). prev_btn y next_btn migran a NavigationController (ya estan ahi en Task 2). Crear nuevo nav_group con SOLO finish_btn y cancel_btn (sin prev/next, esos los pinta NavigationController).
       - Conservar inline (no migrar todavia): grupos "Seleccion" (lineas 226-367), checkbox "auto_mode_checkbox" (lineas 217-223), grupo "Detección automática" (lineas 369-444), grupo "Datos recopilados" (lineas 480-493). **Estos migran en Plans 02 y 03.**

    5. **Metodos a ELIMINAR de slideshow_viewer.py** (porque viven en NavigationController ahora):
       - `_create_output_folder` (lineas 554-564)
       - `_load_image_list` (lineas 566-580)
       - `_show_current_image` (lineas 726-771)
       - `_apply_zoom` (lineas 773-843) — pero antes de eliminar, mover la logica de overlays de watermark (lineas 814-839) y crop overlay (lineas 820-822) a `_apply_zoom` que vive ahora en NavigationController; sin embargo, en este Plan, NavigationController.`_apply_zoom` tiene un TODO Plan 02 — la pintura de overlays se hara desde el processor via slot en Plan 02. Por ahora, los overlays NO se pintan durante Plan 01 — esto es una regresion visual TEMPORAL aceptada en el plan, documentada con `# REGRESION TEMPORAL: cuadros rojo/verde de posiciones y overlay de crop pintaran de vuelta en Plan 02-02 cuando WatermarkProcessor decorate el pixmap via signal/slot`.
       - `_set_zoom` (lineas 1051-1056)
       - `_show_zoom_overlay` (1058-1074)
       - `_hide_zoom_overlay` (1076-1078)
       - `_adjust_window_size` (1080-1107)
       - `_update_counter` (1109-1116)
       - `_clear_image_memory` (1118-1126)
       - `_next_image` (1128-1142)
       - `_previous_image` (1144-1151)
       - `_save_current_image_as_is` (1153-1176)
       - `wheelEvent` (1955-1966) — delegar al navigation: el composer hace `def wheelEvent(self, event): self.navigation.wheelEvent(event)` o no override del todo si NavigationController consume.

    6. **`keyPressEvent` (lineas 1968-2006)** — refactorizar siguiendo PATTERNS lines 515-551:
       ```
       def keyPressEvent(self, event):
           key = event.key()
           # Zoom keys
           if key in (Qt.Key.Key_Plus, Qt.Key.Key_Equal):
               self.navigation.adjust_zoom(10); event.accept(); return
           if key == Qt.Key.Key_Minus:
               self.navigation.adjust_zoom(-10); event.accept(); return
           if key == Qt.Key.Key_0:
               self.navigation.set_zoom_level(100); event.accept(); return
           # Modo manual con preview activo: Space/Backspace van a accept/revert (queda inline)
           check_opc_avanzadas = self.opciones_avanzadas.isChecked()
           if key == Qt.Key.Key_Space:
               if check_opc_avanzadas and self.is_preview_active:
                   self._accept_preview()  # todavia inline; migrara en Plan 02
               else:
                   self.navigation.request_next()
               return
           if key == Qt.Key.Key_Backspace:
               if check_opc_avanzadas and self.is_preview_active:
                   self._revert_preview()
               else:
                   self.navigation.request_previous()
               return
           super().keyPressEvent(event)
       ```
       **CRITICO**: el guard `if check_opc_avanzadas and self.is_preview_active` es load-bearing (RESEARCH Pitfall 2). Preservar la condicion exacta antes de delegar a navigation.

    7. **`mousePressEvent` (lineas 1913-1953)** — conservar inline en SlideshowViewer (es position-grid handler que migra en Plan 02). **PERO**: como `self.image_files`, `self.current_index`, `self.processed_*`, `self.scroll_area`, `self.image_label` viven ahora en `self.navigation`, adaptar:
       - `self.image_files[self.current_index]` -> `self.navigation.image_files[self.navigation.current_index]`
       - `self.processed_images.add(...)` -> `self.navigation.processed_images.add(...)`
       - `self.processed_positions` -> `self.navigation.processed_positions`
       - `self.scroll_area` -> `self.navigation.scroll_area` (exponer como atributo publico en NavigationController)
       - `self.working_image` -> `self.navigation.working_image`
       - `self.output_folder` -> `self.navigation.output_folder`
       - `self._show_current_image()` -> `self.navigation._show_current_image()`
       - `self._next_image()` -> `self.navigation.request_next()`

    8. **`_process_watermark_at_position` (lineas 1178-1258)**, `_remove_watermark_preview` (1419-1509), `_accept_preview` (1511-1589), `_revert_preview` (1591-1618), `_reset_current_image` (1620-1670), todos los metodos auto (1672-1885), `eventFilter` (1260-1281), `_toggle_*`, `_load_watermark_folders`, `_filter_watermark_combo`, `_on_*`, `_apply_crop`, `_draw_*`, `_compute_live_preview`, `_run_auto_detection`, etc — **CONSERVAR INLINE pero adaptar referencias** a `self.image_files/current_index/output_folder/working_image/processed_*` para que apunten a `self.navigation.*`.

       Hacer un grep-replace selectivo: cada referencia en `def _*` que originalmente usaba `self.image_files`, `self.current_index`, `self.working_image`, `self.output_folder`, `self.processed_images`, `self.processed_positions`, `self.image_label`, `self.scroll_area`, `self.current_pixmap`, `self.zoom_level` → cambiar prefijo a `self.navigation.<x>`.

       Excepciones (NO cambiar — viven en SlideshowViewer todavia):
       - `self.is_preview_active`, `self.preview_image`, `self.current_event_*`, `self.watermark_*`, `self.base_image_for_preview` (esta ultima REMUEVE del nav y mantiene en composer porque el manual mode la usa intensivamente; el Plan 02 la migrara junto con su processor)
       - **Decision**: `base_image_for_preview` queda en SlideshowViewer durante Plan 01 (es parte de la maquina de estados manual que migra en Plan 02). NO migrarla a NavigationController en Task 2 — corregir tambien Task 2 si quedo agregada alli (mantenerla solo como TODO comment).

    9. **`_finish_review` (1887-1898), `_cancel_review` (1900-1911)** — conservar inline, sin cambios.

    10. **API publica (lineas 2008-2018)** — preservar firmas, pero adaptar bodies:
       ```
       def get_approved(self) -> bool: return self.user_approved
       def get_output_folder(self) -> Path: return self.navigation.output_folder
       def has_processed_images(self) -> bool: return len(self.navigation.processed_images) > 0
       ```

    11. **`current_dir` global (lineas 17-20)** — preservar (todavia se usa para `wm_positions.json` linea 702 y `training_data.json` lineas 119, 1542, 1855; estos migran en Plans 02 y 03).

    **Mantener funcional**: tras este task el visor debe abrir y cerrar via `gui/controller.py`, navegar con Space/Backspace y prev/next, mostrar el zoom overlay, y permitir manual mode + auto mode + training counts (esos todavia funcionan inline). La REGRESION ACEPTADA: cuadros rojo/verde de posiciones y overlay de crop NO se pintan durante este Plan — se restauran en Plan 02-02.
  </action>
  <verify>
    <automated>python -m py_compile WatermarkRemove/ui/slideshow_viewer.py</automated>
    <automated>python -c "from WatermarkRemove.ui import SlideshowViewer; from WatermarkRemove.ui.components import NavigationController; print('imports OK')"</automated>
    <automated>grep -cE "^\s*def (_load_image_list|_show_current_image|_create_output_folder|_next_image|_previous_image|_save_current_image_as_is|_apply_zoom|_set_zoom|_show_zoom_overlay|_hide_zoom_overlay|_adjust_window_size|_update_counter|_clear_image_memory)" WatermarkRemove/ui/slideshow_viewer.py</automated>
    <automated>grep -c "self.navigation" WatermarkRemove/ui/slideshow_viewer.py</automated>
    <automated>grep -c "review_completed = Signal" WatermarkRemove/ui/slideshow_viewer.py</automated>
    <automated>grep -cE "def (get_approved|get_output_folder|has_processed_images)" WatermarkRemove/ui/slideshow_viewer.py</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "^\s*def (_load_image_list|_show_current_image|_create_output_folder|_next_image|_previous_image|_save_current_image_as_is|_apply_zoom|_set_zoom|_show_zoom_overlay|_hide_zoom_overlay|_adjust_window_size|_update_counter|_clear_image_memory)" WatermarkRemove/ui/slideshow_viewer.py` retorna 0 (todos los metodos de navegacion migrados a NavigationController)
    - `grep -c "self.navigation = NavigationController" WatermarkRemove/ui/slideshow_viewer.py` retorna 1
    - `grep -c "self.processor = WatermarkProcessor" WatermarkRemove/ui/slideshow_viewer.py` retorna 1
    - `grep -c "self.collector = TrainingDataCollector" WatermarkRemove/ui/slideshow_viewer.py` retorna 1
    - `grep -c "from WatermarkRemove.ui.components import" WatermarkRemove/ui/slideshow_viewer.py` retorna 1
    - `grep -c "class SlideshowViewer(QDialog):" WatermarkRemove/ui/slideshow_viewer.py` retorna 1
    - `grep -c "review_completed = Signal(bool)" WatermarkRemove/ui/slideshow_viewer.py` retorna 1
    - `grep -cE "def (get_approved|get_output_folder|has_processed_images)" WatermarkRemove/ui/slideshow_viewer.py` retorna 3
    - `grep -c "def __init__(self, folder_path: str, parent=None, watermark_tab=None)" WatermarkRemove/ui/slideshow_viewer.py` retorna 1 (firma del constructor PRESERVADA verbatim)
    - `grep -c "self.navigation.request_next()" WatermarkRemove/ui/slideshow_viewer.py` >= 1
    - `grep -c "self.navigation.request_previous()" WatermarkRemove/ui/slideshow_viewer.py` >= 1
    - `python -m py_compile WatermarkRemove/ui/slideshow_viewer.py` sale 0
    - `python -m py_compile WatermarkRemove/ui/components/navigation_controller.py WatermarkRemove/ui/components/watermark_processor.py WatermarkRemove/ui/components/training_data_collector.py` sale 0
    - `python -c "from WatermarkRemove.ui import SlideshowViewer"` sale 0
    - Linea contract preservada: `grep -c "viewer = SlideshowViewer(input_path, MainWindow, watermark_tab=watermark_tab)" gui/controller.py` retorna 1 (verificacion del caller — gui/controller.py NO se modifico)
    - Manual: ejecutar `python -m WatermarkRemove.ui.slideshow_viewer` con la carpeta de test del modulo (`if __name__ == "__main__"` en lineas 2022-2040) — el visor abre, Space avanza, Backspace retrocede, manual mode todavia funciona (Plan 02 no aplicado aun)
  </acceptance_criteria>
  <done>
    SlideshowViewer es un composer parcial: la navegacion vive en NavigationController via senales; manual+auto+training siguen inline (migran en Plans 02 y 03). La API publica (`__init__(folder_path, parent, watermark_tab)`, `review_completed`, `get_approved`, `get_output_folder`, `has_processed_images`) esta intacta — `gui/controller.py:321` funciona sin cambios. Ejecutar `python -m WatermarkRemove.ui.slideshow_viewer` lanza el visor y la navegacion Space/Backspace funciona. Documentada la regresion temporal de overlays de posicion/crop (se restaura en Plan 02-02).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gui/controller.py → SlideshowViewer | input_path proviene de `MainWindow.inputField.text()`; ya validado upstream (`Path(input_path).exists()`). Esta fase no introduce nuevos puntos de entrada. |
| NavigationController → wm_remove.load_images_cv2 | Path de archivo de imagen — heredada la mitigacion de path safety via `np.fromfile(str(path))` en `load_images_cv2`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-01 | Tampering | NavigationController.load_images_cv2 path | accept | Mitigacion existente en wm_remove.py heredada — paths son `pathlib.Path`, no concatenacion de strings (RESEARCH Security Domain). Sin cambio. |
| T-02-01-02 | Tampering | NavigationController.guardar() output_folder | accept | Mitigacion existente — `guardar()` escribe a `output_folder / image_path.name` con `Path` objects. Sin cambio. |
| T-02-01-03 | Information Disclosure | wm_persistence (lectura de last_watermark_folder/crop_pixels) | accept | wm_settings.json es estado UI local; sin PII ni secretos. Defensive parsing JSON heredado de Phase 1. Sin cambio. |
| T-02-01-04 | DoS | NavigationController.working_image (load full image into RAM) | accept | Limitacion conocida pre-existente del modulo (RESEARCH Security Domain). Fuera de scope de refactor estructural. |
| T-02-01-SC | Tampering | npm/pip/cargo installs | N/A | No se instalan paquetes en esta fase (refactor 100% estructural — RESEARCH Package Legitimacy Audit explicitamente N/A). |

Conclusion: Phase 2 Plan 01 es redistribucion estructural — sin nuevas superficies de ataque. Mitigaciones existentes heredadas por NavigationController al llamar las mismas funciones (`load_images_cv2`, `guardar`, `wm_persistence`). ASVS Level 1 cumplido por preservacion.
</threat_model>

<verification>
- `python -m py_compile WatermarkRemove/ui/components/__init__.py WatermarkRemove/ui/components/navigation_controller.py WatermarkRemove/ui/components/watermark_processor.py WatermarkRemove/ui/components/training_data_collector.py WatermarkRemove/ui/slideshow_viewer.py` sale 0
- `python -c "from WatermarkRemove.ui import SlideshowViewer; from WatermarkRemove.ui.components import NavigationController, WatermarkProcessor, TrainingDataCollector"` sale 0
- `grep -cE "^\s*def (_load_image_list|_show_current_image|_create_output_folder|_next_image|_previous_image|_save_current_image_as_is|_apply_zoom|_set_zoom|_show_zoom_overlay|_hide_zoom_overlay|_adjust_window_size|_update_counter|_clear_image_memory)" WatermarkRemove/ui/slideshow_viewer.py` retorna 0
- Manual UAT (Seccion 1 de 02-HUMAN-UAT.md): abrir el visor, Space/Backspace navega correctamente, Ctrl+rueda zoomea, contador se actualiza.
- `gui/controller.py` NO modificado: `git diff gui/controller.py` retorna vacio.
</verification>

<success_criteria>
- Paquete `WatermarkRemove/ui/components/` existe con 4 archivos (`__init__.py`, `navigation_controller.py`, `watermark_processor.py`, `training_data_collector.py`)
- `NavigationController(QWidget)` implementado con todos los metodos de navegacion + render basico
- `SlideshowViewer` ya no contiene los 13 metodos de navegacion listados arriba — viven en NavigationController
- `SlideshowViewer.__init__(folder_path, parent, watermark_tab)` firma sin cambios; `review_completed` signal sin cambios; `get_approved/get_output_folder/has_processed_images` sin cambios
- `gui/controller.py:321` funciona sin modificar
- Manual UAT Seccion 1 (Navegacion) completada — Space/Backspace funcionan
- Regresion temporal documentada: overlays de posiciones rojo/verde y crop no se pintan en este Plan; se restauran en Plan 02-02 (decorate via signal)
</success_criteria>

<output>
Al completar este plan, crear `.planning/phases/02-slideshowviewer-decomposition/02-01-SUMMARY.md` siguiendo `$HOME/.claude/get-shit-done/templates/summary.md`. Incluir:
- Lista de metodos migrados (13 metodos listados)
- Confirmacion de regresion temporal de overlays (a restaurar en Plan 02-02)
- Confirmacion de que `gui/controller.py` no fue modificado
- Resultado de manual UAT Seccion 1
</output>
