---
phase: 04-visual-polish
plan: 02
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - WatermarkRemove/ui/components/navigation_controller.py
  - WatermarkRemove/ui/components/watermark_processor.py
  - WatermarkRemove/ui/slideshow_viewer.py
autonomous: true
requirements: [UI-02]
must_haves:
  truths:
    - "El panel izquierdo tiene cuatro secciones visibles en orden: Grupo Navegación → Selector de Modo → QStackedWidget → Grupo Training Data (D-04)"
    - "Los botones [Selección] [Recorte] [Automático] se comportan como tabs exclusivos: solo uno puede estar activo a la vez (D-05)"
    - "Cambiar el modo activo actualiza el QStackedWidget al índice correspondiente (0/1/2) (D-06)"
    - "El modo Selección tiene un checkbox 'Avanzado' que expande controles avanzados dentro del stack (D-07)"
    - "Si el modelo YOLO no está disponible, el botón [Automático] aparece deshabilitado al iniciar (D-08)"
    - "NavigationController expone create_nav_controls_widget() que devuelve un QWidget con los botones prev/next, contador y filename — sin la scroll area (D-04)"
    - "WatermarkProcessor expone panel_seleccion, panel_recorte, panel_auto como QWidget independientes y un método set_mode(index) (D-06)"
  artifacts:
    - path: "WatermarkRemove/ui/components/navigation_controller.py"
      provides: "Método create_nav_controls_widget() que retorna QWidget con controles de nav sin la imagen"
      contains: "def create_nav_controls_widget"
    - path: "WatermarkRemove/ui/components/watermark_processor.py"
      provides: "panel_seleccion, panel_recorte, panel_auto como atributos + set_mode()"
      contains: "def set_mode"
    - path: "WatermarkRemove/ui/slideshow_viewer.py"
      provides: "Panel de controles reorganizado con QButtonGroup + QStackedWidget"
      contains: "QStackedWidget"
  key_links:
    - from: "WatermarkRemove/ui/slideshow_viewer.py"
      to: "WatermarkRemove/ui/components/navigation_controller.py"
      via: "self.navigation.create_nav_controls_widget() en _create_controls_panel"
      pattern: "create_nav_controls_widget"
    - from: "WatermarkRemove/ui/slideshow_viewer.py"
      to: "WatermarkRemove/ui/components/watermark_processor.py"
      via: "self.processor.panel_seleccion/panel_recorte/panel_auto en addWidget al stack"
      pattern: "panel_seleccion|panel_recorte|panel_auto"
    - from: "WatermarkRemove/ui/slideshow_viewer.py"
      to: "WatermarkRemove/ui/components/watermark_processor.py"
      via: "self._mode_group.idClicked → _on_mode_changed → self.processor.set_mode()"
      pattern: "_on_mode_changed"
---

<objective>
Reorganizar el panel de controles del SlideshowViewer en cuatro secciones con separadores visuales (D-04): un Grupo Navegación permanente (prev/next, contador, filename, finish, cancel), un selector de modo con QButtonGroup (D-05), un QStackedWidget con una página por modo (D-06), y el Grupo Training Data permanente abajo.

Para lograrlo: (1) NavigationController separa su widget de imagen del widget de navegación exponiendo `create_nav_controls_widget()`, (2) WatermarkProcessor expone tres paneles independientes (`panel_seleccion`, `panel_recorte`, `panel_auto`) y un método `set_mode(index)` que reemplaza los checkboxes de modo, (3) SlideshowViewer._create_controls_panel() ensamble todo.

Purpose: UI-02 exige que el usuario pueda identificar los controles sin leer documentación. La jerarquía actual mezcla controles de modo (checkboxes) con controles de acción en un layout plano sin estructura visual. El nuevo diseño hace la estructura obvia: sección fija superior, selector de modo visual, controles del modo activo, sección fija inferior.
Output: NavigationController con create_nav_controls_widget + WatermarkProcessor con 3 paneles + set_mode + slideshow_viewer con QStackedWidget + QButtonGroup.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-visual-polish/04-CONTEXT.md
@.planning/phases/04-visual-polish/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Separar NavigationController — extraer create_nav_controls_widget()</name>
  <files>WatermarkRemove/ui/components/navigation_controller.py</files>
  <read_first>
    - WatermarkRemove/ui/components/navigation_controller.py (leer completo — especialmente _setup_ui L130-220 para entender qué widgets construye, y los atributos de instancia counter_label, filename_label, prev_btn, next_btn que otros métodos usan como self.X)
  </read_first>
  <action>
Realizar DOS modificaciones en `NavigationController`:

**Modificación 1: Agregar `self._nav_info_group` en `_setup_ui()`**

En `_setup_ui()`, la variable local `info_group` (creada con `info_group = QGroupBox("ℹ️ Info")`) se agrega al `outer` layout y luego los widgets que contiene serán reparentados por `create_nav_controls_widget()`. Tras el reparentado, `info_group` queda como un QGroupBox vacío y visible en el layout de NavigationController.

Para poder ocultarlo desde `create_nav_controls_widget()`, almacenarlo como atributo de instancia. Localizar la línea en `_setup_ui()` que dice `info_group = QGroupBox("ℹ️ Info")` (aproximadamente L137) y agregar una sola línea debajo:

```python
info_group = QGroupBox("ℹ️ Info")
self._nav_info_group = info_group   # Phase 4: para ocultarlo tras reparentar en create_nav_controls_widget
```

Esta es la ÚNICA modificación a `_setup_ui()`.

**Modificación 2: Agregar `create_nav_controls_widget()` como nuevo método**

Agregar el método justo antes de `_create_output_folder` (aproximadamente L224). El método reparenta los widgets de navegación a un nuevo QGroupBox y oculta el contenedor vacío original:

```python
def create_nav_controls_widget(self) -> 'QGroupBox':
    """Retorna un QGroupBox 'Navegación' con los controles de navegación
    para el panel izquierdo del QSplitter (Phase 4, D-04).

    Reparenta self.counter_label, self.filename_label, self.prev_btn,
    self.next_btn al nuevo contenedor. Los slots y conexiones existentes
    siguen funcionando porque los objetos son los mismos.

    NOTA: Oculta self._nav_info_group (el QGroupBox 'ℹ️ Info' del layout
    de NavigationController) que queda vacío tras el reparentado, de modo
    que NavigationController muestre solo la scroll area (imagen).
    Solo debe llamarse una vez por instancia.
    """
    nav_widget = QGroupBox("Navegación")
    nav_layout = QVBoxLayout(nav_widget)
    nav_layout.setSpacing(5)
    nav_layout.setContentsMargins(8, 6, 8, 6)

    # Reparentar widgets ya existentes (creados en _setup_ui)
    nav_layout.addWidget(self.counter_label)
    nav_layout.addWidget(self.filename_label)

    btn_row = QHBoxLayout()
    btn_row.setSpacing(5)
    btn_row.addWidget(self.prev_btn)
    btn_row.addWidget(self.next_btn)
    nav_layout.addLayout(btn_row)

    # Ocultar el contenedor original (queda vacío tras reparentar sus widgets)
    if hasattr(self, '_nav_info_group'):
        self._nav_info_group.hide()

    return nav_widget
```

CONSTRAINT: No agregar `QLabel("Imagen:")` adicional — `counter_label` ya tiene el label "Imagen:" como contexto del grupo, y el QGroupBox "Navegación" provee el título visual. El `QLabel("Imagen:")` estaba dentro de `info_group` original; como `info_group` se oculta, ese label desaparece sin necesidad de recrearlo.

CONSTRAINT: `_update_counter()`, `_show_current_image()`, y todos los métodos que modifican `self.counter_label.setText()`, `self.filename_label.setText()`, `self.prev_btn.setEnabled()`, `self.next_btn.setEnabled()` siguen funcionando sin cambios porque trabajan sobre los mismos objetos de instancia.

CONSTRAINT: `QGroupBox` ya está importado en `navigation_controller.py` (línea 23). No agregar import redundante.
  </action>
  <verify>
    <automated>cd "c:/Users/Felix/Desktop/Python/manhwa_ocr/SmartStitch" && python -m py_compile WatermarkRemove/ui/components/navigation_controller.py && echo COMPILE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile` sale 0 e imprime COMPILE_OK
    - `python -c "import sys; src=open('WatermarkRemove/ui/components/navigation_controller.py').read(); assert 'create_nav_controls_widget' in src; assert '_nav_info_group' in src; print('NAV_SPLIT_OK')"` imprime NAV_SPLIT_OK
    - El método retorna `QGroupBox` (contiene la cadena `QGroupBox("Navegación")`)
    - `_setup_ui()` contiene `self._nav_info_group = info_group` (una sola línea agregada)
    - `create_nav_controls_widget()` contiene `self._nav_info_group.hide()`
    - Ningún otro método existente fue modificado
  </acceptance_criteria>
  <done>NavigationController almacena info_group como self._nav_info_group para poder ocultarlo, y expone create_nav_controls_widget() que reparenta los widgets de nav y oculta el contenedor vacío original.</done>
</task>

<task type="auto">
  <name>Task 2: Refactorizar WatermarkProcessor — exponer 3 paneles y set_mode()</name>
  <files>WatermarkRemove/ui/components/watermark_processor.py</files>
  <read_first>
    - WatermarkRemove/ui/components/watermark_processor.py (leer _setup_ui L152-382 completo — entender la estructura actual: auto_mode_checkbox toggle, seleccion_group con todos sus widgets, auto_group con sus widgets; también leer _toggle_auto_mode y _toggle_crop_mode para entender qué cambian)
  </read_first>
  <action>
El objetivo es exponer los contenidos de `seleccion_group` y `auto_group` como paneles independientes (`self.panel_seleccion`, `self.panel_recorte`, `self.panel_auto`) que el slideshow_viewer puede agregar a un QStackedWidget, y agregar `set_mode(index)` que el selector de modo llama en lugar de los checkboxes.

**Cambio 1: Refactorizar `_setup_ui()` para crear los tres paneles**

El enfoque es MÍNIMAMENTE invasivo: en lugar de reescribir completamente _setup_ui, extraer los widgets existentes a tres paneles nuevos al final del método, después de que todos los widgets ya existan. Esto preserva toda la lógica de inicialización existente.

Al FINAL del método `_setup_ui` (después de `self.auto_group.hide()` y `outer.addWidget(self.auto_group)` en la línea ~381), agregar:

```python
        # === Phase 4 (D-04, D-06): Crear paneles para QStackedWidget ===
        # Los widgets ya existen (creados arriba). Crear contenedores QWidget
        # que los wrappean para el QStackedWidget del slideshow_viewer.

        # Panel Selección: wrappea el contenido de seleccion_group
        self.panel_seleccion = self.seleccion_group

        # Panel Recorte: mueve los widgets de crop desde seleccion_group a un panel separado.
        # Al agregarlos a recorte_layout, Qt los reparentea fuera de seleccion_group.
        # Los widgets quedan VISIBLES dentro de panel_recorte (el QStackedWidget controla
        # qué panel muestra — NO se ocultan aquí).
        self.panel_recorte = QWidget()
        recorte_layout = QVBoxLayout(self.panel_recorte)
        recorte_layout.setSpacing(5)
        recorte_layout.setContentsMargins(5, 5, 5, 5)
        recorte_layout.addWidget(QLabel("Píxeles a recortar:"))
        recorte_layout.addWidget(self.crop_pixels_input)
        recorte_layout.addWidget(self.crop_invert_checkbox)
        recorte_layout.addWidget(self.crop_apply_btn)
        recorte_layout.addStretch(1)
        # Mostrar los widgets de crop en su panel (estaban ocultos por el checkbox flow anterior)
        self.crop_pixels_input.show()
        self.crop_invert_checkbox.show()
        self.crop_apply_btn.show()
        # IMPORTANTE: NO llamar .hide() sobre los widgets de crop aquí — ya viven en
        # panel_recorte y deben estar visibles cuando ese panel es la página activa del stack.

        # Panel Automático: wrappea auto_group
        self.panel_auto = self.auto_group
        self.panel_auto.show()  # auto_group.hide() fue llamado arriba; revertir para el stack

        # Remover los paneles del outer layout de WatermarkProcessor (el QStackedWidget
        # del slideshow_viewer los contendrá — WatermarkProcessor ya no es visible directamente)
        outer.removeWidget(self.seleccion_group)
        outer.removeWidget(self.auto_group)
        # Remover también auto_mode_checkbox (el QButtonGroup en slideshow_viewer lo reemplaza)
        outer.removeWidget(self.auto_mode_checkbox)
        self.auto_mode_checkbox.hide()
        # Ocultar crop_mode_checkbox (era el mecanismo anterior; el modo se cambia externamente)
        self.crop_mode_checkbox.hide()
```

NOTA IMPORTANTE sobre el enfoque de paneles: dado que mover widgets entre layouts en Qt puede ser complejo (un widget solo puede tener un parent), el enfoque más robusto es:
- `panel_seleccion = self.seleccion_group` (el QGroupBox ya existente)
- `panel_auto = self.auto_group` (el QGroupBox ya existente)
- `panel_recorte` = nuevo QWidget con los widgets de crop duplicados (o movidos)

Para `panel_recorte`, los widgets de crop (`crop_pixels_input`, `crop_invert_checkbox`, `crop_apply_btn`) actualmente están dentro de `seleccion_group`'s layout. Al agregarlos al layout de `panel_recorte`, Qt los reparentea automáticamente. Eso significa que desaparecen de `seleccion_group`. Esto es el comportamiento correcto — en modo Selección los controles de crop no se ven; en modo Recorte están en su propio panel.

Simplificación del `panel_seleccion` después de mover crop: en `seleccion_group` quedan:
- watermark_folder_combo, watermark_filter, watermark_combo
- opciones_avanzadas checkbox (renombrado a "Avanzado" en acción 2)
- label_alpha_adj, alpha_adjust, label_offset_adj, offset_adj_container, quick_preview_checkbox
- reset_btn, accept_btn, revert_btn
- crop_mode_checkbox (que se oculta — el checkbox ya no sirve en el nuevo flujo)

El `crop_mode_checkbox` puede simplemente quedar oculto dentro de seleccion_group sin eliminarlo, para no romper referencias en `_toggle_crop_mode`.

**Cambio 2: Cambiar label de opciones_avanzadas**

Cambiar el texto del checkbox de `"Modo selección manual"` a `"Avanzado"` (D-07). Localizar la línea:
```python
self.opciones_avanzadas = QCheckBox("Modo selección manual")
```
Cambiar a:
```python
self.opciones_avanzadas = QCheckBox("Avanzado")
```

**Cambio 3: Agregar método `set_mode(index: int)`**

Agregar el método al final de la clase (después de todos los métodos existentes), antes del bloque `if __name__ == "__main__":` si existe, o simplemente al final:

```python
def set_mode(self, mode_index: int):
    """Cambia el modo activo desde el selector externo (QButtonGroup en slideshow_viewer).

    Phase 4 (D-06): reemplaza los checkboxes internos auto_mode_checkbox y
    crop_mode_checkbox como mecanismo de cambio de modo. Los métodos internos
    _toggle_auto_mode y _toggle_crop_mode se preservan para compatibilidad pero
    ya no son invocados por checkboxes de UI (los checkboxes están ocultos).

    mode_index:
        0 — Selección: modo por defecto, muestra panel_seleccion
        1 — Recorte: activa crop mode
        2 — Automático: activa auto detection YOLO
    """
    if mode_index == 0:
        # Desactivar modos especiales
        if self.crop_mode_enabled:
            self.crop_mode_enabled = False
            self.request_redraw.emit()
        if self.auto_mode_enabled:
            self.auto_mode_enabled = False
    elif mode_index == 1:
        # Activar modo recorte, desactivar auto
        if self.auto_mode_enabled:
            self.auto_mode_enabled = False
        self.crop_mode_enabled = True
        self.request_redraw.emit()
    elif mode_index == 2:
        # Activar auto YOLO, desactivar crop
        if self.crop_mode_enabled:
            self.crop_mode_enabled = False
        if not self.auto_mode_enabled:
            self.auto_mode_enabled = True
            # Correr detección automática si hay imagen disponible
            if self._working_image is not None:
                self._run_auto_detection()
```

CONSTRAINT: `_toggle_auto_mode`, `_toggle_crop_mode`, `_toggle_manual_mode` NO se eliminan — siguen existiendo porque internamente la lógica de `_run_auto_detection`, `_apply_crop`, etc. puede llamarlos. Solo los checkboxes de UI dejan de ser el gatillo principal.

CONSTRAINT: NO eliminar `self.auto_mode_checkbox` ni `self.crop_mode_checkbox` como atributos — pueden ser referenciados en otros métodos (`_toggle_auto_mode` los usa). Solo ocultarlos en UI.

CONSTRAINT: `decorate_pixmap`, `is_preview_active`, `accept_preview`, `revert_preview`, y todas las señales públicas deben permanecer intactas.
  </action>
  <verify>
    <automated>cd "c:/Users/Felix/Desktop/Python/manhwa_ocr/SmartStitch" && python -m py_compile WatermarkRemove/ui/components/watermark_processor.py && echo COMPILE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile` sale 0 e imprime COMPILE_OK
    - `python -c "import sys; src=open('WatermarkRemove/ui/components/watermark_processor.py').read(); assert 'panel_seleccion' in src; assert 'panel_recorte' in src; assert 'panel_auto' in src; assert 'def set_mode' in src; print('PANELS_OK')"` imprime PANELS_OK
    - El método `set_mode` contiene los tres branches (mode_index 0, 1, 2)
    - `self.auto_mode_checkbox` y `self.crop_mode_checkbox` siguen existiendo en el código (no eliminados)
    - `decorate_pixmap` sigue presente (grep retorna >=1)
    - El checkbox `opciones_avanzadas` tiene el texto `"Avanzado"` (no `"Modo selección manual"`)
  </acceptance_criteria>
  <done>WatermarkProcessor expone panel_seleccion, panel_recorte, panel_auto y el método set_mode(index) para wiring externo desde el QButtonGroup del slideshow_viewer.</done>
</task>

<task type="auto">
  <name>Task 3: Ensamblar panel de controles reorganizado en slideshow_viewer</name>
  <files>WatermarkRemove/ui/slideshow_viewer.py</files>
  <read_first>
    - WatermarkRemove/ui/slideshow_viewer.py (leer el estado actual post Plan 04-01 — especialmente _create_controls_panel y los imports existentes)
    - WatermarkRemove/ui/components/watermark_processor.py (confirmar que panel_seleccion, panel_recorte, panel_auto, set_mode existen tras Task 2)
    - WatermarkRemove/ui/components/navigation_controller.py (confirmar que create_nav_controls_widget existe tras Task 1)
  </read_first>
  <action>
Realizar dos modificaciones en `WatermarkRemove/ui/slideshow_viewer.py`:

**1. Actualizar imports**

Agregar a la línea de imports de PySide6.QtWidgets: `QStackedWidget, QButtonGroup`. El import actual tiene `QSplitter` desde Plan 04-01. Resultado:

```python
from PySide6.QtWidgets import (
    QDialog, QVBoxLayout, QHBoxLayout, QPushButton, QWidget,
    QScrollArea, QGroupBox, QMessageBox, QSplitter,
    QStackedWidget, QButtonGroup,
)
```

**2. Reemplazar `_create_controls_panel()` completo**

El método fue modificado en Plan 04-01 para eliminar el QScrollArea. Ahora reemplazarlo con la versión completa con las cuatro secciones (D-04):

```python
def _create_controls_panel(self) -> QWidget:
    """Panel de controles (izquierda del splitter) — Phase 4 D-04.

    Estructura vertical:
      1. Grupo Navegación (QGroupBox permanente — prev/next + contador + filename + finish/cancel)
      2. Selector de modo (QButtonGroup con 3 QPushButton checkables)
      3. QStackedWidget (página 0=Selección, 1=Recorte, 2=Automático)
      4. Grupo Training Data (QGroupBox permanente — TrainingDataCollector)
    """
    panel = QWidget()
    layout = QVBoxLayout(panel)
    layout.setSpacing(8)
    layout.setContentsMargins(0, 0, 6, 0)

    # === 1. Grupo Navegación ===
    # create_nav_controls_widget() retorna un QGroupBox "Navegación" con
    # counter_label, filename_label, prev_btn, next_btn ya conectados.
    nav_widget = self.navigation.create_nav_controls_widget()
    # Agregar finish_btn y cancel_btn al QGroupBox de navegación
    nav_inner_layout = nav_widget.layout()  # QVBoxLayout definido en create_nav_controls_widget
    
    btn_row = QHBoxLayout()
    btn_row.setSpacing(5)
    
    self.finish_btn = QPushButton("Finalizar y Procesar")
    self.finish_btn.clicked.connect(self._finish_review)
    self.finish_btn.setMaximumHeight(40)
    btn_row.addWidget(self.finish_btn)
    
    self.cancel_btn = QPushButton("Cancelar")
    self.cancel_btn.clicked.connect(self._cancel_review)
    self.cancel_btn.setMaximumHeight(40)
    btn_row.addWidget(self.cancel_btn)
    
    nav_inner_layout.addLayout(btn_row)  # addLayout (no addItem) para agregar QHBoxLayout
    layout.addWidget(nav_widget)

    # === 2. Selector de Modo (QButtonGroup checkable, estilo tab) ===
    mode_selector = QWidget()
    mode_layout = QHBoxLayout(mode_selector)
    mode_layout.setSpacing(2)
    mode_layout.setContentsMargins(0, 0, 0, 0)

    self._mode_group = QButtonGroup(self)
    self._mode_group.setExclusive(True)

    mode_labels = ["Selección", "Recorte", "Automático"]
    for i, label in enumerate(mode_labels):
        btn = QPushButton(label)
        btn.setCheckable(True)
        btn.setObjectName("wm-mode-btn")
        self._mode_group.addButton(btn, i)
        mode_layout.addWidget(btn)

    # Selección activo por defecto
    self._mode_group.button(0).setChecked(True)
    layout.addWidget(mode_selector)

    # Deshabilitar botón Automático si YOLO no está disponible (D-08)
    self._check_yolo_availability()

    # === 3. QStackedWidget — una página por modo ===
    self._mode_stack = QStackedWidget()
    self._mode_stack.addWidget(self.processor.panel_seleccion)   # página 0
    self._mode_stack.addWidget(self.processor.panel_recorte)     # página 1
    self._mode_stack.addWidget(self.processor.panel_auto)        # página 2
    self._mode_stack.setCurrentIndex(0)

    self._mode_group.idClicked.connect(self._on_mode_changed)
    layout.addWidget(self._mode_stack, 1)

    # === 4. Grupo Training Data ===
    layout.addWidget(self.collector)

    return panel
```

**3. Agregar método `_on_mode_changed`**

Insertar justo antes del método `_log`:

```python
def _on_mode_changed(self, mode_index: int):
    """Slot: el QButtonGroup del selector de modo emitió idClicked.
    
    Actualiza el QStackedWidget y notifica al processor para que ajuste
    su estado interno (D-06).
    """
    self._mode_stack.setCurrentIndex(mode_index)
    self.processor.set_mode(mode_index)
```

**4. Agregar método `_check_yolo_availability`**

Insertar junto a `_on_mode_changed`:

```python
def _check_yolo_availability(self):
    """Verifica si el modelo YOLO está disponible al iniciar (D-08).
    
    La importación de auto_detector no falla incluso sin el modelo .onnx;
    el error ocurre al llamar detect_watermarks(). Verificar el archivo
    del modelo directamente.
    """
    try:
        import os
        from pathlib import Path
        # El modelo vive en WatermarkRemove/yolo/ — buscar cualquier .onnx
        yolo_dir = Path(os.path.dirname(os.path.dirname(os.path.abspath(__file__)))) / 'yolo'
        has_model = any(yolo_dir.glob('*.onnx')) if yolo_dir.exists() else False
        if not has_model:
            auto_btn = self._mode_group.button(2)
            if auto_btn is not None:
                auto_btn.setEnabled(False)
    except Exception:
        # Si algo falla, no deshabilitar — mejor fallar luego en el intento de detección
        pass
```

CONSTRAINT: `_wire_signals()` NO se modifica. Los botones `finish_btn` y `cancel_btn` son recreados en este método (no vienen de Plan 04-01 — ese plan los tenía en una versión intermedia que ahora se sobreescribe completamente). Solo hay UN `_create_controls_panel()` al final.

CONSTRAINT: El atributo `self.navigation` ya existe en `__init__` cuando se llama `_setup_ui()`. El método `create_nav_controls_widget()` es seguro de llamar en este punto porque `self.navigation.__init__` ya corrió completo (incluyendo `_setup_ui()` de NavigationController).

CONSTRAINT: `self.processor.panel_seleccion`, `self.processor.panel_recorte`, `self.processor.panel_auto` ya existen porque `self.processor = WatermarkProcessor(...)` corre en `__init__` ANTES de `_setup_ui()`.

CONSTRAINT: Si `nav_inner_layout.addItem(btn_row)` no funciona (QVBoxLayout.addItem espera QLayoutItem, no QHBoxLayout directamente), usar `nav_inner_layout.addLayout(btn_row)` en su lugar. La firma correcta de QVBoxLayout para agregar un layout anidado es `addLayout(layout)`.
  </action>
  <verify>
    <automated>cd "c:/Users/Felix/Desktop/Python/manhwa_ocr/SmartStitch" && python -m py_compile WatermarkRemove/ui/components/navigation_controller.py WatermarkRemove/ui/components/watermark_processor.py WatermarkRemove/ui/slideshow_viewer.py && echo COMPILE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile` sobre los 3 archivos sale 0 e imprime COMPILE_OK
    - `python -c "import sys; src=open('WatermarkRemove/ui/components/watermark_processor.py').read(); assert 'panel_seleccion' in src; assert 'panel_recorte' in src; assert 'panel_auto' in src; assert 'set_mode' in src; print('PANELS_OK')"` imprime PANELS_OK
    - `python -c "import sys; src=open('WatermarkRemove/ui/components/navigation_controller.py').read(); assert 'create_nav_controls_widget' in src; print('NAV_SPLIT_OK')"` imprime NAV_SPLIT_OK
    - `python -c "import sys; src=open('WatermarkRemove/ui/slideshow_viewer.py').read(); assert 'QStackedWidget' in src; assert 'QButtonGroup' in src; assert '_on_mode_changed' in src; assert '_check_yolo_availability' in src; print('STACK_OK')"` imprime STACK_OK
    - `slideshow_viewer.py` NO contiene referencia a `auto_mode_checkbox.stateChanged` (el checkbox interno del processor ya no es el gatillo de modo en el viewer)
  </acceptance_criteria>
  <done>El panel de controles del SlideshowViewer tiene las cuatro secciones de D-04: Grupo Navegación con finish/cancel, selector de modo QButtonGroup, QStackedWidget con 3 páginas, Grupo Training Data. El cambio de modo actualiza el stack y llama processor.set_mode(). YOLO se verifica al arrancar.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → _check_yolo_availability | Path al directorio yolo/ para verificar .onnx |
| QButtonGroup → QStackedWidget → WatermarkProcessor | El índice de modo controla qué panel se muestra y qué estado activa el processor |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering | _check_yolo_availability | accept | Solo lectura de filesystem (glob .onnx); try/except captura cualquier error — el botón Automático queda habilitado ante fallo, lo que solo causa que el usuario descubra el error al intentar detectar |
| T-04-04 | Tampering | QButtonGroup.idClicked → set_mode | accept | mode_index está acotado a {0,1,2} por el QButtonGroup — no hay input externo no controlado; set_mode() ignora índices fuera de rango implícitamente |
| T-04-05 | DoS | Widget reparenting (create_nav_controls_widget) | mitigate | Los widgets de instancia (counter_label, etc.) son reparentados una sola vez en el ciclo de vida del dialog; si create_nav_controls_widget() fuera llamado dos veces los widgets quedarían en el último parent — documentar que solo se llama una vez desde _create_controls_panel |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No se instalan paquetes en Plan 04-02 |
</threat_model>

<verification>
Ejecutar en orden:

```
cd "c:/Users/Felix/Desktop/Python/manhwa_ocr/SmartStitch"

python -m py_compile WatermarkRemove/ui/components/navigation_controller.py WatermarkRemove/ui/components/watermark_processor.py WatermarkRemove/ui/slideshow_viewer.py && echo COMPILE_OK

python -c "import sys; src=open('WatermarkRemove/ui/components/watermark_processor.py').read(); assert 'panel_seleccion' in src; assert 'panel_recorte' in src; assert 'panel_auto' in src; assert 'set_mode' in src; print('PANELS_OK')"

python -c "import sys; src=open('WatermarkRemove/ui/components/navigation_controller.py').read(); assert 'create_nav_controls_widget' in src; print('NAV_SPLIT_OK')"

python -c "import sys; src=open('WatermarkRemove/ui/slideshow_viewer.py').read(); assert 'QStackedWidget' in src; assert 'QButtonGroup' in src; assert '_on_mode_changed' in src; print('STACK_OK')"
```

Los cuatro comandos deben imprimir COMPILE_OK, PANELS_OK, NAV_SPLIT_OK, STACK_OK.
</verification>

<success_criteria>
- NavigationController tiene create_nav_controls_widget() que retorna QGroupBox "Navegación" con los widgets de instancia reparentados
- WatermarkProcessor tiene panel_seleccion, panel_recorte, panel_auto y set_mode(index)
- SlideshowViewer._create_controls_panel() ensambla las 4 secciones con QButtonGroup + QStackedWidget
- _check_yolo_availability() deshabilita el botón [Automático] si no hay .onnx en yolo/
- Todos los signal/slot wiring existentes en _wire_signals() siguen funcionando intactos
</success_criteria>

<output>
Create `.planning/phases/04-visual-polish/04-02-SUMMARY.md` when done
</output>
