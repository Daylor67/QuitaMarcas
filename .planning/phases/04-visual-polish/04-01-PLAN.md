---
phase: 04-visual-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - WatermarkRemove/services/wm_persistence.py
  - WatermarkRemove/ui/slideshow_viewer.py
autonomous: true
requirements: [UI-01]
must_haves:
  truths:
    - "El visor de imagen ocupa el 65% del ancho inicial (585 px de 900) y el panel de controles el 35% (315 px)"
    - "El usuario puede arrastrar el divisor para redistribuir el espacio entre visor y panel"
    - "La posición del splitter se restaura entre sesiones — cerrar y reabrir el diálogo recupera las proporciones anteriores"
    - "El panel de controles NO tiene QScrollArea wrapper — se comprime si la ventana se achica (D-03)"
  artifacts:
    - path: "WatermarkRemove/services/wm_persistence.py"
      provides: "Métodos get_splitter_sizes / set_splitter_sizes que persisten en wm_settings.json"
      contains: "def get_splitter_sizes"
    - path: "WatermarkRemove/ui/slideshow_viewer.py"
      provides: "Layout QSplitter horizontal que reemplaza el QHBoxLayout fijo"
      contains: "QSplitter"
  key_links:
    - from: "WatermarkRemove/ui/slideshow_viewer.py"
      to: "WatermarkRemove/services/wm_persistence.py"
      via: "wm_persistence.get_splitter_sizes / set_splitter_sizes en _setup_ui y _on_splitter_moved"
      pattern: "get_splitter_sizes|set_splitter_sizes"
    - from: "WatermarkRemove/ui/slideshow_viewer.py"
      to: "QSplitter"
      via: "self._splitter.addWidget(controls_panel) + self._splitter.addWidget(self.navigation)"
      pattern: "self\\._splitter\\.addWidget"
---

<objective>
Reemplazar el layout QHBoxLayout con ancho fijo (280 px) por un QSplitter horizontal redimensionable, y extender WmPersistenceService con métodos de lectura/escritura de splitter sizes para que la proporción persista entre sesiones.

Purpose: UI-01 exige que el visor de imagen domine el espacio (65%+ del ancho). El layout actual fija el panel de controles a 280 px sin posibilidad de que el usuario redistribuya. El QSplitter da control inmediato al usuario y la persistencia evita que tenga que ajustarlo en cada sesión.
Output: WmPersistenceService con 2 métodos nuevos + SlideshowViewer con QSplitter como layout principal + closeEvent que persiste el tamaño final.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-visual-polish/04-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extender WmPersistenceService con get_splitter_sizes / set_splitter_sizes</name>
  <files>WatermarkRemove/services/wm_persistence.py</files>
  <read_first>
    - WatermarkRemove/services/wm_persistence.py (patrón existente de get/set con UtilJson — leer completo; el archivo tiene 39 líneas)
  </read_first>
  <action>
Agregar dos métodos al final de la clase `WmPersistenceService` en `WatermarkRemove/services/wm_persistence.py`. No modificar ningún método existente.

`get_splitter_sizes(self, default: list) -> list`: Llama `UtilJson(self._path).get('splitter_sizes', default)`. Si el valor retornado es una lista no vacía de enteros (o convertible a int), retornarlo. Si es `None` o no es una lista, retornar `default`. Implementación concisa:

```python
def get_splitter_sizes(self, default: list) -> list:
    """Retorna los tamaños del splitter desde wm_settings.json. Default si no existe."""
    value = UtilJson(self._path).get('splitter_sizes', default)
    if isinstance(value, list) and len(value) > 0:
        return [int(v) for v in value]
    return list(default)
```

`set_splitter_sizes(self, sizes: list) -> None`: Llama `UtilJson(self._path).set('splitter_sizes', [int(v) for v in sizes])`. Convierte a lista de int porque `QSplitter.sizes()` retorna `list[int]` pero el tipo exacto puede variar entre plataformas.

```python
def set_splitter_sizes(self, sizes: list) -> None:
    """Persiste los tamaños del splitter en wm_settings.json."""
    UtilJson(self._path).set('splitter_sizes', [int(v) for v in sizes])
```

No agregar imports — `UtilJson` ya está importado en la línea 8 del archivo.
  </action>
  <verify>
    <automated>cd "c:/Users/Felix/Desktop/Python/manhwa_ocr/SmartStitch" && python -m py_compile WatermarkRemove/services/wm_persistence.py && echo COMPILE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile` sale 0 e imprime COMPILE_OK
    - El comando de integración a continuación imprime `[315, 585]` y `SPLITTER_PERSIST_OK`:
      `python -c "import sys; sys.path.insert(0,'.'); from WatermarkRemove.services.wm_persistence import WmPersistenceService; s=WmPersistenceService(); s.set_splitter_sizes([315,585]); print(s.get_splitter_sizes([100,200])); print('SPLITTER_PERSIST_OK')"`
    - `wm_persistence.py` contiene las cadenas `def get_splitter_sizes` y `def set_splitter_sizes`
    - Los 4 métodos previos (`get_last_crop_pixels`, `set_last_crop_pixels`, `get_last_watermark_folder`, `set_last_watermark_folder`) siguen presentes sin modificaciones
  </acceptance_criteria>
  <done>WmPersistenceService expone get_splitter_sizes/set_splitter_sizes, compila sin errores, y el round-trip de persistencia funciona.</done>
</task>

<task type="auto">
  <name>Task 2: Reemplazar QHBoxLayout fijo por QSplitter en SlideshowViewer</name>
  <files>WatermarkRemove/ui/slideshow_viewer.py</files>
  <read_first>
    - WatermarkRemove/ui/slideshow_viewer.py (leer completo — especialmente _setup_ui L123-135 y _create_controls_panel L140-189 que son los métodos a modificar)
  </read_first>
  <action>
Realizar cuatro modificaciones en `WatermarkRemove/ui/slideshow_viewer.py`:

**1. Actualizar imports al inicio del archivo**

Agregar `QSplitter` a la línea de imports de `PySide6.QtWidgets` y **eliminar** `QScrollArea` (ya no se usa en este archivo tras remover el wrapper). La línea actual (L19-22) importa `QDialog, QVBoxLayout, QHBoxLayout, QPushButton, QWidget, QScrollArea, QGroupBox, QMessageBox`. Cambiar por:

```python
from PySide6.QtWidgets import (
    QDialog, QVBoxLayout, QHBoxLayout, QPushButton, QWidget,
    QGroupBox, QMessageBox, QSplitter,
)
```

También agregar `wm_persistence` al import del barrel de servicios. La línea actual importa solo `NavigationController, WatermarkProcessor, TrainingDataCollector`. Agregar una línea separada debajo del import de components:

```python
from WatermarkRemove.services import wm_persistence
```

(Este import puede ya existir indirectamente via components — añadirlo explícitamente en slideshow_viewer para claridad y para usarlo en _setup_ui.)

**2. Refactorizar `_setup_ui()`**

Reemplazar el cuerpo completo del método `_setup_ui` (L123-135) por:

```python
def _setup_ui(self):
    """Layout principal: QSplitter horizontal (65% visor / 35% controles).
    
    Reemplaza el QHBoxLayout con ancho fijo de 280 px (Phase 4 — D-01, D-02).
    La proporción inicial es [315, 585] (35%/65% de 900 px iniciales).
    """
    self.setWindowTitle("Revisión de Imágenes")
    self.setModal(True)
    self.resize(900, 650)

    outer = QVBoxLayout(self)
    outer.setContentsMargins(10, 10, 10, 10)
    outer.setSpacing(0)

    self._splitter = QSplitter(Qt.Orientation.Horizontal)
    self._splitter.addWidget(self._create_controls_panel())
    self._splitter.addWidget(self.navigation)
    self._splitter.setSizes(wm_persistence.get_splitter_sizes([315, 585]))
    self._splitter.splitterMoved.connect(self._on_splitter_moved)

    outer.addWidget(self._splitter)
```

**3. Refactorizar `_create_controls_panel()`**

Reemplazar el cuerpo completo del método `_create_controls_panel` (L140-189). El objetivo es:
- Eliminar `panel.setFixedWidth(self.controls_panel_width)` (D-01: el splitter controla el ancho)
- Eliminar el `QScrollArea` wrapper completo (D-03: sin scroll en panel de controles)
- Eliminar el `setStyleSheet` inline del scroll (D-12/D-13)
- Conservar el contenido (processor + nav_group con finish/cancel + collector + stretch)
- Eliminar el `setStyleSheet` inline de `finish_btn` y `cancel_btn` (se elimina el hardcode; Plan 04-03 los restablece vía QSS global)

Nuevo cuerpo:

```python
def _create_controls_panel(self) -> QWidget:
    """Panel de controles (izquierda del splitter): WatermarkProcessor +
    grupo Navegación (finish/cancel) + TrainingDataCollector.
    
    Sin QScrollArea (D-03) y sin ancho fijo (D-01 — el splitter lo controla).
    """
    panel = QWidget()
    layout = QVBoxLayout(panel)
    layout.setSpacing(10)
    layout.setContentsMargins(0, 0, 6, 0)

    # === 1. WatermarkProcessor (sus GroupBoxes "Selección" + "Auto") ===
    layout.addWidget(self.processor)

    # === 2. Grupo Navegación (finish + cancel) ===
    nav_group = QGroupBox("Navegación")
    nav_action_layout = QHBoxLayout(nav_group)
    nav_action_layout.setSpacing(5)

    self.finish_btn = QPushButton("Finalizar y Procesar")
    self.finish_btn.clicked.connect(self._finish_review)
    self.finish_btn.setMaximumHeight(40)
    nav_action_layout.addWidget(self.finish_btn)

    self.cancel_btn = QPushButton("Cancelar")
    self.cancel_btn.clicked.connect(self._cancel_review)
    self.cancel_btn.setMaximumHeight(40)
    nav_action_layout.addWidget(self.cancel_btn)

    layout.addWidget(nav_group)

    # === 3. TrainingDataCollector (su propio GroupBox "Datos recopilados") ===
    layout.addWidget(self.collector)
    layout.addStretch(1)
    return panel
```

Nota: se eliminan los `setStyleSheet()` de `finish_btn` y `cancel_btn` porque Plan 04-03 los asignará vía `setObjectName()` + QSS global. El título del QGroupBox cambia de `"✳️ Navegación"` a `"Navegación"` (D-09: sin emojis).

**4. Agregar métodos `_on_splitter_moved` y `closeEvent`**

Insertar los dos métodos nuevos justo antes del método `_log` (L191):

```python
def _on_splitter_moved(self, pos: int, index: int):
    """Slot: persiste los sizes del splitter al moverlo (D-02)."""
    wm_persistence.set_splitter_sizes(list(self._splitter.sizes()))

def closeEvent(self, event):
    """Persiste el tamaño del splitter al cerrar el diálogo (D-02)."""
    wm_persistence.set_splitter_sizes(list(self._splitter.sizes()))
    super().closeEvent(event)
```

CONSTRAINT: No modificar `_wire_signals`, `_sync_initial_state`, `keyPressEvent`, `_finish_review`, `_cancel_review`, ni los métodos de la API pública (`get_approved`, `get_output_folder`, `has_processed_images`). Solo se modifican `_setup_ui`, `_create_controls_panel`, y se agregan `_on_splitter_moved` + `closeEvent`.

CONSTRAINT: El `self.controls_panel_width = 280` en `__init__` (L53) puede quedar como está — `_request_window_resize` en NavigationController lo sigue usando para calcular el ancho sugerido de ventana. No eliminar ese atributo.
  </action>
  <verify>
    <automated>cd "c:/Users/Felix/Desktop/Python/manhwa_ocr/SmartStitch" && python -m py_compile WatermarkRemove/ui/slideshow_viewer.py && echo COMPILE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `python -m py_compile WatermarkRemove/ui/slideshow_viewer.py` sale 0 e imprime COMPILE_OK
    - `python -c "import sys; src=open('WatermarkRemove/ui/slideshow_viewer.py').read(); assert 'QSplitter' in src, 'QSplitter missing'; assert 'setSizes' in src, 'setSizes missing'; assert 'setFixedWidth' not in src, 'setFixedWidth still present'; assert 'QScrollArea' not in src, 'QScrollArea still in imports (dead import after removing wrapper)'; print('LAYOUT_OK')"` imprime `LAYOUT_OK`
    - `slideshow_viewer.py` contiene `_on_splitter_moved` y `closeEvent`
    - `slideshow_viewer.py` contiene `wm_persistence.get_splitter_sizes` y `wm_persistence.set_splitter_sizes`
    - `slideshow_viewer.py` NO contiene `setFixedWidth`
    - El grupo de navegación usa el título `"Navegación"` (sin emoji `✳️`)
    - Los botones `finish_btn` y `cancel_btn` NO tienen `setStyleSheet()` inline
  </acceptance_criteria>
  <done>SlideshowViewer usa QSplitter con proporción inicial 315/585, persiste los sizes al mover y al cerrar, y el panel de controles no tiene ancho fijo ni QScrollArea wrapper.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| wm_settings.json → WmPersistenceService | Datos leídos desde disco (enteros de tamaño del splitter) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | WmPersistenceService.get_splitter_sizes | mitigate | Defensivo `isinstance(value, list)` + conversión `int(v)` — valores corruptos en JSON caen al default `[315, 585]`; nunca se pasan directamente a `setSizes()` sin validar |
| T-04-02 | DoS | QSplitter.setSizes | accept | Tamaños inválidos (negativos o suma cero) son manejados internamente por Qt; el riesgo es solo visual (splitter en posición extraña), no un crash |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No se instalan paquetes en Plan 04-01 (solo PySide6.QtWidgets.QSplitter, ya en el entorno) |
</threat_model>

<verification>
Ejecutar en orden:

```
cd "c:/Users/Felix/Desktop/Python/manhwa_ocr/SmartStitch"

python -m py_compile WatermarkRemove/services/wm_persistence.py WatermarkRemove/ui/slideshow_viewer.py && echo COMPILE_OK

python -c "import sys; sys.path.insert(0,'.'); from WatermarkRemove.services.wm_persistence import WmPersistenceService; s=WmPersistenceService(); s.set_splitter_sizes([300,600]); print(s.get_splitter_sizes([315,585])); print('SPLITTER_PERSIST_OK')"

python -c "import sys; src=open('WatermarkRemove/ui/slideshow_viewer.py').read(); assert 'QSplitter' in src; assert 'setSizes' in src; assert 'setFixedWidth' not in src; print('LAYOUT_OK')"
```

Los tres comandos deben salir sin error: COMPILE_OK, `[300, 600]` + SPLITTER_PERSIST_OK, LAYOUT_OK.
</verification>

<success_criteria>
- WmPersistenceService tiene `get_splitter_sizes` y `set_splitter_sizes`; el round-trip JSON funciona
- SlideshowViewer usa QSplitter como layout principal con `setSizes([315, 585])` por defecto
- No hay `setFixedWidth` ni `QScrollArea` wrapper en el panel de controles
- `closeEvent` y `_on_splitter_moved` persisten los sizes vía `wm_persistence`
- Ningún método de la API pública fue modificado
</success_criteria>

<output>
Create `.planning/phases/04-visual-polish/04-01-SUMMARY.md` when done
</output>
